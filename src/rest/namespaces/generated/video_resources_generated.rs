// Code generated by scripts/generate_rest.py; DO NOT EDIT.
//
// Regenerate with:
//   python3 scripts/generate_rest.py
//
// Generated REST resources for the 'video' namespace.

use std::collections::HashMap;

use serde_json::{Map, Value};

use crate::rest::error::SignalWireRestError;
use crate::rest::generated_bases::{BaseResource, CrudResource, ReadResource};
use crate::rest::http_client::HttpClient;
use crate::rest::pagination::PaginatedIterator;

/// Named request parameters for the generated method (Rust options-builder
/// idiom — required fields in `new`, optionals via setters, `extras` open door).
#[derive(Debug, Clone, Default)]
pub struct VideoConferencesCreateStreamRequest {
    url: String,
    extras: Map<String, Value>,
}

impl VideoConferencesCreateStreamRequest {
    /// Construct the request with its required fields.
    pub fn new(url: impl Into<String>) -> Self {
        VideoConferencesCreateStreamRequest {
            url: url.into(),
            ..Default::default()
        }
    }
    /// Add a forward-compat field the spec does not yet name.
    #[must_use]
    pub fn extra(mut self, key: impl Into<String>, value: impl Into<Value>) -> Self {
        self.extras.insert(key.into(), value.into());
        self
    }
    /// Assemble the `body` JSON object (unset optionals omitted).
    #[must_use]
    pub fn build(self) -> Value {
        let mut obj = Map::new();
        obj.insert("url".to_string(), Value::from(self.url));
        for (k, v) in self.extras {
            obj.insert(k, v);
        }
        Value::Object(obj)
    }
}

/// Named request parameters for the generated method (Rust options-builder
/// idiom — required fields in `new`, optionals via setters, `extras` open door).
#[derive(Debug, Clone, Default)]
pub struct VideoRoomTokensCreateRequest {
    room_name: String,
    user_name: Option<String>,
    permissions: Option<serde_json::Value>,
    join_from: Option<String>,
    join_until: Option<String>,
    remove_at: Option<String>,
    remove_after_seconds_elapsed: Option<i64>,
    join_audio_muted: Option<bool>,
    join_video_muted: Option<bool>,
    auto_create_room: Option<bool>,
    enable_room_previews: Option<bool>,
    room_display_name: Option<String>,
    end_room_session_on_leave: Option<bool>,
    join_as: Option<String>,
    media_allowed: Option<String>,
    room_meta: Option<serde_json::Value>,
    meta: Option<serde_json::Value>,
    sync_audio_video: Option<bool>,
    extras: Map<String, Value>,
}

impl VideoRoomTokensCreateRequest {
    /// Construct the request with its required fields.
    pub fn new(room_name: impl Into<String>) -> Self {
        VideoRoomTokensCreateRequest {
            room_name: room_name.into(),
            ..Default::default()
        }
    }
    /// Set the optional `user_name` field.
    #[must_use]
    pub fn user_name(mut self, value: impl Into<String>) -> Self {
        self.user_name = Some(value.into());
        self
    }
    /// Set the optional `permissions` field.
    #[must_use]
    pub fn permissions(mut self, value: serde_json::Value) -> Self {
        self.permissions = Some(value);
        self
    }
    /// Set the optional `join_from` field.
    #[must_use]
    pub fn join_from(mut self, value: impl Into<String>) -> Self {
        self.join_from = Some(value.into());
        self
    }
    /// Set the optional `join_until` field.
    #[must_use]
    pub fn join_until(mut self, value: impl Into<String>) -> Self {
        self.join_until = Some(value.into());
        self
    }
    /// Set the optional `remove_at` field.
    #[must_use]
    pub fn remove_at(mut self, value: impl Into<String>) -> Self {
        self.remove_at = Some(value.into());
        self
    }
    /// Set the optional `remove_after_seconds_elapsed` field.
    #[must_use]
    pub fn remove_after_seconds_elapsed(mut self, value: i64) -> Self {
        self.remove_after_seconds_elapsed = Some(value);
        self
    }
    /// Set the optional `join_audio_muted` field.
    #[must_use]
    pub fn join_audio_muted(mut self, value: bool) -> Self {
        self.join_audio_muted = Some(value);
        self
    }
    /// Set the optional `join_video_muted` field.
    #[must_use]
    pub fn join_video_muted(mut self, value: bool) -> Self {
        self.join_video_muted = Some(value);
        self
    }
    /// Set the optional `auto_create_room` field.
    #[must_use]
    pub fn auto_create_room(mut self, value: bool) -> Self {
        self.auto_create_room = Some(value);
        self
    }
    /// Set the optional `enable_room_previews` field.
    #[must_use]
    pub fn enable_room_previews(mut self, value: bool) -> Self {
        self.enable_room_previews = Some(value);
        self
    }
    /// Set the optional `room_display_name` field.
    #[must_use]
    pub fn room_display_name(mut self, value: impl Into<String>) -> Self {
        self.room_display_name = Some(value.into());
        self
    }
    /// Set the optional `end_room_session_on_leave` field.
    #[must_use]
    pub fn end_room_session_on_leave(mut self, value: bool) -> Self {
        self.end_room_session_on_leave = Some(value);
        self
    }
    /// Set the optional `join_as` field.
    #[must_use]
    pub fn join_as(mut self, value: impl Into<String>) -> Self {
        self.join_as = Some(value.into());
        self
    }
    /// Set the optional `media_allowed` field.
    #[must_use]
    pub fn media_allowed(mut self, value: impl Into<String>) -> Self {
        self.media_allowed = Some(value.into());
        self
    }
    /// Set the optional `room_meta` field.
    #[must_use]
    pub fn room_meta(mut self, value: serde_json::Value) -> Self {
        self.room_meta = Some(value);
        self
    }
    /// Set the optional `meta` field.
    #[must_use]
    pub fn meta(mut self, value: serde_json::Value) -> Self {
        self.meta = Some(value);
        self
    }
    /// Set the optional `sync_audio_video` field.
    #[must_use]
    pub fn sync_audio_video(mut self, value: bool) -> Self {
        self.sync_audio_video = Some(value);
        self
    }
    /// Add a forward-compat field the spec does not yet name.
    #[must_use]
    pub fn extra(mut self, key: impl Into<String>, value: impl Into<Value>) -> Self {
        self.extras.insert(key.into(), value.into());
        self
    }
    /// Assemble the `body` JSON object (unset optionals omitted).
    #[must_use]
    pub fn build(self) -> Value {
        let mut obj = Map::new();
        obj.insert("room_name".to_string(), Value::from(self.room_name));
        if let Some(v) = self.user_name {
            obj.insert("user_name".to_string(), Value::from(v));
        }
        if let Some(v) = self.permissions {
            obj.insert("permissions".to_string(), v);
        }
        if let Some(v) = self.join_from {
            obj.insert("join_from".to_string(), Value::from(v));
        }
        if let Some(v) = self.join_until {
            obj.insert("join_until".to_string(), Value::from(v));
        }
        if let Some(v) = self.remove_at {
            obj.insert("remove_at".to_string(), Value::from(v));
        }
        if let Some(v) = self.remove_after_seconds_elapsed {
            obj.insert("remove_after_seconds_elapsed".to_string(), Value::from(v));
        }
        if let Some(v) = self.join_audio_muted {
            obj.insert("join_audio_muted".to_string(), Value::from(v));
        }
        if let Some(v) = self.join_video_muted {
            obj.insert("join_video_muted".to_string(), Value::from(v));
        }
        if let Some(v) = self.auto_create_room {
            obj.insert("auto_create_room".to_string(), Value::from(v));
        }
        if let Some(v) = self.enable_room_previews {
            obj.insert("enable_room_previews".to_string(), Value::from(v));
        }
        if let Some(v) = self.room_display_name {
            obj.insert("room_display_name".to_string(), Value::from(v));
        }
        if let Some(v) = self.end_room_session_on_leave {
            obj.insert("end_room_session_on_leave".to_string(), Value::from(v));
        }
        if let Some(v) = self.join_as {
            obj.insert("join_as".to_string(), Value::from(v));
        }
        if let Some(v) = self.media_allowed {
            obj.insert("media_allowed".to_string(), Value::from(v));
        }
        if let Some(v) = self.room_meta {
            obj.insert("room_meta".to_string(), v);
        }
        if let Some(v) = self.meta {
            obj.insert("meta".to_string(), v);
        }
        if let Some(v) = self.sync_audio_video {
            obj.insert("sync_audio_video".to_string(), Value::from(v));
        }
        for (k, v) in self.extras {
            obj.insert(k, v);
        }
        Value::Object(obj)
    }
}

/// Named request parameters for the generated method (Rust options-builder
/// idiom — required fields in `new`, optionals via setters, `extras` open door).
#[derive(Debug, Clone, Default)]
pub struct VideoRoomsCreateStreamRequest {
    url: String,
    extras: Map<String, Value>,
}

impl VideoRoomsCreateStreamRequest {
    /// Construct the request with its required fields.
    pub fn new(url: impl Into<String>) -> Self {
        VideoRoomsCreateStreamRequest {
            url: url.into(),
            ..Default::default()
        }
    }
    /// Add a forward-compat field the spec does not yet name.
    #[must_use]
    pub fn extra(mut self, key: impl Into<String>, value: impl Into<Value>) -> Self {
        self.extras.insert(key.into(), value.into());
        self
    }
    /// Assemble the `body` JSON object (unset optionals omitted).
    #[must_use]
    pub fn build(self) -> Value {
        let mut obj = Map::new();
        obj.insert("url".to_string(), Value::from(self.url));
        for (k, v) in self.extras {
            obj.insert(k, v);
        }
        Value::Object(obj)
    }
}

/// Named request parameters for the generated method (Rust options-builder
/// idiom — required fields in `new`, optionals via setters, `extras` open door).
#[derive(Debug, Clone, Default)]
pub struct VideoStreamsUpdateRequest {
    url: String,
    extras: Map<String, Value>,
}

impl VideoStreamsUpdateRequest {
    /// Construct the request with its required fields.
    pub fn new(url: impl Into<String>) -> Self {
        VideoStreamsUpdateRequest {
            url: url.into(),
            ..Default::default()
        }
    }
    /// Add a forward-compat field the spec does not yet name.
    #[must_use]
    pub fn extra(mut self, key: impl Into<String>, value: impl Into<Value>) -> Self {
        self.extras.insert(key.into(), value.into());
        self
    }
    /// Assemble the `body` JSON object (unset optionals omitted).
    #[must_use]
    pub fn build(self) -> Value {
        let mut obj = Map::new();
        obj.insert("url".to_string(), Value::from(self.url));
        for (k, v) in self.extras {
            obj.insert(k, v);
        }
        Value::Object(obj)
    }
}

/// `VideoConferenceTokens` resource for the SignalWire `video` REST API.
///
/// Composes [`BaseResource`] (its base path is baked in) and adds the
/// resource's own methods.
pub struct VideoConferenceTokens<'a> {
    base: BaseResource<'a>,
}

impl<'a> VideoConferenceTokens<'a> {
    /// Construct the resource; its base path (§4) is baked in.
    #[must_use]
    pub fn new(client: &'a HttpClient) -> Self {
        VideoConferenceTokens {
            base: BaseResource::new(client, "/api/video/conference_tokens"),
        }
    }

    #[must_use]
    pub fn base_path(&self) -> &str {
        self.base.base_path()
    }

    fn client(&self) -> &HttpClient {
        self.base.client()
    }

    fn path(&self, parts: &[&str]) -> String {
        self.base.path(parts)
    }

    /// `GET /conference_tokens/{id}` (generated operation method; query params).
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure, a non-2xx status.
    pub fn get(
        &self,
        id: &str,
        params: &HashMap<String, String>,
    ) -> Result<Value, SignalWireRestError> {
        self.client().get(&self.path(&[id]), params)
    }

    /// `POST /conference_tokens/{id}/reset` (generated operation method; no body).
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure, a non-2xx status.
    pub fn reset(&self, id: &str) -> Result<Value, SignalWireRestError> {
        self.client()
            .post(&self.path(&[id, "reset"]), &Value::Object(Map::new()))
    }
}

/// `VideoConferences` resource for the SignalWire `video` REST API.
///
/// Composes [`CrudResource`] (its base path is baked in) and adds the
/// resource's own methods.
pub struct VideoConferences<'a> {
    base: CrudResource<'a>,
}

impl<'a> VideoConferences<'a> {
    /// Construct the resource; its base path (§4) is baked in.
    #[must_use]
    pub fn new(client: &'a HttpClient) -> Self {
        VideoConferences {
            base: CrudResource::new(client, "/api/video/conferences", "PUT"),
        }
    }

    #[must_use]
    pub fn base_path(&self) -> &str {
        self.base.base_path()
    }

    fn client(&self) -> &HttpClient {
        self.base.client()
    }

    fn path(&self, parts: &[&str]) -> String {
        self.base.path(parts)
    }

    /// `list` (delegated to the base; GET base path).
    ///
    /// # Errors
    /// See the base resource.
    pub fn list(&self, params: &HashMap<String, String>) -> Result<Value, SignalWireRestError> {
        self.base.list(params)
    }

    /// `get` (delegated to the base; GET base/{id}).
    ///
    /// # Errors
    /// See the base resource.
    pub fn get(&self, id: &str) -> Result<Value, SignalWireRestError> {
        self.base.get(id)
    }

    /// `create` (delegated to the base; POST base path).
    ///
    /// # Errors
    /// See the base resource.
    pub fn create(&self, data: &Value) -> Result<Value, SignalWireRestError> {
        self.base.create(data)
    }

    /// `update` (delegated to the base; PUT/PATCH base/{id}).
    ///
    /// # Errors
    /// See the base resource.
    pub fn update(&self, id: &str, data: &Value) -> Result<Value, SignalWireRestError> {
        self.base.update(id, data)
    }

    /// `delete` (delegated to the base; DELETE base/{id}).
    ///
    /// # Errors
    /// See the base resource.
    pub fn delete(&self, id: &str) -> Result<Value, SignalWireRestError> {
        self.base.delete(id)
    }

    /// `GET /conferences/{id}/conference_tokens` (generated operation method; query params).
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure, a non-2xx status.
    pub fn list_conference_tokens(
        &self,
        id: &str,
        params: &HashMap<String, String>,
    ) -> Result<Value, SignalWireRestError> {
        self.client()
            .get(&self.path(&[id, "conference_tokens"]), params)
    }

    /// `GET /conferences/{id}/streams` (generated operation method; query params).
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure, a non-2xx status.
    pub fn list_streams(
        &self,
        id: &str,
        params: &HashMap<String, String>,
    ) -> Result<Value, SignalWireRestError> {
        self.client().get(&self.path(&[id, "streams"]), params)
    }

    /// `POST /conferences/{id}/streams` (generated operation method).
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure, a non-2xx
    /// status, or an unparseable response body.
    pub fn create_stream(
        &self,
        id: &str,
        request: VideoConferencesCreateStreamRequest,
    ) -> Result<Value, SignalWireRestError> {
        self.client()
            .post(&self.path(&[id, "streams"]), &request.build())
    }
}

/// `VideoRoomRecordings` resource for the SignalWire `video` REST API.
///
/// Composes [`BaseResource`] (its base path is baked in) and adds the
/// resource's own methods.
pub struct VideoRoomRecordings<'a> {
    base: BaseResource<'a>,
}

impl<'a> VideoRoomRecordings<'a> {
    /// Construct the resource; its base path (§4) is baked in.
    #[must_use]
    pub fn new(client: &'a HttpClient) -> Self {
        VideoRoomRecordings {
            base: BaseResource::new(client, "/api/video/room_recordings"),
        }
    }

    #[must_use]
    pub fn base_path(&self) -> &str {
        self.base.base_path()
    }

    fn client(&self) -> &HttpClient {
        self.base.client()
    }

    fn path(&self, parts: &[&str]) -> String {
        self.base.path(parts)
    }

    /// `GET /room_recordings` (generated operation method; query params).
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure, a non-2xx status.
    pub fn list(&self, params: &HashMap<String, String>) -> Result<Value, SignalWireRestError> {
        self.client().get(self.base_path(), params)
    }

    /// `GET /room_recordings/{id}` (generated operation method; query params).
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure, a non-2xx status.
    pub fn get(
        &self,
        id: &str,
        params: &HashMap<String, String>,
    ) -> Result<Value, SignalWireRestError> {
        self.client().get(&self.path(&[id]), params)
    }

    /// `DELETE /room_recordings/{id}` (generated operation method).
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure, a non-2xx status.
    pub fn delete(&self, id: &str) -> Result<Value, SignalWireRestError> {
        self.client().delete(&self.path(&[id]))
    }

    /// `GET /room_recordings/{id}/events` (generated operation method; query params).
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure, a non-2xx status.
    pub fn list_events(
        &self,
        id: &str,
        params: &HashMap<String, String>,
    ) -> Result<Value, SignalWireRestError> {
        self.client().get(&self.path(&[id, "events"]), params)
    }
}

/// `VideoRoomSessions` resource for the SignalWire `video` REST API.
///
/// Composes [`ReadResource`] (its base path is baked in) and adds the
/// resource's own methods.
pub struct VideoRoomSessions<'a> {
    base: ReadResource<'a>,
}

impl<'a> VideoRoomSessions<'a> {
    /// Construct the resource; its base path (§4) is baked in.
    #[must_use]
    pub fn new(client: &'a HttpClient) -> Self {
        VideoRoomSessions {
            base: ReadResource::new(client, "/api/video/room_sessions"),
        }
    }

    #[must_use]
    pub fn base_path(&self) -> &str {
        self.base.base_path()
    }

    fn client(&self) -> &HttpClient {
        self.base.client()
    }

    fn path(&self, parts: &[&str]) -> String {
        self.base.path(parts)
    }

    /// `list` (delegated to the base; GET base path).
    ///
    /// # Errors
    /// See the base resource.
    pub fn list(&self, params: &HashMap<String, String>) -> Result<Value, SignalWireRestError> {
        self.base.list(params)
    }

    /// `get` (delegated to the base; GET base/{id}).
    ///
    /// # Errors
    /// See the base resource.
    pub fn get(&self, id: &str) -> Result<Value, SignalWireRestError> {
        self.base.get(id)
    }

    /// `paginate` (delegated to the base): iterate every item across all
    /// pages, following the response's `links.next` cursor.
    #[must_use]
    pub fn paginate(&self, params: &HashMap<String, String>) -> PaginatedIterator<'a> {
        self.base.paginate(params)
    }

    /// `GET /room_sessions/{id}/events` (generated operation method; query params).
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure, a non-2xx status.
    pub fn list_events(
        &self,
        id: &str,
        params: &HashMap<String, String>,
    ) -> Result<Value, SignalWireRestError> {
        self.client().get(&self.path(&[id, "events"]), params)
    }

    /// `GET /room_sessions/{id}/members` (generated operation method; query params).
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure, a non-2xx status.
    pub fn list_members(
        &self,
        id: &str,
        params: &HashMap<String, String>,
    ) -> Result<Value, SignalWireRestError> {
        self.client().get(&self.path(&[id, "members"]), params)
    }

    /// `GET /room_sessions/{id}/recordings` (generated operation method; query params).
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure, a non-2xx status.
    pub fn list_recordings(
        &self,
        id: &str,
        params: &HashMap<String, String>,
    ) -> Result<Value, SignalWireRestError> {
        self.client().get(&self.path(&[id, "recordings"]), params)
    }
}

/// `VideoRoomTokens` resource for the SignalWire `video` REST API.
///
/// Composes [`BaseResource`] (its base path is baked in) and adds the
/// resource's own methods.
pub struct VideoRoomTokens<'a> {
    base: BaseResource<'a>,
}

impl<'a> VideoRoomTokens<'a> {
    /// Construct the resource; its base path (§4) is baked in.
    #[must_use]
    pub fn new(client: &'a HttpClient) -> Self {
        VideoRoomTokens {
            base: BaseResource::new(client, "/api/video/room_tokens"),
        }
    }

    #[must_use]
    pub fn base_path(&self) -> &str {
        self.base.base_path()
    }

    fn client(&self) -> &HttpClient {
        self.base.client()
    }

    /// `POST /room_tokens` (generated operation method).
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure, a non-2xx
    /// status, or an unparseable response body.
    pub fn create(
        &self,
        request: VideoRoomTokensCreateRequest,
    ) -> Result<Value, SignalWireRestError> {
        self.client().post(self.base_path(), &request.build())
    }
}

/// `VideoRooms` resource for the SignalWire `video` REST API.
///
/// Composes [`CrudResource`] (its base path is baked in) and adds the
/// resource's own methods.
pub struct VideoRooms<'a> {
    base: CrudResource<'a>,
}

impl<'a> VideoRooms<'a> {
    /// Construct the resource; its base path (§4) is baked in.
    #[must_use]
    pub fn new(client: &'a HttpClient) -> Self {
        VideoRooms {
            base: CrudResource::new(client, "/api/video/rooms", "PUT"),
        }
    }

    #[must_use]
    pub fn base_path(&self) -> &str {
        self.base.base_path()
    }

    fn client(&self) -> &HttpClient {
        self.base.client()
    }

    fn path(&self, parts: &[&str]) -> String {
        self.base.path(parts)
    }

    /// `list` (delegated to the base; GET base path).
    ///
    /// # Errors
    /// See the base resource.
    pub fn list(&self, params: &HashMap<String, String>) -> Result<Value, SignalWireRestError> {
        self.base.list(params)
    }

    /// `get` (delegated to the base; GET base/{id}).
    ///
    /// # Errors
    /// See the base resource.
    pub fn get(&self, id: &str) -> Result<Value, SignalWireRestError> {
        self.base.get(id)
    }

    /// `create` (delegated to the base; POST base path).
    ///
    /// # Errors
    /// See the base resource.
    pub fn create(&self, data: &Value) -> Result<Value, SignalWireRestError> {
        self.base.create(data)
    }

    /// `update` (delegated to the base; PUT/PATCH base/{id}).
    ///
    /// # Errors
    /// See the base resource.
    pub fn update(&self, id: &str, data: &Value) -> Result<Value, SignalWireRestError> {
        self.base.update(id, data)
    }

    /// `delete` (delegated to the base; DELETE base/{id}).
    ///
    /// # Errors
    /// See the base resource.
    pub fn delete(&self, id: &str) -> Result<Value, SignalWireRestError> {
        self.base.delete(id)
    }

    /// `GET /rooms/{id}/streams` (generated operation method; query params).
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure, a non-2xx status.
    pub fn list_streams(
        &self,
        id: &str,
        params: &HashMap<String, String>,
    ) -> Result<Value, SignalWireRestError> {
        self.client().get(&self.path(&[id, "streams"]), params)
    }

    /// `POST /rooms/{id}/streams` (generated operation method).
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure, a non-2xx
    /// status, or an unparseable response body.
    pub fn create_stream(
        &self,
        id: &str,
        request: VideoRoomsCreateStreamRequest,
    ) -> Result<Value, SignalWireRestError> {
        self.client()
            .post(&self.path(&[id, "streams"]), &request.build())
    }
}

/// `VideoStreams` resource for the SignalWire `video` REST API.
///
/// Composes [`BaseResource`] (its base path is baked in) and adds the
/// resource's own methods.
pub struct VideoStreams<'a> {
    base: BaseResource<'a>,
}

impl<'a> VideoStreams<'a> {
    /// Construct the resource; its base path (§4) is baked in.
    #[must_use]
    pub fn new(client: &'a HttpClient) -> Self {
        VideoStreams {
            base: BaseResource::new(client, "/api/video/streams"),
        }
    }

    #[must_use]
    pub fn base_path(&self) -> &str {
        self.base.base_path()
    }

    fn client(&self) -> &HttpClient {
        self.base.client()
    }

    fn path(&self, parts: &[&str]) -> String {
        self.base.path(parts)
    }

    /// `GET /streams/{id}` (generated operation method; query params).
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure, a non-2xx status.
    pub fn get(
        &self,
        id: &str,
        params: &HashMap<String, String>,
    ) -> Result<Value, SignalWireRestError> {
        self.client().get(&self.path(&[id]), params)
    }

    /// `PUT /streams/{id}` (generated operation method).
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure, a non-2xx
    /// status, or an unparseable response body.
    pub fn update(
        &self,
        id: &str,
        request: VideoStreamsUpdateRequest,
    ) -> Result<Value, SignalWireRestError> {
        self.client().put(&self.path(&[id]), &request.build())
    }

    /// `DELETE /streams/{id}` (generated operation method).
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure, a non-2xx status.
    pub fn delete(&self, id: &str) -> Result<Value, SignalWireRestError> {
        self.client().delete(&self.path(&[id]))
    }
}
