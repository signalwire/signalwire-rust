// Code generated by scripts/generate_rest.py; DO NOT EDIT.
//
// Regenerate with:
//   python3 scripts/generate_rest.py
//
// Generated REST resources for the 'calling' namespace.

use serde_json::{Map, Value};

use crate::rest::error::SignalWireRestError;
use crate::rest::http_client::HttpClient;

/// Named request parameters for the generated method (Rust options-builder
/// idiom — required fields in `new`, optionals via setters, `extras` open door).
#[derive(Debug, Clone, Default)]
pub struct CallingAiHoldRequest {
    timeout: Option<i64>,
    prompt: Option<String>,
    extras: Map<String, Value>,
}

impl CallingAiHoldRequest {
    /// Construct the request with its required fields.
    pub fn new() -> Self {
        CallingAiHoldRequest {
            ..Default::default()
        }
    }
    /// Set the optional `timeout` field.
    #[must_use]
    pub fn timeout(mut self, value: i64) -> Self {
        self.timeout = Some(value);
        self
    }
    /// Set the optional `prompt` field.
    #[must_use]
    pub fn prompt(mut self, value: impl Into<String>) -> Self {
        self.prompt = Some(value.into());
        self
    }
    /// Add a forward-compat field the spec does not yet name.
    #[must_use]
    pub fn extra(mut self, key: impl Into<String>, value: impl Into<Value>) -> Self {
        self.extras.insert(key.into(), value.into());
        self
    }
    /// Assemble the `params` JSON object (unset optionals omitted).
    #[must_use]
    pub fn build(self) -> Value {
        let mut obj = Map::new();
        if let Some(v) = self.timeout {
            obj.insert("timeout".to_string(), Value::from(v));
        }
        if let Some(v) = self.prompt {
            obj.insert("prompt".to_string(), Value::from(v));
        }
        for (k, v) in self.extras {
            obj.insert(k, v);
        }
        Value::Object(obj)
    }
}

/// Named request parameters for the generated method (Rust options-builder
/// idiom — required fields in `new`, optionals via setters, `extras` open door).
#[derive(Debug, Clone, Default)]
pub struct CallingAiMessageRequest {
    role: Option<String>,
    message_text: Option<String>,
    reset: Option<serde_json::Value>,
    global_data: Option<serde_json::Value>,
    extras: Map<String, Value>,
}

impl CallingAiMessageRequest {
    /// Construct the request with its required fields.
    pub fn new() -> Self {
        CallingAiMessageRequest {
            ..Default::default()
        }
    }
    /// Set the optional `role` field.
    #[must_use]
    pub fn role(mut self, value: impl Into<String>) -> Self {
        self.role = Some(value.into());
        self
    }
    /// Set the optional `message_text` field.
    #[must_use]
    pub fn message_text(mut self, value: impl Into<String>) -> Self {
        self.message_text = Some(value.into());
        self
    }
    /// Set the optional `reset` field.
    #[must_use]
    pub fn reset(mut self, value: serde_json::Value) -> Self {
        self.reset = Some(value);
        self
    }
    /// Set the optional `global_data` field.
    #[must_use]
    pub fn global_data(mut self, value: serde_json::Value) -> Self {
        self.global_data = Some(value);
        self
    }
    /// Add a forward-compat field the spec does not yet name.
    #[must_use]
    pub fn extra(mut self, key: impl Into<String>, value: impl Into<Value>) -> Self {
        self.extras.insert(key.into(), value.into());
        self
    }
    /// Assemble the `params` JSON object (unset optionals omitted).
    #[must_use]
    pub fn build(self) -> Value {
        let mut obj = Map::new();
        if let Some(v) = self.role {
            obj.insert("role".to_string(), Value::from(v));
        }
        if let Some(v) = self.message_text {
            obj.insert("message_text".to_string(), Value::from(v));
        }
        if let Some(v) = self.reset {
            obj.insert("reset".to_string(), v);
        }
        if let Some(v) = self.global_data {
            obj.insert("global_data".to_string(), v);
        }
        for (k, v) in self.extras {
            obj.insert(k, v);
        }
        Value::Object(obj)
    }
}

/// Named request parameters for the generated method (Rust options-builder
/// idiom — required fields in `new`, optionals via setters, `extras` open door).
#[derive(Debug, Clone, Default)]
pub struct CallingAiStopRequest {
    control_id: String,
    extras: Map<String, Value>,
}

impl CallingAiStopRequest {
    /// Construct the request with its required fields.
    pub fn new(control_id: impl Into<String>) -> Self {
        CallingAiStopRequest {
            control_id: control_id.into(),
            ..Default::default()
        }
    }
    /// Add a forward-compat field the spec does not yet name.
    #[must_use]
    pub fn extra(mut self, key: impl Into<String>, value: impl Into<Value>) -> Self {
        self.extras.insert(key.into(), value.into());
        self
    }
    /// Assemble the `params` JSON object (unset optionals omitted).
    #[must_use]
    pub fn build(self) -> Value {
        let mut obj = Map::new();
        obj.insert("control_id".to_string(), Value::from(self.control_id));
        for (k, v) in self.extras {
            obj.insert(k, v);
        }
        Value::Object(obj)
    }
}

/// Named request parameters for the generated method (Rust options-builder
/// idiom — required fields in `new`, optionals via setters, `extras` open door).
#[derive(Debug, Clone, Default)]
pub struct CallingAiUnholdRequest {
    prompt: Option<String>,
    extras: Map<String, Value>,
}

impl CallingAiUnholdRequest {
    /// Construct the request with its required fields.
    pub fn new() -> Self {
        CallingAiUnholdRequest {
            ..Default::default()
        }
    }
    /// Set the optional `prompt` field.
    #[must_use]
    pub fn prompt(mut self, value: impl Into<String>) -> Self {
        self.prompt = Some(value.into());
        self
    }
    /// Add a forward-compat field the spec does not yet name.
    #[must_use]
    pub fn extra(mut self, key: impl Into<String>, value: impl Into<Value>) -> Self {
        self.extras.insert(key.into(), value.into());
        self
    }
    /// Assemble the `params` JSON object (unset optionals omitted).
    #[must_use]
    pub fn build(self) -> Value {
        let mut obj = Map::new();
        if let Some(v) = self.prompt {
            obj.insert("prompt".to_string(), Value::from(v));
        }
        for (k, v) in self.extras {
            obj.insert(k, v);
        }
        Value::Object(obj)
    }
}

/// Named request parameters for the generated method (Rust options-builder
/// idiom — required fields in `new`, optionals via setters, `extras` open door).
#[derive(Debug, Clone, Default)]
pub struct CallingCollectRequest {
    control_id: Option<String>,
    initial_timeout: Option<f64>,
    digits: Option<serde_json::Value>,
    speech: Option<serde_json::Value>,
    continuous: Option<bool>,
    partial_results: Option<bool>,
    extras: Map<String, Value>,
}

impl CallingCollectRequest {
    /// Construct the request with its required fields.
    pub fn new() -> Self {
        CallingCollectRequest {
            ..Default::default()
        }
    }
    /// Set the optional `control_id` field.
    #[must_use]
    pub fn control_id(mut self, value: impl Into<String>) -> Self {
        self.control_id = Some(value.into());
        self
    }
    /// Set the optional `initial_timeout` field.
    #[must_use]
    pub fn initial_timeout(mut self, value: f64) -> Self {
        self.initial_timeout = Some(value);
        self
    }
    /// Set the optional `digits` field.
    #[must_use]
    pub fn digits(mut self, value: serde_json::Value) -> Self {
        self.digits = Some(value);
        self
    }
    /// Set the optional `speech` field.
    #[must_use]
    pub fn speech(mut self, value: serde_json::Value) -> Self {
        self.speech = Some(value);
        self
    }
    /// Set the optional `continuous` field.
    #[must_use]
    pub fn continuous(mut self, value: bool) -> Self {
        self.continuous = Some(value);
        self
    }
    /// Set the optional `partial_results` field.
    #[must_use]
    pub fn partial_results(mut self, value: bool) -> Self {
        self.partial_results = Some(value);
        self
    }
    /// Add a forward-compat field the spec does not yet name.
    #[must_use]
    pub fn extra(mut self, key: impl Into<String>, value: impl Into<Value>) -> Self {
        self.extras.insert(key.into(), value.into());
        self
    }
    /// Assemble the `params` JSON object (unset optionals omitted).
    #[must_use]
    pub fn build(self) -> Value {
        let mut obj = Map::new();
        if let Some(v) = self.control_id {
            obj.insert("control_id".to_string(), Value::from(v));
        }
        if let Some(v) = self.initial_timeout {
            obj.insert("initial_timeout".to_string(), Value::from(v));
        }
        if let Some(v) = self.digits {
            obj.insert("digits".to_string(), v);
        }
        if let Some(v) = self.speech {
            obj.insert("speech".to_string(), v);
        }
        if let Some(v) = self.continuous {
            obj.insert("continuous".to_string(), Value::from(v));
        }
        if let Some(v) = self.partial_results {
            obj.insert("partial_results".to_string(), Value::from(v));
        }
        for (k, v) in self.extras {
            obj.insert(k, v);
        }
        Value::Object(obj)
    }
}

/// Named request parameters for the generated method (Rust options-builder
/// idiom — required fields in `new`, optionals via setters, `extras` open door).
#[derive(Debug, Clone, Default)]
pub struct CallingCollectStartInputTimersRequest {
    control_id: String,
    extras: Map<String, Value>,
}

impl CallingCollectStartInputTimersRequest {
    /// Construct the request with its required fields.
    pub fn new(control_id: impl Into<String>) -> Self {
        CallingCollectStartInputTimersRequest {
            control_id: control_id.into(),
            ..Default::default()
        }
    }
    /// Add a forward-compat field the spec does not yet name.
    #[must_use]
    pub fn extra(mut self, key: impl Into<String>, value: impl Into<Value>) -> Self {
        self.extras.insert(key.into(), value.into());
        self
    }
    /// Assemble the `params` JSON object (unset optionals omitted).
    #[must_use]
    pub fn build(self) -> Value {
        let mut obj = Map::new();
        obj.insert("control_id".to_string(), Value::from(self.control_id));
        for (k, v) in self.extras {
            obj.insert(k, v);
        }
        Value::Object(obj)
    }
}

/// Named request parameters for the generated method (Rust options-builder
/// idiom — required fields in `new`, optionals via setters, `extras` open door).
#[derive(Debug, Clone, Default)]
pub struct CallingCollectStopRequest {
    control_id: String,
    extras: Map<String, Value>,
}

impl CallingCollectStopRequest {
    /// Construct the request with its required fields.
    pub fn new(control_id: impl Into<String>) -> Self {
        CallingCollectStopRequest {
            control_id: control_id.into(),
            ..Default::default()
        }
    }
    /// Add a forward-compat field the spec does not yet name.
    #[must_use]
    pub fn extra(mut self, key: impl Into<String>, value: impl Into<Value>) -> Self {
        self.extras.insert(key.into(), value.into());
        self
    }
    /// Assemble the `params` JSON object (unset optionals omitted).
    #[must_use]
    pub fn build(self) -> Value {
        let mut obj = Map::new();
        obj.insert("control_id".to_string(), Value::from(self.control_id));
        for (k, v) in self.extras {
            obj.insert(k, v);
        }
        Value::Object(obj)
    }
}

/// Named request parameters for the generated method (Rust options-builder
/// idiom — required fields in `new`, optionals via setters, `extras` open door).
#[derive(Debug, Clone, Default)]
pub struct CallingDenoiseRequest {
    extras: Map<String, Value>,
}

impl CallingDenoiseRequest {
    /// Construct the request with its required fields.
    pub fn new() -> Self {
        CallingDenoiseRequest {
            ..Default::default()
        }
    }
    /// Add a forward-compat field the spec does not yet name.
    #[must_use]
    pub fn extra(mut self, key: impl Into<String>, value: impl Into<Value>) -> Self {
        self.extras.insert(key.into(), value.into());
        self
    }
    /// Assemble the `params` JSON object (unset optionals omitted).
    #[must_use]
    pub fn build(self) -> Value {
        let mut obj = Map::new();
        for (k, v) in self.extras {
            obj.insert(k, v);
        }
        Value::Object(obj)
    }
}

/// Named request parameters for the generated method (Rust options-builder
/// idiom — required fields in `new`, optionals via setters, `extras` open door).
#[derive(Debug, Clone, Default)]
pub struct CallingDenoiseStopRequest {
    extras: Map<String, Value>,
}

impl CallingDenoiseStopRequest {
    /// Construct the request with its required fields.
    pub fn new() -> Self {
        CallingDenoiseStopRequest {
            ..Default::default()
        }
    }
    /// Add a forward-compat field the spec does not yet name.
    #[must_use]
    pub fn extra(mut self, key: impl Into<String>, value: impl Into<Value>) -> Self {
        self.extras.insert(key.into(), value.into());
        self
    }
    /// Assemble the `params` JSON object (unset optionals omitted).
    #[must_use]
    pub fn build(self) -> Value {
        let mut obj = Map::new();
        for (k, v) in self.extras {
            obj.insert(k, v);
        }
        Value::Object(obj)
    }
}

/// Named request parameters for the generated method (Rust options-builder
/// idiom — required fields in `new`, optionals via setters, `extras` open door).
#[derive(Debug, Clone, Default)]
pub struct CallingDetectRequest {
    detect: serde_json::Value,
    control_id: Option<String>,
    timeout: Option<f64>,
    extras: Map<String, Value>,
}

impl CallingDetectRequest {
    /// Construct the request with its required fields.
    pub fn new(detect: serde_json::Value) -> Self {
        CallingDetectRequest {
            detect,
            ..Default::default()
        }
    }
    /// Set the optional `control_id` field.
    #[must_use]
    pub fn control_id(mut self, value: impl Into<String>) -> Self {
        self.control_id = Some(value.into());
        self
    }
    /// Set the optional `timeout` field.
    #[must_use]
    pub fn timeout(mut self, value: f64) -> Self {
        self.timeout = Some(value);
        self
    }
    /// Add a forward-compat field the spec does not yet name.
    #[must_use]
    pub fn extra(mut self, key: impl Into<String>, value: impl Into<Value>) -> Self {
        self.extras.insert(key.into(), value.into());
        self
    }
    /// Assemble the `params` JSON object (unset optionals omitted).
    #[must_use]
    pub fn build(self) -> Value {
        let mut obj = Map::new();
        obj.insert("detect".to_string(), self.detect);
        if let Some(v) = self.control_id {
            obj.insert("control_id".to_string(), Value::from(v));
        }
        if let Some(v) = self.timeout {
            obj.insert("timeout".to_string(), Value::from(v));
        }
        for (k, v) in self.extras {
            obj.insert(k, v);
        }
        Value::Object(obj)
    }
}

/// Named request parameters for the generated method (Rust options-builder
/// idiom — required fields in `new`, optionals via setters, `extras` open door).
#[derive(Debug, Clone, Default)]
pub struct CallingDetectStopRequest {
    control_id: String,
    extras: Map<String, Value>,
}

impl CallingDetectStopRequest {
    /// Construct the request with its required fields.
    pub fn new(control_id: impl Into<String>) -> Self {
        CallingDetectStopRequest {
            control_id: control_id.into(),
            ..Default::default()
        }
    }
    /// Add a forward-compat field the spec does not yet name.
    #[must_use]
    pub fn extra(mut self, key: impl Into<String>, value: impl Into<Value>) -> Self {
        self.extras.insert(key.into(), value.into());
        self
    }
    /// Assemble the `params` JSON object (unset optionals omitted).
    #[must_use]
    pub fn build(self) -> Value {
        let mut obj = Map::new();
        obj.insert("control_id".to_string(), Value::from(self.control_id));
        for (k, v) in self.extras {
            obj.insert(k, v);
        }
        Value::Object(obj)
    }
}

/// Named request parameters for the generated method (Rust options-builder
/// idiom — required fields in `new`, optionals via setters, `extras` open door).
#[derive(Debug, Clone, Default)]
pub struct CallingDialRequest {
    from: String,
    to: String,
    caller_id: Option<String>,
    fallback_url: Option<String>,
    status_url: Option<String>,
    status_events: Option<serde_json::Value>,
    url_method: Option<String>,
    url: Option<String>,
    codecs: Option<serde_json::Value>,
    swml: Option<serde_json::Value>,
    extras: Map<String, Value>,
}

impl CallingDialRequest {
    /// Construct the request with its required fields.
    pub fn new(from: impl Into<String>, to: impl Into<String>) -> Self {
        CallingDialRequest {
            from: from.into(),
            to: to.into(),
            ..Default::default()
        }
    }
    /// Set the optional `caller_id` field.
    #[must_use]
    pub fn caller_id(mut self, value: impl Into<String>) -> Self {
        self.caller_id = Some(value.into());
        self
    }
    /// Set the optional `fallback_url` field.
    #[must_use]
    pub fn fallback_url(mut self, value: impl Into<String>) -> Self {
        self.fallback_url = Some(value.into());
        self
    }
    /// Set the optional `status_url` field.
    #[must_use]
    pub fn status_url(mut self, value: impl Into<String>) -> Self {
        self.status_url = Some(value.into());
        self
    }
    /// Set the optional `status_events` field.
    #[must_use]
    pub fn status_events(mut self, value: serde_json::Value) -> Self {
        self.status_events = Some(value);
        self
    }
    /// Set the optional `url_method` field.
    #[must_use]
    pub fn url_method(mut self, value: impl Into<String>) -> Self {
        self.url_method = Some(value.into());
        self
    }
    /// Set the optional `url` field.
    #[must_use]
    pub fn url(mut self, value: impl Into<String>) -> Self {
        self.url = Some(value.into());
        self
    }
    /// Set the optional `codecs` field.
    #[must_use]
    pub fn codecs(mut self, value: serde_json::Value) -> Self {
        self.codecs = Some(value);
        self
    }
    /// Set the optional `swml` field.
    #[must_use]
    pub fn swml(mut self, value: serde_json::Value) -> Self {
        self.swml = Some(value);
        self
    }
    /// Add a forward-compat field the spec does not yet name.
    #[must_use]
    pub fn extra(mut self, key: impl Into<String>, value: impl Into<Value>) -> Self {
        self.extras.insert(key.into(), value.into());
        self
    }
    /// Assemble the `params` JSON object (unset optionals omitted).
    #[must_use]
    pub fn build(self) -> Value {
        let mut obj = Map::new();
        obj.insert("from".to_string(), Value::from(self.from));
        obj.insert("to".to_string(), Value::from(self.to));
        if let Some(v) = self.caller_id {
            obj.insert("caller_id".to_string(), Value::from(v));
        }
        if let Some(v) = self.fallback_url {
            obj.insert("fallback_url".to_string(), Value::from(v));
        }
        if let Some(v) = self.status_url {
            obj.insert("status_url".to_string(), Value::from(v));
        }
        if let Some(v) = self.status_events {
            obj.insert("status_events".to_string(), v);
        }
        if let Some(v) = self.url_method {
            obj.insert("url_method".to_string(), Value::from(v));
        }
        if let Some(v) = self.url {
            obj.insert("url".to_string(), Value::from(v));
        }
        if let Some(v) = self.codecs {
            obj.insert("codecs".to_string(), v);
        }
        if let Some(v) = self.swml {
            obj.insert("swml".to_string(), v);
        }
        for (k, v) in self.extras {
            obj.insert(k, v);
        }
        Value::Object(obj)
    }
}

/// Named request parameters for the generated method (Rust options-builder
/// idiom — required fields in `new`, optionals via setters, `extras` open door).
#[derive(Debug, Clone, Default)]
pub struct CallingDisconnectRequest {
    extras: Map<String, Value>,
}

impl CallingDisconnectRequest {
    /// Construct the request with its required fields.
    pub fn new() -> Self {
        CallingDisconnectRequest {
            ..Default::default()
        }
    }
    /// Add a forward-compat field the spec does not yet name.
    #[must_use]
    pub fn extra(mut self, key: impl Into<String>, value: impl Into<Value>) -> Self {
        self.extras.insert(key.into(), value.into());
        self
    }
    /// Assemble the `params` JSON object (unset optionals omitted).
    #[must_use]
    pub fn build(self) -> Value {
        let mut obj = Map::new();
        for (k, v) in self.extras {
            obj.insert(k, v);
        }
        Value::Object(obj)
    }
}

/// Named request parameters for the generated method (Rust options-builder
/// idiom — required fields in `new`, optionals via setters, `extras` open door).
#[derive(Debug, Clone, Default)]
pub struct CallingEndRequest {
    reason: Option<String>,
    extras: Map<String, Value>,
}

impl CallingEndRequest {
    /// Construct the request with its required fields.
    pub fn new() -> Self {
        CallingEndRequest {
            ..Default::default()
        }
    }
    /// Set the optional `reason` field.
    #[must_use]
    pub fn reason(mut self, value: impl Into<String>) -> Self {
        self.reason = Some(value.into());
        self
    }
    /// Add a forward-compat field the spec does not yet name.
    #[must_use]
    pub fn extra(mut self, key: impl Into<String>, value: impl Into<Value>) -> Self {
        self.extras.insert(key.into(), value.into());
        self
    }
    /// Assemble the `params` JSON object (unset optionals omitted).
    #[must_use]
    pub fn build(self) -> Value {
        let mut obj = Map::new();
        if let Some(v) = self.reason {
            obj.insert("reason".to_string(), Value::from(v));
        }
        for (k, v) in self.extras {
            obj.insert(k, v);
        }
        Value::Object(obj)
    }
}

/// Named request parameters for the generated method (Rust options-builder
/// idiom — required fields in `new`, optionals via setters, `extras` open door).
#[derive(Debug, Clone, Default)]
pub struct CallingLiveTranscribeRequest {
    action: serde_json::Value,
    extras: Map<String, Value>,
}

impl CallingLiveTranscribeRequest {
    /// Construct the request with its required fields.
    pub fn new(action: serde_json::Value) -> Self {
        CallingLiveTranscribeRequest {
            action,
            ..Default::default()
        }
    }
    /// Add a forward-compat field the spec does not yet name.
    #[must_use]
    pub fn extra(mut self, key: impl Into<String>, value: impl Into<Value>) -> Self {
        self.extras.insert(key.into(), value.into());
        self
    }
    /// Assemble the `params` JSON object (unset optionals omitted).
    #[must_use]
    pub fn build(self) -> Value {
        let mut obj = Map::new();
        obj.insert("action".to_string(), self.action);
        for (k, v) in self.extras {
            obj.insert(k, v);
        }
        Value::Object(obj)
    }
}

/// Named request parameters for the generated method (Rust options-builder
/// idiom — required fields in `new`, optionals via setters, `extras` open door).
#[derive(Debug, Clone, Default)]
pub struct CallingLiveTranslateRequest {
    action: serde_json::Value,
    status_url: Option<String>,
    extras: Map<String, Value>,
}

impl CallingLiveTranslateRequest {
    /// Construct the request with its required fields.
    pub fn new(action: serde_json::Value) -> Self {
        CallingLiveTranslateRequest {
            action,
            ..Default::default()
        }
    }
    /// Set the optional `status_url` field.
    #[must_use]
    pub fn status_url(mut self, value: impl Into<String>) -> Self {
        self.status_url = Some(value.into());
        self
    }
    /// Add a forward-compat field the spec does not yet name.
    #[must_use]
    pub fn extra(mut self, key: impl Into<String>, value: impl Into<Value>) -> Self {
        self.extras.insert(key.into(), value.into());
        self
    }
    /// Assemble the `params` JSON object (unset optionals omitted).
    #[must_use]
    pub fn build(self) -> Value {
        let mut obj = Map::new();
        obj.insert("action".to_string(), self.action);
        if let Some(v) = self.status_url {
            obj.insert("status_url".to_string(), Value::from(v));
        }
        for (k, v) in self.extras {
            obj.insert(k, v);
        }
        Value::Object(obj)
    }
}

/// Named request parameters for the generated method (Rust options-builder
/// idiom — required fields in `new`, optionals via setters, `extras` open door).
#[derive(Debug, Clone, Default)]
pub struct CallingPlayPauseRequest {
    control_id: String,
    extras: Map<String, Value>,
}

impl CallingPlayPauseRequest {
    /// Construct the request with its required fields.
    pub fn new(control_id: impl Into<String>) -> Self {
        CallingPlayPauseRequest {
            control_id: control_id.into(),
            ..Default::default()
        }
    }
    /// Add a forward-compat field the spec does not yet name.
    #[must_use]
    pub fn extra(mut self, key: impl Into<String>, value: impl Into<Value>) -> Self {
        self.extras.insert(key.into(), value.into());
        self
    }
    /// Assemble the `params` JSON object (unset optionals omitted).
    #[must_use]
    pub fn build(self) -> Value {
        let mut obj = Map::new();
        obj.insert("control_id".to_string(), Value::from(self.control_id));
        for (k, v) in self.extras {
            obj.insert(k, v);
        }
        Value::Object(obj)
    }
}

/// Named request parameters for the generated method (Rust options-builder
/// idiom — required fields in `new`, optionals via setters, `extras` open door).
#[derive(Debug, Clone, Default)]
pub struct CallingPlayRequest {
    play: serde_json::Value,
    control_id: Option<String>,
    volume: Option<f64>,
    direction: Option<String>,
    r#loop: Option<i64>,
    status_url: Option<String>,
    extras: Map<String, Value>,
}

impl CallingPlayRequest {
    /// Construct the request with its required fields.
    pub fn new(play: serde_json::Value) -> Self {
        CallingPlayRequest {
            play,
            ..Default::default()
        }
    }
    /// Set the optional `control_id` field.
    #[must_use]
    pub fn control_id(mut self, value: impl Into<String>) -> Self {
        self.control_id = Some(value.into());
        self
    }
    /// Set the optional `volume` field.
    #[must_use]
    pub fn volume(mut self, value: f64) -> Self {
        self.volume = Some(value);
        self
    }
    /// Set the optional `direction` field.
    #[must_use]
    pub fn direction(mut self, value: impl Into<String>) -> Self {
        self.direction = Some(value.into());
        self
    }
    /// Set the optional `loop` field.
    #[must_use]
    pub fn r#loop(mut self, value: i64) -> Self {
        self.r#loop = Some(value);
        self
    }
    /// Set the optional `status_url` field.
    #[must_use]
    pub fn status_url(mut self, value: impl Into<String>) -> Self {
        self.status_url = Some(value.into());
        self
    }
    /// Add a forward-compat field the spec does not yet name.
    #[must_use]
    pub fn extra(mut self, key: impl Into<String>, value: impl Into<Value>) -> Self {
        self.extras.insert(key.into(), value.into());
        self
    }
    /// Assemble the `params` JSON object (unset optionals omitted).
    #[must_use]
    pub fn build(self) -> Value {
        let mut obj = Map::new();
        obj.insert("play".to_string(), self.play);
        if let Some(v) = self.control_id {
            obj.insert("control_id".to_string(), Value::from(v));
        }
        if let Some(v) = self.volume {
            obj.insert("volume".to_string(), Value::from(v));
        }
        if let Some(v) = self.direction {
            obj.insert("direction".to_string(), Value::from(v));
        }
        if let Some(v) = self.r#loop {
            obj.insert("loop".to_string(), Value::from(v));
        }
        if let Some(v) = self.status_url {
            obj.insert("status_url".to_string(), Value::from(v));
        }
        for (k, v) in self.extras {
            obj.insert(k, v);
        }
        Value::Object(obj)
    }
}

/// Named request parameters for the generated method (Rust options-builder
/// idiom — required fields in `new`, optionals via setters, `extras` open door).
#[derive(Debug, Clone, Default)]
pub struct CallingPlayResumeRequest {
    control_id: String,
    extras: Map<String, Value>,
}

impl CallingPlayResumeRequest {
    /// Construct the request with its required fields.
    pub fn new(control_id: impl Into<String>) -> Self {
        CallingPlayResumeRequest {
            control_id: control_id.into(),
            ..Default::default()
        }
    }
    /// Add a forward-compat field the spec does not yet name.
    #[must_use]
    pub fn extra(mut self, key: impl Into<String>, value: impl Into<Value>) -> Self {
        self.extras.insert(key.into(), value.into());
        self
    }
    /// Assemble the `params` JSON object (unset optionals omitted).
    #[must_use]
    pub fn build(self) -> Value {
        let mut obj = Map::new();
        obj.insert("control_id".to_string(), Value::from(self.control_id));
        for (k, v) in self.extras {
            obj.insert(k, v);
        }
        Value::Object(obj)
    }
}

/// Named request parameters for the generated method (Rust options-builder
/// idiom — required fields in `new`, optionals via setters, `extras` open door).
#[derive(Debug, Clone, Default)]
pub struct CallingPlayStopRequest {
    control_id: String,
    extras: Map<String, Value>,
}

impl CallingPlayStopRequest {
    /// Construct the request with its required fields.
    pub fn new(control_id: impl Into<String>) -> Self {
        CallingPlayStopRequest {
            control_id: control_id.into(),
            ..Default::default()
        }
    }
    /// Add a forward-compat field the spec does not yet name.
    #[must_use]
    pub fn extra(mut self, key: impl Into<String>, value: impl Into<Value>) -> Self {
        self.extras.insert(key.into(), value.into());
        self
    }
    /// Assemble the `params` JSON object (unset optionals omitted).
    #[must_use]
    pub fn build(self) -> Value {
        let mut obj = Map::new();
        obj.insert("control_id".to_string(), Value::from(self.control_id));
        for (k, v) in self.extras {
            obj.insert(k, v);
        }
        Value::Object(obj)
    }
}

/// Named request parameters for the generated method (Rust options-builder
/// idiom — required fields in `new`, optionals via setters, `extras` open door).
#[derive(Debug, Clone, Default)]
pub struct CallingPlayVolumeRequest {
    control_id: String,
    volume: f64,
    extras: Map<String, Value>,
}

impl CallingPlayVolumeRequest {
    /// Construct the request with its required fields.
    pub fn new(control_id: impl Into<String>, volume: f64) -> Self {
        CallingPlayVolumeRequest {
            control_id: control_id.into(),
            volume,
            ..Default::default()
        }
    }
    /// Add a forward-compat field the spec does not yet name.
    #[must_use]
    pub fn extra(mut self, key: impl Into<String>, value: impl Into<Value>) -> Self {
        self.extras.insert(key.into(), value.into());
        self
    }
    /// Assemble the `params` JSON object (unset optionals omitted).
    #[must_use]
    pub fn build(self) -> Value {
        let mut obj = Map::new();
        obj.insert("control_id".to_string(), Value::from(self.control_id));
        obj.insert("volume".to_string(), Value::from(self.volume));
        for (k, v) in self.extras {
            obj.insert(k, v);
        }
        Value::Object(obj)
    }
}

/// Named request parameters for the generated method (Rust options-builder
/// idiom — required fields in `new`, optionals via setters, `extras` open door).
#[derive(Debug, Clone, Default)]
pub struct CallingReceiveFaxStopRequest {
    control_id: String,
    extras: Map<String, Value>,
}

impl CallingReceiveFaxStopRequest {
    /// Construct the request with its required fields.
    pub fn new(control_id: impl Into<String>) -> Self {
        CallingReceiveFaxStopRequest {
            control_id: control_id.into(),
            ..Default::default()
        }
    }
    /// Add a forward-compat field the spec does not yet name.
    #[must_use]
    pub fn extra(mut self, key: impl Into<String>, value: impl Into<Value>) -> Self {
        self.extras.insert(key.into(), value.into());
        self
    }
    /// Assemble the `params` JSON object (unset optionals omitted).
    #[must_use]
    pub fn build(self) -> Value {
        let mut obj = Map::new();
        obj.insert("control_id".to_string(), Value::from(self.control_id));
        for (k, v) in self.extras {
            obj.insert(k, v);
        }
        Value::Object(obj)
    }
}

/// Named request parameters for the generated method (Rust options-builder
/// idiom — required fields in `new`, optionals via setters, `extras` open door).
#[derive(Debug, Clone, Default)]
pub struct CallingRecordPauseRequest {
    control_id: String,
    extras: Map<String, Value>,
}

impl CallingRecordPauseRequest {
    /// Construct the request with its required fields.
    pub fn new(control_id: impl Into<String>) -> Self {
        CallingRecordPauseRequest {
            control_id: control_id.into(),
            ..Default::default()
        }
    }
    /// Add a forward-compat field the spec does not yet name.
    #[must_use]
    pub fn extra(mut self, key: impl Into<String>, value: impl Into<Value>) -> Self {
        self.extras.insert(key.into(), value.into());
        self
    }
    /// Assemble the `params` JSON object (unset optionals omitted).
    #[must_use]
    pub fn build(self) -> Value {
        let mut obj = Map::new();
        obj.insert("control_id".to_string(), Value::from(self.control_id));
        for (k, v) in self.extras {
            obj.insert(k, v);
        }
        Value::Object(obj)
    }
}

/// Named request parameters for the generated method (Rust options-builder
/// idiom — required fields in `new`, optionals via setters, `extras` open door).
#[derive(Debug, Clone, Default)]
pub struct CallingRecordRequest {
    control_id: Option<String>,
    audio: Option<serde_json::Value>,
    status_url: Option<String>,
    extras: Map<String, Value>,
}

impl CallingRecordRequest {
    /// Construct the request with its required fields.
    pub fn new() -> Self {
        CallingRecordRequest {
            ..Default::default()
        }
    }
    /// Set the optional `control_id` field.
    #[must_use]
    pub fn control_id(mut self, value: impl Into<String>) -> Self {
        self.control_id = Some(value.into());
        self
    }
    /// Set the optional `audio` field.
    #[must_use]
    pub fn audio(mut self, value: serde_json::Value) -> Self {
        self.audio = Some(value);
        self
    }
    /// Set the optional `status_url` field.
    #[must_use]
    pub fn status_url(mut self, value: impl Into<String>) -> Self {
        self.status_url = Some(value.into());
        self
    }
    /// Add a forward-compat field the spec does not yet name.
    #[must_use]
    pub fn extra(mut self, key: impl Into<String>, value: impl Into<Value>) -> Self {
        self.extras.insert(key.into(), value.into());
        self
    }
    /// Assemble the `params` JSON object (unset optionals omitted).
    #[must_use]
    pub fn build(self) -> Value {
        let mut obj = Map::new();
        if let Some(v) = self.control_id {
            obj.insert("control_id".to_string(), Value::from(v));
        }
        if let Some(v) = self.audio {
            obj.insert("audio".to_string(), v);
        }
        if let Some(v) = self.status_url {
            obj.insert("status_url".to_string(), Value::from(v));
        }
        for (k, v) in self.extras {
            obj.insert(k, v);
        }
        Value::Object(obj)
    }
}

/// Named request parameters for the generated method (Rust options-builder
/// idiom — required fields in `new`, optionals via setters, `extras` open door).
#[derive(Debug, Clone, Default)]
pub struct CallingRecordResumeRequest {
    control_id: String,
    extras: Map<String, Value>,
}

impl CallingRecordResumeRequest {
    /// Construct the request with its required fields.
    pub fn new(control_id: impl Into<String>) -> Self {
        CallingRecordResumeRequest {
            control_id: control_id.into(),
            ..Default::default()
        }
    }
    /// Add a forward-compat field the spec does not yet name.
    #[must_use]
    pub fn extra(mut self, key: impl Into<String>, value: impl Into<Value>) -> Self {
        self.extras.insert(key.into(), value.into());
        self
    }
    /// Assemble the `params` JSON object (unset optionals omitted).
    #[must_use]
    pub fn build(self) -> Value {
        let mut obj = Map::new();
        obj.insert("control_id".to_string(), Value::from(self.control_id));
        for (k, v) in self.extras {
            obj.insert(k, v);
        }
        Value::Object(obj)
    }
}

/// Named request parameters for the generated method (Rust options-builder
/// idiom — required fields in `new`, optionals via setters, `extras` open door).
#[derive(Debug, Clone, Default)]
pub struct CallingRecordStopRequest {
    control_id: String,
    extras: Map<String, Value>,
}

impl CallingRecordStopRequest {
    /// Construct the request with its required fields.
    pub fn new(control_id: impl Into<String>) -> Self {
        CallingRecordStopRequest {
            control_id: control_id.into(),
            ..Default::default()
        }
    }
    /// Add a forward-compat field the spec does not yet name.
    #[must_use]
    pub fn extra(mut self, key: impl Into<String>, value: impl Into<Value>) -> Self {
        self.extras.insert(key.into(), value.into());
        self
    }
    /// Assemble the `params` JSON object (unset optionals omitted).
    #[must_use]
    pub fn build(self) -> Value {
        let mut obj = Map::new();
        obj.insert("control_id".to_string(), Value::from(self.control_id));
        for (k, v) in self.extras {
            obj.insert(k, v);
        }
        Value::Object(obj)
    }
}

/// Named request parameters for the generated method (Rust options-builder
/// idiom — required fields in `new`, optionals via setters, `extras` open door).
#[derive(Debug, Clone, Default)]
pub struct CallingReferRequest {
    device: serde_json::Value,
    status_url: Option<String>,
    extras: Map<String, Value>,
}

impl CallingReferRequest {
    /// Construct the request with its required fields.
    pub fn new(device: serde_json::Value) -> Self {
        CallingReferRequest {
            device,
            ..Default::default()
        }
    }
    /// Set the optional `status_url` field.
    #[must_use]
    pub fn status_url(mut self, value: impl Into<String>) -> Self {
        self.status_url = Some(value.into());
        self
    }
    /// Add a forward-compat field the spec does not yet name.
    #[must_use]
    pub fn extra(mut self, key: impl Into<String>, value: impl Into<Value>) -> Self {
        self.extras.insert(key.into(), value.into());
        self
    }
    /// Assemble the `params` JSON object (unset optionals omitted).
    #[must_use]
    pub fn build(self) -> Value {
        let mut obj = Map::new();
        obj.insert("device".to_string(), self.device);
        if let Some(v) = self.status_url {
            obj.insert("status_url".to_string(), Value::from(v));
        }
        for (k, v) in self.extras {
            obj.insert(k, v);
        }
        Value::Object(obj)
    }
}

/// Named request parameters for the generated method (Rust options-builder
/// idiom — required fields in `new`, optionals via setters, `extras` open door).
#[derive(Debug, Clone, Default)]
pub struct CallingSendFaxStopRequest {
    control_id: String,
    extras: Map<String, Value>,
}

impl CallingSendFaxStopRequest {
    /// Construct the request with its required fields.
    pub fn new(control_id: impl Into<String>) -> Self {
        CallingSendFaxStopRequest {
            control_id: control_id.into(),
            ..Default::default()
        }
    }
    /// Add a forward-compat field the spec does not yet name.
    #[must_use]
    pub fn extra(mut self, key: impl Into<String>, value: impl Into<Value>) -> Self {
        self.extras.insert(key.into(), value.into());
        self
    }
    /// Assemble the `params` JSON object (unset optionals omitted).
    #[must_use]
    pub fn build(self) -> Value {
        let mut obj = Map::new();
        obj.insert("control_id".to_string(), Value::from(self.control_id));
        for (k, v) in self.extras {
            obj.insert(k, v);
        }
        Value::Object(obj)
    }
}

/// Named request parameters for the generated method (Rust options-builder
/// idiom — required fields in `new`, optionals via setters, `extras` open door).
#[derive(Debug, Clone, Default)]
pub struct CallingStreamRequest {
    url: String,
    control_id: Option<String>,
    codec: Option<String>,
    track: Option<String>,
    authorization_bearer_token: Option<String>,
    custom_parameters: Option<serde_json::Value>,
    extras: Map<String, Value>,
}

impl CallingStreamRequest {
    /// Construct the request with its required fields.
    pub fn new(url: impl Into<String>) -> Self {
        CallingStreamRequest {
            url: url.into(),
            ..Default::default()
        }
    }
    /// Set the optional `control_id` field.
    #[must_use]
    pub fn control_id(mut self, value: impl Into<String>) -> Self {
        self.control_id = Some(value.into());
        self
    }
    /// Set the optional `codec` field.
    #[must_use]
    pub fn codec(mut self, value: impl Into<String>) -> Self {
        self.codec = Some(value.into());
        self
    }
    /// Set the optional `track` field.
    #[must_use]
    pub fn track(mut self, value: impl Into<String>) -> Self {
        self.track = Some(value.into());
        self
    }
    /// Set the optional `authorization_bearer_token` field.
    #[must_use]
    pub fn authorization_bearer_token(mut self, value: impl Into<String>) -> Self {
        self.authorization_bearer_token = Some(value.into());
        self
    }
    /// Set the optional `custom_parameters` field.
    #[must_use]
    pub fn custom_parameters(mut self, value: serde_json::Value) -> Self {
        self.custom_parameters = Some(value);
        self
    }
    /// Add a forward-compat field the spec does not yet name.
    #[must_use]
    pub fn extra(mut self, key: impl Into<String>, value: impl Into<Value>) -> Self {
        self.extras.insert(key.into(), value.into());
        self
    }
    /// Assemble the `params` JSON object (unset optionals omitted).
    #[must_use]
    pub fn build(self) -> Value {
        let mut obj = Map::new();
        obj.insert("url".to_string(), Value::from(self.url));
        if let Some(v) = self.control_id {
            obj.insert("control_id".to_string(), Value::from(v));
        }
        if let Some(v) = self.codec {
            obj.insert("codec".to_string(), Value::from(v));
        }
        if let Some(v) = self.track {
            obj.insert("track".to_string(), Value::from(v));
        }
        if let Some(v) = self.authorization_bearer_token {
            obj.insert("authorization_bearer_token".to_string(), Value::from(v));
        }
        if let Some(v) = self.custom_parameters {
            obj.insert("custom_parameters".to_string(), v);
        }
        for (k, v) in self.extras {
            obj.insert(k, v);
        }
        Value::Object(obj)
    }
}

/// Named request parameters for the generated method (Rust options-builder
/// idiom — required fields in `new`, optionals via setters, `extras` open door).
#[derive(Debug, Clone, Default)]
pub struct CallingStreamStopRequest {
    control_id: String,
    extras: Map<String, Value>,
}

impl CallingStreamStopRequest {
    /// Construct the request with its required fields.
    pub fn new(control_id: impl Into<String>) -> Self {
        CallingStreamStopRequest {
            control_id: control_id.into(),
            ..Default::default()
        }
    }
    /// Add a forward-compat field the spec does not yet name.
    #[must_use]
    pub fn extra(mut self, key: impl Into<String>, value: impl Into<Value>) -> Self {
        self.extras.insert(key.into(), value.into());
        self
    }
    /// Assemble the `params` JSON object (unset optionals omitted).
    #[must_use]
    pub fn build(self) -> Value {
        let mut obj = Map::new();
        obj.insert("control_id".to_string(), Value::from(self.control_id));
        for (k, v) in self.extras {
            obj.insert(k, v);
        }
        Value::Object(obj)
    }
}

/// Named request parameters for the generated method (Rust options-builder
/// idiom — required fields in `new`, optionals via setters, `extras` open door).
#[derive(Debug, Clone, Default)]
pub struct CallingTapRequest {
    tap: serde_json::Value,
    device: serde_json::Value,
    control_id: Option<String>,
    extras: Map<String, Value>,
}

impl CallingTapRequest {
    /// Construct the request with its required fields.
    pub fn new(tap: serde_json::Value, device: serde_json::Value) -> Self {
        CallingTapRequest {
            tap,
            device,
            ..Default::default()
        }
    }
    /// Set the optional `control_id` field.
    #[must_use]
    pub fn control_id(mut self, value: impl Into<String>) -> Self {
        self.control_id = Some(value.into());
        self
    }
    /// Add a forward-compat field the spec does not yet name.
    #[must_use]
    pub fn extra(mut self, key: impl Into<String>, value: impl Into<Value>) -> Self {
        self.extras.insert(key.into(), value.into());
        self
    }
    /// Assemble the `params` JSON object (unset optionals omitted).
    #[must_use]
    pub fn build(self) -> Value {
        let mut obj = Map::new();
        obj.insert("tap".to_string(), self.tap);
        obj.insert("device".to_string(), self.device);
        if let Some(v) = self.control_id {
            obj.insert("control_id".to_string(), Value::from(v));
        }
        for (k, v) in self.extras {
            obj.insert(k, v);
        }
        Value::Object(obj)
    }
}

/// Named request parameters for the generated method (Rust options-builder
/// idiom — required fields in `new`, optionals via setters, `extras` open door).
#[derive(Debug, Clone, Default)]
pub struct CallingTapStopRequest {
    control_id: String,
    extras: Map<String, Value>,
}

impl CallingTapStopRequest {
    /// Construct the request with its required fields.
    pub fn new(control_id: impl Into<String>) -> Self {
        CallingTapStopRequest {
            control_id: control_id.into(),
            ..Default::default()
        }
    }
    /// Add a forward-compat field the spec does not yet name.
    #[must_use]
    pub fn extra(mut self, key: impl Into<String>, value: impl Into<Value>) -> Self {
        self.extras.insert(key.into(), value.into());
        self
    }
    /// Assemble the `params` JSON object (unset optionals omitted).
    #[must_use]
    pub fn build(self) -> Value {
        let mut obj = Map::new();
        obj.insert("control_id".to_string(), Value::from(self.control_id));
        for (k, v) in self.extras {
            obj.insert(k, v);
        }
        Value::Object(obj)
    }
}

/// Named request parameters for the generated method (Rust options-builder
/// idiom — required fields in `new`, optionals via setters, `extras` open door).
#[derive(Debug, Clone, Default)]
pub struct CallingTranscribeRequest {
    control_id: Option<String>,
    status_url: Option<String>,
    extras: Map<String, Value>,
}

impl CallingTranscribeRequest {
    /// Construct the request with its required fields.
    pub fn new() -> Self {
        CallingTranscribeRequest {
            ..Default::default()
        }
    }
    /// Set the optional `control_id` field.
    #[must_use]
    pub fn control_id(mut self, value: impl Into<String>) -> Self {
        self.control_id = Some(value.into());
        self
    }
    /// Set the optional `status_url` field.
    #[must_use]
    pub fn status_url(mut self, value: impl Into<String>) -> Self {
        self.status_url = Some(value.into());
        self
    }
    /// Add a forward-compat field the spec does not yet name.
    #[must_use]
    pub fn extra(mut self, key: impl Into<String>, value: impl Into<Value>) -> Self {
        self.extras.insert(key.into(), value.into());
        self
    }
    /// Assemble the `params` JSON object (unset optionals omitted).
    #[must_use]
    pub fn build(self) -> Value {
        let mut obj = Map::new();
        if let Some(v) = self.control_id {
            obj.insert("control_id".to_string(), Value::from(v));
        }
        if let Some(v) = self.status_url {
            obj.insert("status_url".to_string(), Value::from(v));
        }
        for (k, v) in self.extras {
            obj.insert(k, v);
        }
        Value::Object(obj)
    }
}

/// Named request parameters for the generated method (Rust options-builder
/// idiom — required fields in `new`, optionals via setters, `extras` open door).
#[derive(Debug, Clone, Default)]
pub struct CallingTranscribeStopRequest {
    control_id: String,
    extras: Map<String, Value>,
}

impl CallingTranscribeStopRequest {
    /// Construct the request with its required fields.
    pub fn new(control_id: impl Into<String>) -> Self {
        CallingTranscribeStopRequest {
            control_id: control_id.into(),
            ..Default::default()
        }
    }
    /// Add a forward-compat field the spec does not yet name.
    #[must_use]
    pub fn extra(mut self, key: impl Into<String>, value: impl Into<Value>) -> Self {
        self.extras.insert(key.into(), value.into());
        self
    }
    /// Assemble the `params` JSON object (unset optionals omitted).
    #[must_use]
    pub fn build(self) -> Value {
        let mut obj = Map::new();
        obj.insert("control_id".to_string(), Value::from(self.control_id));
        for (k, v) in self.extras {
            obj.insert(k, v);
        }
        Value::Object(obj)
    }
}

/// Named request parameters for the generated method (Rust options-builder
/// idiom — required fields in `new`, optionals via setters, `extras` open door).
#[derive(Debug, Clone, Default)]
pub struct CallingTransferRequest {
    dest: serde_json::Value,
    extras: Map<String, Value>,
}

impl CallingTransferRequest {
    /// Construct the request with its required fields.
    pub fn new(dest: serde_json::Value) -> Self {
        CallingTransferRequest {
            dest,
            ..Default::default()
        }
    }
    /// Add a forward-compat field the spec does not yet name.
    #[must_use]
    pub fn extra(mut self, key: impl Into<String>, value: impl Into<Value>) -> Self {
        self.extras.insert(key.into(), value.into());
        self
    }
    /// Assemble the `params` JSON object (unset optionals omitted).
    #[must_use]
    pub fn build(self) -> Value {
        let mut obj = Map::new();
        obj.insert("dest".to_string(), self.dest);
        for (k, v) in self.extras {
            obj.insert(k, v);
        }
        Value::Object(obj)
    }
}

/// Named request parameters for the generated method (Rust options-builder
/// idiom — required fields in `new`, optionals via setters, `extras` open door).
#[derive(Debug, Clone, Default)]
pub struct CallingUpdateRequest {
    id: String,
    fallback_url: Option<String>,
    status: Option<String>,
    status_url: Option<String>,
    url: Option<String>,
    swml: Option<serde_json::Value>,
    extras: Map<String, Value>,
}

impl CallingUpdateRequest {
    /// Construct the request with its required fields.
    pub fn new(id: impl Into<String>) -> Self {
        CallingUpdateRequest {
            id: id.into(),
            ..Default::default()
        }
    }
    /// Set the optional `fallback_url` field.
    #[must_use]
    pub fn fallback_url(mut self, value: impl Into<String>) -> Self {
        self.fallback_url = Some(value.into());
        self
    }
    /// Set the optional `status` field.
    #[must_use]
    pub fn status(mut self, value: impl Into<String>) -> Self {
        self.status = Some(value.into());
        self
    }
    /// Set the optional `status_url` field.
    #[must_use]
    pub fn status_url(mut self, value: impl Into<String>) -> Self {
        self.status_url = Some(value.into());
        self
    }
    /// Set the optional `url` field.
    #[must_use]
    pub fn url(mut self, value: impl Into<String>) -> Self {
        self.url = Some(value.into());
        self
    }
    /// Set the optional `swml` field.
    #[must_use]
    pub fn swml(mut self, value: serde_json::Value) -> Self {
        self.swml = Some(value);
        self
    }
    /// Add a forward-compat field the spec does not yet name.
    #[must_use]
    pub fn extra(mut self, key: impl Into<String>, value: impl Into<Value>) -> Self {
        self.extras.insert(key.into(), value.into());
        self
    }
    /// Assemble the `params` JSON object (unset optionals omitted).
    #[must_use]
    pub fn build(self) -> Value {
        let mut obj = Map::new();
        obj.insert("id".to_string(), Value::from(self.id));
        if let Some(v) = self.fallback_url {
            obj.insert("fallback_url".to_string(), Value::from(v));
        }
        if let Some(v) = self.status {
            obj.insert("status".to_string(), Value::from(v));
        }
        if let Some(v) = self.status_url {
            obj.insert("status_url".to_string(), Value::from(v));
        }
        if let Some(v) = self.url {
            obj.insert("url".to_string(), Value::from(v));
        }
        if let Some(v) = self.swml {
            obj.insert("swml".to_string(), v);
        }
        for (k, v) in self.extras {
            obj.insert(k, v);
        }
        Value::Object(obj)
    }
}

/// Named request parameters for the generated method (Rust options-builder
/// idiom — required fields in `new`, optionals via setters, `extras` open door).
#[derive(Debug, Clone, Default)]
pub struct CallingUserEventRequest {
    event: serde_json::Value,
    extras: Map<String, Value>,
}

impl CallingUserEventRequest {
    /// Construct the request with its required fields.
    pub fn new(event: serde_json::Value) -> Self {
        CallingUserEventRequest {
            event,
            ..Default::default()
        }
    }
    /// Add a forward-compat field the spec does not yet name.
    #[must_use]
    pub fn extra(mut self, key: impl Into<String>, value: impl Into<Value>) -> Self {
        self.extras.insert(key.into(), value.into());
        self
    }
    /// Assemble the `params` JSON object (unset optionals omitted).
    #[must_use]
    pub fn build(self) -> Value {
        let mut obj = Map::new();
        obj.insert("event".to_string(), self.event);
        for (k, v) in self.extras {
            obj.insert(k, v);
        }
        Value::Object(obj)
    }
}

/// `Calling` — command-dispatch resource (calling spec). Each method POSTs
/// `{command, params, id?}` to `/api/calling/calls`.
pub struct Calling<'a> {
    client: &'a HttpClient,
}

impl<'a> Calling<'a> {
    const BASE_PATH: &'static str = "/api/calling/calls";

    #[must_use]
    pub fn new(client: &'a HttpClient) -> Self {
        Calling { client }
    }

    #[must_use]
    pub fn base_path(&self) -> &str {
        Self::BASE_PATH
    }

    fn execute(
        &self,
        command: &str,
        call_id: Option<&str>,
        params: Value,
    ) -> Result<Value, SignalWireRestError> {
        let mut body = Map::new();
        body.insert("command".to_string(), Value::from(command));
        body.insert("params".to_string(), params);
        if let Some(id) = call_id {
            body.insert("id".to_string(), Value::from(id));
        }
        self.client.post(Self::BASE_PATH, &Value::Object(body))
    }

    /// `dial` — generated command method.
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure or a non-2xx status.
    pub fn dial(&self, request: CallingDialRequest) -> Result<Value, SignalWireRestError> {
        self.execute("dial", None, request.build())
    }

    /// `update` — generated command method.
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure or a non-2xx status.
    pub fn update(&self, request: CallingUpdateRequest) -> Result<Value, SignalWireRestError> {
        self.execute("update", None, request.build())
    }

    /// `calling.end` — generated command method.
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure or a non-2xx status.
    pub fn end(
        &self,
        call_id: &str,
        request: CallingEndRequest,
    ) -> Result<Value, SignalWireRestError> {
        self.execute("calling.end", Some(call_id), request.build())
    }

    /// `calling.ai_hold` — generated command method.
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure or a non-2xx status.
    pub fn ai_hold(
        &self,
        call_id: &str,
        request: CallingAiHoldRequest,
    ) -> Result<Value, SignalWireRestError> {
        self.execute("calling.ai_hold", Some(call_id), request.build())
    }

    /// `calling.ai_unhold` — generated command method.
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure or a non-2xx status.
    pub fn ai_unhold(
        &self,
        call_id: &str,
        request: CallingAiUnholdRequest,
    ) -> Result<Value, SignalWireRestError> {
        self.execute("calling.ai_unhold", Some(call_id), request.build())
    }

    /// `calling.ai_message` — generated command method.
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure or a non-2xx status.
    pub fn ai_message(
        &self,
        call_id: &str,
        request: CallingAiMessageRequest,
    ) -> Result<Value, SignalWireRestError> {
        self.execute("calling.ai_message", Some(call_id), request.build())
    }

    /// `calling.live_transcribe` — generated command method.
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure or a non-2xx status.
    pub fn live_transcribe(
        &self,
        call_id: &str,
        request: CallingLiveTranscribeRequest,
    ) -> Result<Value, SignalWireRestError> {
        self.execute("calling.live_transcribe", Some(call_id), request.build())
    }

    /// `calling.live_translate` — generated command method.
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure or a non-2xx status.
    pub fn live_translate(
        &self,
        call_id: &str,
        request: CallingLiveTranslateRequest,
    ) -> Result<Value, SignalWireRestError> {
        self.execute("calling.live_translate", Some(call_id), request.build())
    }

    /// `calling.transfer` — generated command method.
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure or a non-2xx status.
    pub fn transfer(
        &self,
        call_id: &str,
        request: CallingTransferRequest,
    ) -> Result<Value, SignalWireRestError> {
        self.execute("calling.transfer", Some(call_id), request.build())
    }

    /// `calling.user_event` — generated command method.
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure or a non-2xx status.
    pub fn user_event(
        &self,
        call_id: &str,
        request: CallingUserEventRequest,
    ) -> Result<Value, SignalWireRestError> {
        self.execute("calling.user_event", Some(call_id), request.build())
    }

    /// `calling.disconnect` — generated command method.
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure or a non-2xx status.
    pub fn disconnect(
        &self,
        call_id: &str,
        request: CallingDisconnectRequest,
    ) -> Result<Value, SignalWireRestError> {
        self.execute("calling.disconnect", Some(call_id), request.build())
    }

    /// `calling.play` — generated command method.
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure or a non-2xx status.
    pub fn play(
        &self,
        call_id: &str,
        request: CallingPlayRequest,
    ) -> Result<Value, SignalWireRestError> {
        self.execute("calling.play", Some(call_id), request.build())
    }

    /// `calling.play.pause` — generated command method.
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure or a non-2xx status.
    pub fn play_pause(
        &self,
        call_id: &str,
        request: CallingPlayPauseRequest,
    ) -> Result<Value, SignalWireRestError> {
        self.execute("calling.play.pause", Some(call_id), request.build())
    }

    /// `calling.play.resume` — generated command method.
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure or a non-2xx status.
    pub fn play_resume(
        &self,
        call_id: &str,
        request: CallingPlayResumeRequest,
    ) -> Result<Value, SignalWireRestError> {
        self.execute("calling.play.resume", Some(call_id), request.build())
    }

    /// `calling.play.stop` — generated command method.
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure or a non-2xx status.
    pub fn play_stop(
        &self,
        call_id: &str,
        request: CallingPlayStopRequest,
    ) -> Result<Value, SignalWireRestError> {
        self.execute("calling.play.stop", Some(call_id), request.build())
    }

    /// `calling.play.volume` — generated command method.
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure or a non-2xx status.
    pub fn play_volume(
        &self,
        call_id: &str,
        request: CallingPlayVolumeRequest,
    ) -> Result<Value, SignalWireRestError> {
        self.execute("calling.play.volume", Some(call_id), request.build())
    }

    /// `calling.record` — generated command method.
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure or a non-2xx status.
    pub fn record(
        &self,
        call_id: &str,
        request: CallingRecordRequest,
    ) -> Result<Value, SignalWireRestError> {
        self.execute("calling.record", Some(call_id), request.build())
    }

    /// `calling.record.pause` — generated command method.
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure or a non-2xx status.
    pub fn record_pause(
        &self,
        call_id: &str,
        request: CallingRecordPauseRequest,
    ) -> Result<Value, SignalWireRestError> {
        self.execute("calling.record.pause", Some(call_id), request.build())
    }

    /// `calling.record.resume` — generated command method.
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure or a non-2xx status.
    pub fn record_resume(
        &self,
        call_id: &str,
        request: CallingRecordResumeRequest,
    ) -> Result<Value, SignalWireRestError> {
        self.execute("calling.record.resume", Some(call_id), request.build())
    }

    /// `calling.record.stop` — generated command method.
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure or a non-2xx status.
    pub fn record_stop(
        &self,
        call_id: &str,
        request: CallingRecordStopRequest,
    ) -> Result<Value, SignalWireRestError> {
        self.execute("calling.record.stop", Some(call_id), request.build())
    }

    /// `calling.collect` — generated command method.
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure or a non-2xx status.
    pub fn collect(
        &self,
        call_id: &str,
        request: CallingCollectRequest,
    ) -> Result<Value, SignalWireRestError> {
        self.execute("calling.collect", Some(call_id), request.build())
    }

    /// `calling.collect.stop` — generated command method.
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure or a non-2xx status.
    pub fn collect_stop(
        &self,
        call_id: &str,
        request: CallingCollectStopRequest,
    ) -> Result<Value, SignalWireRestError> {
        self.execute("calling.collect.stop", Some(call_id), request.build())
    }

    /// `calling.collect.start_input_timers` — generated command method.
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure or a non-2xx status.
    pub fn collect_start_input_timers(
        &self,
        call_id: &str,
        request: CallingCollectStartInputTimersRequest,
    ) -> Result<Value, SignalWireRestError> {
        self.execute(
            "calling.collect.start_input_timers",
            Some(call_id),
            request.build(),
        )
    }

    /// `calling.detect` — generated command method.
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure or a non-2xx status.
    pub fn detect(
        &self,
        call_id: &str,
        request: CallingDetectRequest,
    ) -> Result<Value, SignalWireRestError> {
        self.execute("calling.detect", Some(call_id), request.build())
    }

    /// `calling.detect.stop` — generated command method.
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure or a non-2xx status.
    pub fn detect_stop(
        &self,
        call_id: &str,
        request: CallingDetectStopRequest,
    ) -> Result<Value, SignalWireRestError> {
        self.execute("calling.detect.stop", Some(call_id), request.build())
    }

    /// `calling.tap` — generated command method.
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure or a non-2xx status.
    pub fn tap(
        &self,
        call_id: &str,
        request: CallingTapRequest,
    ) -> Result<Value, SignalWireRestError> {
        self.execute("calling.tap", Some(call_id), request.build())
    }

    /// `calling.tap.stop` — generated command method.
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure or a non-2xx status.
    pub fn tap_stop(
        &self,
        call_id: &str,
        request: CallingTapStopRequest,
    ) -> Result<Value, SignalWireRestError> {
        self.execute("calling.tap.stop", Some(call_id), request.build())
    }

    /// `calling.stream` — generated command method.
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure or a non-2xx status.
    pub fn stream(
        &self,
        call_id: &str,
        request: CallingStreamRequest,
    ) -> Result<Value, SignalWireRestError> {
        self.execute("calling.stream", Some(call_id), request.build())
    }

    /// `calling.stream.stop` — generated command method.
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure or a non-2xx status.
    pub fn stream_stop(
        &self,
        call_id: &str,
        request: CallingStreamStopRequest,
    ) -> Result<Value, SignalWireRestError> {
        self.execute("calling.stream.stop", Some(call_id), request.build())
    }

    /// `calling.denoise` — generated command method.
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure or a non-2xx status.
    pub fn denoise(
        &self,
        call_id: &str,
        request: CallingDenoiseRequest,
    ) -> Result<Value, SignalWireRestError> {
        self.execute("calling.denoise", Some(call_id), request.build())
    }

    /// `calling.denoise.stop` — generated command method.
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure or a non-2xx status.
    pub fn denoise_stop(
        &self,
        call_id: &str,
        request: CallingDenoiseStopRequest,
    ) -> Result<Value, SignalWireRestError> {
        self.execute("calling.denoise.stop", Some(call_id), request.build())
    }

    /// `calling.transcribe` — generated command method.
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure or a non-2xx status.
    pub fn transcribe(
        &self,
        call_id: &str,
        request: CallingTranscribeRequest,
    ) -> Result<Value, SignalWireRestError> {
        self.execute("calling.transcribe", Some(call_id), request.build())
    }

    /// `calling.transcribe.stop` — generated command method.
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure or a non-2xx status.
    pub fn transcribe_stop(
        &self,
        call_id: &str,
        request: CallingTranscribeStopRequest,
    ) -> Result<Value, SignalWireRestError> {
        self.execute("calling.transcribe.stop", Some(call_id), request.build())
    }

    /// `calling.ai.stop` — generated command method.
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure or a non-2xx status.
    pub fn ai_stop(
        &self,
        call_id: &str,
        request: CallingAiStopRequest,
    ) -> Result<Value, SignalWireRestError> {
        self.execute("calling.ai.stop", Some(call_id), request.build())
    }

    /// `calling.send_fax.stop` — generated command method.
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure or a non-2xx status.
    pub fn send_fax_stop(
        &self,
        call_id: &str,
        request: CallingSendFaxStopRequest,
    ) -> Result<Value, SignalWireRestError> {
        self.execute("calling.send_fax.stop", Some(call_id), request.build())
    }

    /// `calling.receive_fax.stop` — generated command method.
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure or a non-2xx status.
    pub fn receive_fax_stop(
        &self,
        call_id: &str,
        request: CallingReceiveFaxStopRequest,
    ) -> Result<Value, SignalWireRestError> {
        self.execute("calling.receive_fax.stop", Some(call_id), request.build())
    }

    /// `calling.refer` — generated command method.
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure or a non-2xx status.
    pub fn refer(
        &self,
        call_id: &str,
        request: CallingReferRequest,
    ) -> Result<Value, SignalWireRestError> {
        self.execute("calling.refer", Some(call_id), request.build())
    }
}
