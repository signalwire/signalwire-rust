// Code generated by scripts/generate_rest.py; DO NOT EDIT.
//
// AUTO-GENERATED from porting-sdk/rest-apis/ (x-sdk-* markup) — regenerate with:
//   python3 scripts/generate_rest.py
//
// Generated REST resources for the 'project' namespace.

// Generated code is not hand-tuned for every pedantic clippy lint; the LINT
// gate governs SOURCE STYLE (parity-neutral), so the generated layer allows the
// pedantic lints its emission shape naturally trips. The hand base resources
// (generated_bases.rs) and the rest of the crate stay under the strict gate.
// ``dead_code``: a resource's private client()/path() helpers are used only by
// its DECLARED operation methods; a pure-CRUD resource (all methods delegated to
// the base) doesn't call them, so they read as dead in that file.
#![allow(
    clippy::too_many_arguments,
    clippy::needless_pass_by_value,
    clippy::redundant_field_names,
    clippy::useless_asref,
    clippy::unnecessary_to_owned,
    clippy::uninlined_format_args,
    clippy::doc_markdown,
    dead_code,
    unused_imports
)]

use std::collections::HashMap;

use serde_json::{Map, Value};

use crate::rest::error::SignalWireRestError;
use crate::rest::generated_bases::{BaseResource, CrudResource, FabricResource, ReadResource};
use crate::rest::http_client::HttpClient;

/// Named request parameters for the generated method (Rust options-builder
/// idiom — required fields in `new`, optionals via setters, `extras` open door).
#[derive(Debug, Clone, Default)]
pub struct ProjectTokensCreateRequest {
    name: String,
    permissions: serde_json::Value,
    subproject_id: Option<String>,
    extras: Map<String, Value>,
}

impl ProjectTokensCreateRequest {
    /// Construct the request with its required fields.
    pub fn new(name: impl Into<String>, permissions: serde_json::Value) -> Self {
        ProjectTokensCreateRequest {
            name: name.into(),
            permissions: permissions,
            ..Default::default()
        }
    }
    /// Set the optional `subproject_id` field.
    #[must_use]
    pub fn subproject_id(mut self, value: impl Into<String>) -> Self {
        self.subproject_id = Some(value.into());
        self
    }
    /// Add a forward-compat field the spec does not yet name.
    #[must_use]
    pub fn extra(mut self, key: impl Into<String>, value: impl Into<Value>) -> Self {
        self.extras.insert(key.into(), value.into());
        self
    }
    /// Assemble the `body` JSON object (unset optionals omitted).
    #[must_use]
    pub fn build(self) -> Value {
        let mut obj = Map::new();
        obj.insert("name".to_string(), Value::from(self.name));
        obj.insert("permissions".to_string(), self.permissions);
        if let Some(v) = self.subproject_id {
            obj.insert("subproject_id".to_string(), Value::from(v));
        }
        for (k, v) in self.extras {
            obj.insert(k, v);
        }
        Value::Object(obj)
    }
}

/// Named request parameters for the generated method (Rust options-builder
/// idiom — required fields in `new`, optionals via setters, `extras` open door).
#[derive(Debug, Clone, Default)]
pub struct ProjectTokensUpdateRequest {
    name: Option<String>,
    permissions: Option<serde_json::Value>,
    extras: Map<String, Value>,
}

impl ProjectTokensUpdateRequest {
    /// Construct the request with its required fields.
    pub fn new() -> Self {
        ProjectTokensUpdateRequest {
            ..Default::default()
        }
    }
    /// Set the optional `name` field.
    #[must_use]
    pub fn name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }
    /// Set the optional `permissions` field.
    #[must_use]
    pub fn permissions(mut self, value: serde_json::Value) -> Self {
        self.permissions = Some(value);
        self
    }
    /// Add a forward-compat field the spec does not yet name.
    #[must_use]
    pub fn extra(mut self, key: impl Into<String>, value: impl Into<Value>) -> Self {
        self.extras.insert(key.into(), value.into());
        self
    }
    /// Assemble the `body` JSON object (unset optionals omitted).
    #[must_use]
    pub fn build(self) -> Value {
        let mut obj = Map::new();
        if let Some(v) = self.name {
            obj.insert("name".to_string(), Value::from(v));
        }
        if let Some(v) = self.permissions {
            obj.insert("permissions".to_string(), v);
        }
        for (k, v) in self.extras {
            obj.insert(k, v);
        }
        Value::Object(obj)
    }
}

/// `ProjectTokens` — generated from x-sdk-resource 'ProjectTokens' (project spec, base BaseResource).
///
/// Composes [`BaseResource`] (its base-path is baked in per §4) and adds the
/// resource's declared/set methods.
pub struct ProjectTokens<'a> {
    base: BaseResource<'a>,
}

impl<'a> ProjectTokens<'a> {
    /// Construct the resource; its base path (§4) is baked in.
    #[must_use]
    pub fn new(client: &'a HttpClient) -> Self {
        ProjectTokens {
            base: BaseResource::new(client, "/api/project/tokens"),
        }
    }

    #[must_use]
    pub fn base_path(&self) -> &str {
        self.base.base_path()
    }

    fn client(&self) -> &HttpClient {
        self.base.client()
    }

    fn path(&self, parts: &[&str]) -> String {
        self.base.path(parts)
    }

    /// `POST /tokens` (generated operation method).
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure, a non-2xx
    /// status, or an unparseable response body.
    pub fn create(
        &self,
        request: ProjectTokensCreateRequest,
    ) -> Result<Value, SignalWireRestError> {
        self.client()
            .post(&self.base_path().to_string(), &request.build())
    }

    /// `PATCH /tokens/{token_id}` (generated operation method).
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure, a non-2xx
    /// status, or an unparseable response body.
    pub fn update(
        &self,
        token_id: &str,
        request: ProjectTokensUpdateRequest,
    ) -> Result<Value, SignalWireRestError> {
        self.client()
            .patch(&self.path(&[token_id.as_ref()]), &request.build())
    }

    /// `DELETE /tokens/{token_id}` (generated operation method).
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure, a non-2xx status.
    pub fn delete(&self, token_id: &str) -> Result<Value, SignalWireRestError> {
        self.client().delete(&self.path(&[token_id.as_ref()]))
    }
}
