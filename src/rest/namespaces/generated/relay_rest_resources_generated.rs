// Code generated by scripts/generate_rest.py; DO NOT EDIT.
//
// Regenerate with:
//   python3 scripts/generate_rest.py
//
// Generated REST resources for the 'relay-rest' namespace.

use std::collections::HashMap;

use serde_json::{Map, Value};

use crate::rest::error::SignalWireRestError;
use crate::rest::generated_bases::{BaseResource, CrudResource};
use crate::rest::http_client::HttpClient;

/// Named request parameters for the generated method (Rust options-builder
/// idiom — required fields in `new`, optionals via setters, `extras` open door).
#[derive(Debug, Clone, Default)]
pub struct AddressesCreateRequest {
    label: String,
    country: String,
    first_name: String,
    last_name: String,
    street_number: String,
    street_name: String,
    city: String,
    state: String,
    postal_code: String,
    address_type: Option<String>,
    address_number: Option<String>,
    extras: Map<String, Value>,
}

impl AddressesCreateRequest {
    /// Construct the request with its required fields.
    #[allow(clippy::too_many_arguments)]
    pub fn new(
        label: impl Into<String>,
        country: impl Into<String>,
        first_name: impl Into<String>,
        last_name: impl Into<String>,
        street_number: impl Into<String>,
        street_name: impl Into<String>,
        city: impl Into<String>,
        state: impl Into<String>,
        postal_code: impl Into<String>,
    ) -> Self {
        AddressesCreateRequest {
            label: label.into(),
            country: country.into(),
            first_name: first_name.into(),
            last_name: last_name.into(),
            street_number: street_number.into(),
            street_name: street_name.into(),
            city: city.into(),
            state: state.into(),
            postal_code: postal_code.into(),
            ..Default::default()
        }
    }
    /// Set the optional `address_type` field.
    #[must_use]
    pub fn address_type(mut self, value: impl Into<String>) -> Self {
        self.address_type = Some(value.into());
        self
    }
    /// Set the optional `address_number` field.
    #[must_use]
    pub fn address_number(mut self, value: impl Into<String>) -> Self {
        self.address_number = Some(value.into());
        self
    }
    /// Add a forward-compat field the spec does not yet name.
    #[must_use]
    pub fn extra(mut self, key: impl Into<String>, value: impl Into<Value>) -> Self {
        self.extras.insert(key.into(), value.into());
        self
    }
    /// Assemble the `body` JSON object (unset optionals omitted).
    #[must_use]
    pub fn build(self) -> Value {
        let mut obj = Map::new();
        obj.insert("label".to_string(), Value::from(self.label));
        obj.insert("country".to_string(), Value::from(self.country));
        obj.insert("first_name".to_string(), Value::from(self.first_name));
        obj.insert("last_name".to_string(), Value::from(self.last_name));
        obj.insert("street_number".to_string(), Value::from(self.street_number));
        obj.insert("street_name".to_string(), Value::from(self.street_name));
        obj.insert("city".to_string(), Value::from(self.city));
        obj.insert("state".to_string(), Value::from(self.state));
        obj.insert("postal_code".to_string(), Value::from(self.postal_code));
        if let Some(v) = self.address_type {
            obj.insert("address_type".to_string(), Value::from(v));
        }
        if let Some(v) = self.address_number {
            obj.insert("address_number".to_string(), Value::from(v));
        }
        for (k, v) in self.extras {
            obj.insert(k, v);
        }
        Value::Object(obj)
    }
}

/// Named request parameters for the generated method (Rust options-builder
/// idiom — required fields in `new`, optionals via setters, `extras` open door).
#[derive(Debug, Clone, Default)]
pub struct ImportedNumbersCreateRequest {
    number: String,
    number_type: String,
    capabilities: Option<serde_json::Value>,
    extras: Map<String, Value>,
}

impl ImportedNumbersCreateRequest {
    /// Construct the request with its required fields.
    pub fn new(number: impl Into<String>, number_type: impl Into<String>) -> Self {
        ImportedNumbersCreateRequest {
            number: number.into(),
            number_type: number_type.into(),
            ..Default::default()
        }
    }
    /// Set the optional `capabilities` field.
    #[must_use]
    pub fn capabilities(mut self, value: serde_json::Value) -> Self {
        self.capabilities = Some(value);
        self
    }
    /// Add a forward-compat field the spec does not yet name.
    #[must_use]
    pub fn extra(mut self, key: impl Into<String>, value: impl Into<Value>) -> Self {
        self.extras.insert(key.into(), value.into());
        self
    }
    /// Assemble the `body` JSON object (unset optionals omitted).
    #[must_use]
    pub fn build(self) -> Value {
        let mut obj = Map::new();
        obj.insert("number".to_string(), Value::from(self.number));
        obj.insert("number_type".to_string(), Value::from(self.number_type));
        if let Some(v) = self.capabilities {
            obj.insert("capabilities".to_string(), v);
        }
        for (k, v) in self.extras {
            obj.insert(k, v);
        }
        Value::Object(obj)
    }
}

/// Named request parameters for the generated method (Rust options-builder
/// idiom — required fields in `new`, optionals via setters, `extras` open door).
#[derive(Debug, Clone, Default)]
pub struct MfaCallRequest {
    to: String,
    from: Option<String>,
    message: Option<String>,
    token_length: Option<i64>,
    valid_for: Option<i64>,
    max_attempts: Option<i64>,
    allow_alphas: Option<bool>,
    extras: Map<String, Value>,
}

impl MfaCallRequest {
    /// Construct the request with its required fields.
    pub fn new(to: impl Into<String>) -> Self {
        MfaCallRequest {
            to: to.into(),
            ..Default::default()
        }
    }
    /// Set the optional `from` field.
    #[must_use]
    pub fn from(mut self, value: impl Into<String>) -> Self {
        self.from = Some(value.into());
        self
    }
    /// Set the optional `message` field.
    #[must_use]
    pub fn message(mut self, value: impl Into<String>) -> Self {
        self.message = Some(value.into());
        self
    }
    /// Set the optional `token_length` field.
    #[must_use]
    pub fn token_length(mut self, value: i64) -> Self {
        self.token_length = Some(value);
        self
    }
    /// Set the optional `valid_for` field.
    #[must_use]
    pub fn valid_for(mut self, value: i64) -> Self {
        self.valid_for = Some(value);
        self
    }
    /// Set the optional `max_attempts` field.
    #[must_use]
    pub fn max_attempts(mut self, value: i64) -> Self {
        self.max_attempts = Some(value);
        self
    }
    /// Set the optional `allow_alphas` field.
    #[must_use]
    pub fn allow_alphas(mut self, value: bool) -> Self {
        self.allow_alphas = Some(value);
        self
    }
    /// Add a forward-compat field the spec does not yet name.
    #[must_use]
    pub fn extra(mut self, key: impl Into<String>, value: impl Into<Value>) -> Self {
        self.extras.insert(key.into(), value.into());
        self
    }
    /// Assemble the `body` JSON object (unset optionals omitted).
    #[must_use]
    pub fn build(self) -> Value {
        let mut obj = Map::new();
        obj.insert("to".to_string(), Value::from(self.to));
        if let Some(v) = self.from {
            obj.insert("from".to_string(), Value::from(v));
        }
        if let Some(v) = self.message {
            obj.insert("message".to_string(), Value::from(v));
        }
        if let Some(v) = self.token_length {
            obj.insert("token_length".to_string(), Value::from(v));
        }
        if let Some(v) = self.valid_for {
            obj.insert("valid_for".to_string(), Value::from(v));
        }
        if let Some(v) = self.max_attempts {
            obj.insert("max_attempts".to_string(), Value::from(v));
        }
        if let Some(v) = self.allow_alphas {
            obj.insert("allow_alphas".to_string(), Value::from(v));
        }
        for (k, v) in self.extras {
            obj.insert(k, v);
        }
        Value::Object(obj)
    }
}

/// Named request parameters for the generated method (Rust options-builder
/// idiom — required fields in `new`, optionals via setters, `extras` open door).
#[derive(Debug, Clone, Default)]
pub struct MfaSmsRequest {
    to: String,
    from: Option<String>,
    message: Option<String>,
    token_length: Option<i64>,
    valid_for: Option<i64>,
    max_attempts: Option<i64>,
    allow_alphas: Option<bool>,
    extras: Map<String, Value>,
}

impl MfaSmsRequest {
    /// Construct the request with its required fields.
    pub fn new(to: impl Into<String>) -> Self {
        MfaSmsRequest {
            to: to.into(),
            ..Default::default()
        }
    }
    /// Set the optional `from` field.
    #[must_use]
    pub fn from(mut self, value: impl Into<String>) -> Self {
        self.from = Some(value.into());
        self
    }
    /// Set the optional `message` field.
    #[must_use]
    pub fn message(mut self, value: impl Into<String>) -> Self {
        self.message = Some(value.into());
        self
    }
    /// Set the optional `token_length` field.
    #[must_use]
    pub fn token_length(mut self, value: i64) -> Self {
        self.token_length = Some(value);
        self
    }
    /// Set the optional `valid_for` field.
    #[must_use]
    pub fn valid_for(mut self, value: i64) -> Self {
        self.valid_for = Some(value);
        self
    }
    /// Set the optional `max_attempts` field.
    #[must_use]
    pub fn max_attempts(mut self, value: i64) -> Self {
        self.max_attempts = Some(value);
        self
    }
    /// Set the optional `allow_alphas` field.
    #[must_use]
    pub fn allow_alphas(mut self, value: bool) -> Self {
        self.allow_alphas = Some(value);
        self
    }
    /// Add a forward-compat field the spec does not yet name.
    #[must_use]
    pub fn extra(mut self, key: impl Into<String>, value: impl Into<Value>) -> Self {
        self.extras.insert(key.into(), value.into());
        self
    }
    /// Assemble the `body` JSON object (unset optionals omitted).
    #[must_use]
    pub fn build(self) -> Value {
        let mut obj = Map::new();
        obj.insert("to".to_string(), Value::from(self.to));
        if let Some(v) = self.from {
            obj.insert("from".to_string(), Value::from(v));
        }
        if let Some(v) = self.message {
            obj.insert("message".to_string(), Value::from(v));
        }
        if let Some(v) = self.token_length {
            obj.insert("token_length".to_string(), Value::from(v));
        }
        if let Some(v) = self.valid_for {
            obj.insert("valid_for".to_string(), Value::from(v));
        }
        if let Some(v) = self.max_attempts {
            obj.insert("max_attempts".to_string(), Value::from(v));
        }
        if let Some(v) = self.allow_alphas {
            obj.insert("allow_alphas".to_string(), Value::from(v));
        }
        for (k, v) in self.extras {
            obj.insert(k, v);
        }
        Value::Object(obj)
    }
}

/// Named request parameters for the generated method (Rust options-builder
/// idiom — required fields in `new`, optionals via setters, `extras` open door).
#[derive(Debug, Clone, Default)]
pub struct MfaVerifyRequest {
    token: String,
    extras: Map<String, Value>,
}

impl MfaVerifyRequest {
    /// Construct the request with its required fields.
    pub fn new(token: impl Into<String>) -> Self {
        MfaVerifyRequest {
            token: token.into(),
            ..Default::default()
        }
    }
    /// Add a forward-compat field the spec does not yet name.
    #[must_use]
    pub fn extra(mut self, key: impl Into<String>, value: impl Into<Value>) -> Self {
        self.extras.insert(key.into(), value.into());
        self
    }
    /// Assemble the `body` JSON object (unset optionals omitted).
    #[must_use]
    pub fn build(self) -> Value {
        let mut obj = Map::new();
        obj.insert("token".to_string(), Value::from(self.token));
        for (k, v) in self.extras {
            obj.insert(k, v);
        }
        Value::Object(obj)
    }
}

/// Named request parameters for the generated method (Rust options-builder
/// idiom — required fields in `new`, optionals via setters, `extras` open door).
#[derive(Debug, Clone, Default)]
pub struct NumberGroupsAddMembershipRequest {
    phone_number_id: String,
    extras: Map<String, Value>,
}

impl NumberGroupsAddMembershipRequest {
    /// Construct the request with its required fields.
    pub fn new(phone_number_id: impl Into<String>) -> Self {
        NumberGroupsAddMembershipRequest {
            phone_number_id: phone_number_id.into(),
            ..Default::default()
        }
    }
    /// Add a forward-compat field the spec does not yet name.
    #[must_use]
    pub fn extra(mut self, key: impl Into<String>, value: impl Into<Value>) -> Self {
        self.extras.insert(key.into(), value.into());
        self
    }
    /// Assemble the `body` JSON object (unset optionals omitted).
    #[must_use]
    pub fn build(self) -> Value {
        let mut obj = Map::new();
        obj.insert(
            "phone_number_id".to_string(),
            Value::from(self.phone_number_id),
        );
        for (k, v) in self.extras {
            obj.insert(k, v);
        }
        Value::Object(obj)
    }
}

/// Named request parameters for a generated `set_*` wrapper (binds args to
/// update-request fields + a fixed `call_handler`; Rust options-builder idiom).
#[derive(Debug, Clone, Default)]
pub struct PhoneNumbersSetAiAgentRequest {
    agent_id: String,
    extras: Map<String, Value>,
}

impl PhoneNumbersSetAiAgentRequest {
    pub fn new(agent_id: impl Into<String>) -> Self {
        PhoneNumbersSetAiAgentRequest {
            agent_id: agent_id.into(),
            ..Default::default()
        }
    }
    #[must_use]
    pub fn extra(mut self, key: impl Into<String>, value: impl Into<Value>) -> Self {
        self.extras.insert(key.into(), value.into());
        self
    }
    #[must_use]
    pub fn build(self) -> Value {
        let mut obj = Map::new();
        obj.insert("call_handler".to_string(), Value::from("ai_agent"));
        obj.insert("call_ai_agent_id".to_string(), Value::from(self.agent_id));
        for (k, v) in self.extras {
            obj.insert(k, v);
        }
        Value::Object(obj)
    }
}

/// Named request parameters for a generated `set_*` wrapper (binds args to
/// update-request fields + a fixed `call_handler`; Rust options-builder idiom).
#[derive(Debug, Clone, Default)]
pub struct PhoneNumbersSetCallFlowRequest {
    flow_id: String,
    version: Option<String>,
    extras: Map<String, Value>,
}

impl PhoneNumbersSetCallFlowRequest {
    pub fn new(flow_id: impl Into<String>) -> Self {
        PhoneNumbersSetCallFlowRequest {
            flow_id: flow_id.into(),
            ..Default::default()
        }
    }
    #[must_use]
    pub fn version(mut self, value: impl Into<String>) -> Self {
        self.version = Some(value.into());
        self
    }
    #[must_use]
    pub fn extra(mut self, key: impl Into<String>, value: impl Into<Value>) -> Self {
        self.extras.insert(key.into(), value.into());
        self
    }
    #[must_use]
    pub fn build(self) -> Value {
        let mut obj = Map::new();
        obj.insert("call_handler".to_string(), Value::from("call_flow"));
        obj.insert("call_flow_id".to_string(), Value::from(self.flow_id));
        if let Some(v) = self.version {
            obj.insert("call_flow_version".to_string(), Value::from(v));
        }
        for (k, v) in self.extras {
            obj.insert(k, v);
        }
        Value::Object(obj)
    }
}

/// Named request parameters for a generated `set_*` wrapper (binds args to
/// update-request fields + a fixed `call_handler`; Rust options-builder idiom).
#[derive(Debug, Clone, Default)]
pub struct PhoneNumbersSetCxmlApplicationRequest {
    application_id: String,
    extras: Map<String, Value>,
}

impl PhoneNumbersSetCxmlApplicationRequest {
    pub fn new(application_id: impl Into<String>) -> Self {
        PhoneNumbersSetCxmlApplicationRequest {
            application_id: application_id.into(),
            ..Default::default()
        }
    }
    #[must_use]
    pub fn extra(mut self, key: impl Into<String>, value: impl Into<Value>) -> Self {
        self.extras.insert(key.into(), value.into());
        self
    }
    #[must_use]
    pub fn build(self) -> Value {
        let mut obj = Map::new();
        obj.insert("call_handler".to_string(), Value::from("laml_application"));
        obj.insert(
            "call_laml_application_id".to_string(),
            Value::from(self.application_id),
        );
        for (k, v) in self.extras {
            obj.insert(k, v);
        }
        Value::Object(obj)
    }
}

/// Named request parameters for a generated `set_*` wrapper (binds args to
/// update-request fields + a fixed `call_handler`; Rust options-builder idiom).
#[derive(Debug, Clone, Default)]
pub struct PhoneNumbersSetCxmlWebhookRequest {
    url: String,
    fallback_url: Option<String>,
    status_callback_url: Option<String>,
    extras: Map<String, Value>,
}

impl PhoneNumbersSetCxmlWebhookRequest {
    pub fn new(url: impl Into<String>) -> Self {
        PhoneNumbersSetCxmlWebhookRequest {
            url: url.into(),
            ..Default::default()
        }
    }
    #[must_use]
    pub fn fallback_url(mut self, value: impl Into<String>) -> Self {
        self.fallback_url = Some(value.into());
        self
    }
    #[must_use]
    pub fn status_callback_url(mut self, value: impl Into<String>) -> Self {
        self.status_callback_url = Some(value.into());
        self
    }
    #[must_use]
    pub fn extra(mut self, key: impl Into<String>, value: impl Into<Value>) -> Self {
        self.extras.insert(key.into(), value.into());
        self
    }
    #[must_use]
    pub fn build(self) -> Value {
        let mut obj = Map::new();
        obj.insert("call_handler".to_string(), Value::from("laml_webhooks"));
        obj.insert("call_request_url".to_string(), Value::from(self.url));
        if let Some(v) = self.fallback_url {
            obj.insert("call_fallback_url".to_string(), Value::from(v));
        }
        if let Some(v) = self.status_callback_url {
            obj.insert("call_status_callback_url".to_string(), Value::from(v));
        }
        for (k, v) in self.extras {
            obj.insert(k, v);
        }
        Value::Object(obj)
    }
}

/// Named request parameters for a generated `set_*` wrapper (binds args to
/// update-request fields + a fixed `call_handler`; Rust options-builder idiom).
#[derive(Debug, Clone, Default)]
pub struct PhoneNumbersSetRelayApplicationRequest {
    name: String,
    extras: Map<String, Value>,
}

impl PhoneNumbersSetRelayApplicationRequest {
    pub fn new(name: impl Into<String>) -> Self {
        PhoneNumbersSetRelayApplicationRequest {
            name: name.into(),
            ..Default::default()
        }
    }
    #[must_use]
    pub fn extra(mut self, key: impl Into<String>, value: impl Into<Value>) -> Self {
        self.extras.insert(key.into(), value.into());
        self
    }
    #[must_use]
    pub fn build(self) -> Value {
        let mut obj = Map::new();
        obj.insert("call_handler".to_string(), Value::from("relay_application"));
        obj.insert("call_relay_application".to_string(), Value::from(self.name));
        for (k, v) in self.extras {
            obj.insert(k, v);
        }
        Value::Object(obj)
    }
}

/// Named request parameters for a generated `set_*` wrapper (binds args to
/// update-request fields + a fixed `call_handler`; Rust options-builder idiom).
#[derive(Debug, Clone, Default)]
pub struct PhoneNumbersSetRelayTopicRequest {
    topic: String,
    status_callback_url: Option<String>,
    extras: Map<String, Value>,
}

impl PhoneNumbersSetRelayTopicRequest {
    pub fn new(topic: impl Into<String>) -> Self {
        PhoneNumbersSetRelayTopicRequest {
            topic: topic.into(),
            ..Default::default()
        }
    }
    #[must_use]
    pub fn status_callback_url(mut self, value: impl Into<String>) -> Self {
        self.status_callback_url = Some(value.into());
        self
    }
    #[must_use]
    pub fn extra(mut self, key: impl Into<String>, value: impl Into<Value>) -> Self {
        self.extras.insert(key.into(), value.into());
        self
    }
    #[must_use]
    pub fn build(self) -> Value {
        let mut obj = Map::new();
        obj.insert("call_handler".to_string(), Value::from("relay_topic"));
        obj.insert("call_relay_topic".to_string(), Value::from(self.topic));
        if let Some(v) = self.status_callback_url {
            obj.insert(
                "call_relay_topic_status_callback_url".to_string(),
                Value::from(v),
            );
        }
        for (k, v) in self.extras {
            obj.insert(k, v);
        }
        Value::Object(obj)
    }
}

/// Named request parameters for a generated `set_*` wrapper (binds args to
/// update-request fields + a fixed `call_handler`; Rust options-builder idiom).
#[derive(Debug, Clone, Default)]
pub struct PhoneNumbersSetSwmlWebhookRequest {
    url: String,
    extras: Map<String, Value>,
}

impl PhoneNumbersSetSwmlWebhookRequest {
    pub fn new(url: impl Into<String>) -> Self {
        PhoneNumbersSetSwmlWebhookRequest {
            url: url.into(),
            ..Default::default()
        }
    }
    #[must_use]
    pub fn extra(mut self, key: impl Into<String>, value: impl Into<Value>) -> Self {
        self.extras.insert(key.into(), value.into());
        self
    }
    #[must_use]
    pub fn build(self) -> Value {
        let mut obj = Map::new();
        obj.insert("call_handler".to_string(), Value::from("relay_script"));
        obj.insert("call_relay_script_url".to_string(), Value::from(self.url));
        for (k, v) in self.extras {
            obj.insert(k, v);
        }
        Value::Object(obj)
    }
}

/// Named request parameters for the generated method (Rust options-builder
/// idiom — required fields in `new`, optionals via setters, `extras` open door).
#[derive(Debug, Clone, Default)]
pub struct RegistryCampaignsCreateOrderRequest {
    phone_numbers: Option<serde_json::Value>,
    status_callback_url: Option<String>,
    extras: Map<String, Value>,
}

impl RegistryCampaignsCreateOrderRequest {
    /// Construct the request with its required fields.
    pub fn new() -> Self {
        RegistryCampaignsCreateOrderRequest {
            ..Default::default()
        }
    }
    /// Set the optional `phone_numbers` field.
    #[must_use]
    pub fn phone_numbers(mut self, value: serde_json::Value) -> Self {
        self.phone_numbers = Some(value);
        self
    }
    /// Set the optional `status_callback_url` field.
    #[must_use]
    pub fn status_callback_url(mut self, value: impl Into<String>) -> Self {
        self.status_callback_url = Some(value.into());
        self
    }
    /// Add a forward-compat field the spec does not yet name.
    #[must_use]
    pub fn extra(mut self, key: impl Into<String>, value: impl Into<Value>) -> Self {
        self.extras.insert(key.into(), value.into());
        self
    }
    /// Assemble the `body` JSON object (unset optionals omitted).
    #[must_use]
    pub fn build(self) -> Value {
        let mut obj = Map::new();
        if let Some(v) = self.phone_numbers {
            obj.insert("phone_numbers".to_string(), v);
        }
        if let Some(v) = self.status_callback_url {
            obj.insert("status_callback_url".to_string(), Value::from(v));
        }
        for (k, v) in self.extras {
            obj.insert(k, v);
        }
        Value::Object(obj)
    }
}

/// Named request parameters for the generated method (Rust options-builder
/// idiom — required fields in `new`, optionals via setters, `extras` open door).
#[derive(Debug, Clone, Default)]
pub struct RegistryCampaignsUpdateRequest {
    name: Option<String>,
    extras: Map<String, Value>,
}

impl RegistryCampaignsUpdateRequest {
    /// Construct the request with its required fields.
    pub fn new() -> Self {
        RegistryCampaignsUpdateRequest {
            ..Default::default()
        }
    }
    /// Set the optional `name` field.
    #[must_use]
    pub fn name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }
    /// Add a forward-compat field the spec does not yet name.
    #[must_use]
    pub fn extra(mut self, key: impl Into<String>, value: impl Into<Value>) -> Self {
        self.extras.insert(key.into(), value.into());
        self
    }
    /// Assemble the `body` JSON object (unset optionals omitted).
    #[must_use]
    pub fn build(self) -> Value {
        let mut obj = Map::new();
        if let Some(v) = self.name {
            obj.insert("name".to_string(), Value::from(v));
        }
        for (k, v) in self.extras {
            obj.insert(k, v);
        }
        Value::Object(obj)
    }
}

/// Named request parameters for the generated method (Rust options-builder
/// idiom — required fields in `new`, optionals via setters, `extras` open door).
#[derive(Debug, Clone, Default)]
pub struct ShortCodesUpdateRequest {
    name: String,
    message_handler: String,
    message_request_url: Option<String>,
    message_request_method: Option<String>,
    message_fallback_url: Option<String>,
    message_fallback_method: Option<String>,
    message_laml_application_id: Option<String>,
    message_relay_context: Option<String>,
    extras: Map<String, Value>,
}

impl ShortCodesUpdateRequest {
    /// Construct the request with its required fields.
    pub fn new(name: impl Into<String>, message_handler: impl Into<String>) -> Self {
        ShortCodesUpdateRequest {
            name: name.into(),
            message_handler: message_handler.into(),
            ..Default::default()
        }
    }
    /// Set the optional `message_request_url` field.
    #[must_use]
    pub fn message_request_url(mut self, value: impl Into<String>) -> Self {
        self.message_request_url = Some(value.into());
        self
    }
    /// Set the optional `message_request_method` field.
    #[must_use]
    pub fn message_request_method(mut self, value: impl Into<String>) -> Self {
        self.message_request_method = Some(value.into());
        self
    }
    /// Set the optional `message_fallback_url` field.
    #[must_use]
    pub fn message_fallback_url(mut self, value: impl Into<String>) -> Self {
        self.message_fallback_url = Some(value.into());
        self
    }
    /// Set the optional `message_fallback_method` field.
    #[must_use]
    pub fn message_fallback_method(mut self, value: impl Into<String>) -> Self {
        self.message_fallback_method = Some(value.into());
        self
    }
    /// Set the optional `message_laml_application_id` field.
    #[must_use]
    pub fn message_laml_application_id(mut self, value: impl Into<String>) -> Self {
        self.message_laml_application_id = Some(value.into());
        self
    }
    /// Set the optional `message_relay_context` field.
    #[must_use]
    pub fn message_relay_context(mut self, value: impl Into<String>) -> Self {
        self.message_relay_context = Some(value.into());
        self
    }
    /// Add a forward-compat field the spec does not yet name.
    #[must_use]
    pub fn extra(mut self, key: impl Into<String>, value: impl Into<Value>) -> Self {
        self.extras.insert(key.into(), value.into());
        self
    }
    /// Assemble the `body` JSON object (unset optionals omitted).
    #[must_use]
    pub fn build(self) -> Value {
        let mut obj = Map::new();
        obj.insert("name".to_string(), Value::from(self.name));
        obj.insert(
            "message_handler".to_string(),
            Value::from(self.message_handler),
        );
        if let Some(v) = self.message_request_url {
            obj.insert("message_request_url".to_string(), Value::from(v));
        }
        if let Some(v) = self.message_request_method {
            obj.insert("message_request_method".to_string(), Value::from(v));
        }
        if let Some(v) = self.message_fallback_url {
            obj.insert("message_fallback_url".to_string(), Value::from(v));
        }
        if let Some(v) = self.message_fallback_method {
            obj.insert("message_fallback_method".to_string(), Value::from(v));
        }
        if let Some(v) = self.message_laml_application_id {
            obj.insert("message_laml_application_id".to_string(), Value::from(v));
        }
        if let Some(v) = self.message_relay_context {
            obj.insert("message_relay_context".to_string(), Value::from(v));
        }
        for (k, v) in self.extras {
            obj.insert(k, v);
        }
        Value::Object(obj)
    }
}

/// Named request parameters for the generated method (Rust options-builder
/// idiom — required fields in `new`, optionals via setters, `extras` open door).
#[derive(Debug, Clone, Default)]
pub struct SipProfileUpdateRequest {
    domain_identifier: Option<String>,
    default_codecs: Option<serde_json::Value>,
    default_ciphers: Option<serde_json::Value>,
    default_encryption: Option<String>,
    default_send_as: Option<String>,
    extras: Map<String, Value>,
}

impl SipProfileUpdateRequest {
    /// Construct the request with its required fields.
    pub fn new() -> Self {
        SipProfileUpdateRequest {
            ..Default::default()
        }
    }
    /// Set the optional `domain_identifier` field.
    #[must_use]
    pub fn domain_identifier(mut self, value: impl Into<String>) -> Self {
        self.domain_identifier = Some(value.into());
        self
    }
    /// Set the optional `default_codecs` field.
    #[must_use]
    pub fn default_codecs(mut self, value: serde_json::Value) -> Self {
        self.default_codecs = Some(value);
        self
    }
    /// Set the optional `default_ciphers` field.
    #[must_use]
    pub fn default_ciphers(mut self, value: serde_json::Value) -> Self {
        self.default_ciphers = Some(value);
        self
    }
    /// Set the optional `default_encryption` field.
    #[must_use]
    pub fn default_encryption(mut self, value: impl Into<String>) -> Self {
        self.default_encryption = Some(value.into());
        self
    }
    /// Set the optional `default_send_as` field.
    #[must_use]
    pub fn default_send_as(mut self, value: impl Into<String>) -> Self {
        self.default_send_as = Some(value.into());
        self
    }
    /// Add a forward-compat field the spec does not yet name.
    #[must_use]
    pub fn extra(mut self, key: impl Into<String>, value: impl Into<Value>) -> Self {
        self.extras.insert(key.into(), value.into());
        self
    }
    /// Assemble the `body` JSON object (unset optionals omitted).
    #[must_use]
    pub fn build(self) -> Value {
        let mut obj = Map::new();
        if let Some(v) = self.domain_identifier {
            obj.insert("domain_identifier".to_string(), Value::from(v));
        }
        if let Some(v) = self.default_codecs {
            obj.insert("default_codecs".to_string(), v);
        }
        if let Some(v) = self.default_ciphers {
            obj.insert("default_ciphers".to_string(), v);
        }
        if let Some(v) = self.default_encryption {
            obj.insert("default_encryption".to_string(), Value::from(v));
        }
        if let Some(v) = self.default_send_as {
            obj.insert("default_send_as".to_string(), Value::from(v));
        }
        for (k, v) in self.extras {
            obj.insert(k, v);
        }
        Value::Object(obj)
    }
}

/// Named request parameters for the generated method (Rust options-builder
/// idiom — required fields in `new`, optionals via setters, `extras` open door).
#[derive(Debug, Clone, Default)]
pub struct VerifiedCallersSubmitVerificationRequest {
    verification_code: String,
    extras: Map<String, Value>,
}

impl VerifiedCallersSubmitVerificationRequest {
    /// Construct the request with its required fields.
    pub fn new(verification_code: impl Into<String>) -> Self {
        VerifiedCallersSubmitVerificationRequest {
            verification_code: verification_code.into(),
            ..Default::default()
        }
    }
    /// Add a forward-compat field the spec does not yet name.
    #[must_use]
    pub fn extra(mut self, key: impl Into<String>, value: impl Into<Value>) -> Self {
        self.extras.insert(key.into(), value.into());
        self
    }
    /// Assemble the `body` JSON object (unset optionals omitted).
    #[must_use]
    pub fn build(self) -> Value {
        let mut obj = Map::new();
        obj.insert(
            "verification_code".to_string(),
            Value::from(self.verification_code),
        );
        for (k, v) in self.extras {
            obj.insert(k, v);
        }
        Value::Object(obj)
    }
}

/// `Addresses` resource for the SignalWire `relay-rest` REST API.
///
/// Composes [`BaseResource`] (its base path is baked in) and adds the
/// resource's own methods.
pub struct Addresses<'a> {
    base: BaseResource<'a>,
}

impl<'a> Addresses<'a> {
    /// Construct the resource; its base path (§4) is baked in.
    #[must_use]
    pub fn new(client: &'a HttpClient) -> Self {
        Addresses {
            base: BaseResource::new(client, "/api/relay/rest/addresses"),
        }
    }

    #[must_use]
    pub fn base_path(&self) -> &str {
        self.base.base_path()
    }

    fn client(&self) -> &HttpClient {
        self.base.client()
    }

    fn path(&self, parts: &[&str]) -> String {
        self.base.path(parts)
    }

    /// `GET /addresses` (generated operation method; query params).
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure, a non-2xx status.
    pub fn list(&self, params: &HashMap<String, String>) -> Result<Value, SignalWireRestError> {
        self.client().get(self.base_path(), params)
    }

    /// `POST /addresses` (generated operation method).
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure, a non-2xx
    /// status, or an unparseable response body.
    pub fn create(&self, request: AddressesCreateRequest) -> Result<Value, SignalWireRestError> {
        self.client().post(self.base_path(), &request.build())
    }

    /// `GET /addresses/{id}` (generated operation method; query params).
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure, a non-2xx status.
    pub fn get(
        &self,
        id: &str,
        params: &HashMap<String, String>,
    ) -> Result<Value, SignalWireRestError> {
        self.client().get(&self.path(&[id]), params)
    }

    /// `DELETE /addresses/{id}` (generated operation method).
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure, a non-2xx status.
    pub fn delete(&self, id: &str) -> Result<Value, SignalWireRestError> {
        self.client().delete(&self.path(&[id]))
    }
}

/// `ImportedNumbers` resource for the SignalWire `relay-rest` REST API.
///
/// Composes [`BaseResource`] (its base path is baked in) and adds the
/// resource's own methods.
pub struct ImportedNumbers<'a> {
    base: BaseResource<'a>,
}

impl<'a> ImportedNumbers<'a> {
    /// Construct the resource; its base path (§4) is baked in.
    #[must_use]
    pub fn new(client: &'a HttpClient) -> Self {
        ImportedNumbers {
            base: BaseResource::new(client, "/api/relay/rest/imported_phone_numbers"),
        }
    }

    #[must_use]
    pub fn base_path(&self) -> &str {
        self.base.base_path()
    }

    fn client(&self) -> &HttpClient {
        self.base.client()
    }

    /// `POST /imported_phone_numbers` (generated operation method).
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure, a non-2xx
    /// status, or an unparseable response body.
    pub fn create(
        &self,
        request: ImportedNumbersCreateRequest,
    ) -> Result<Value, SignalWireRestError> {
        self.client().post(self.base_path(), &request.build())
    }
}

/// `Lookup` resource for the SignalWire `relay-rest` REST API.
///
/// Composes [`BaseResource`] (its base path is baked in) and adds the
/// resource's own methods.
pub struct Lookup<'a> {
    base: BaseResource<'a>,
}

impl<'a> Lookup<'a> {
    /// Construct the resource; its base path (§4) is baked in.
    #[must_use]
    pub fn new(client: &'a HttpClient) -> Self {
        Lookup {
            base: BaseResource::new(client, "/api/relay/rest/lookup"),
        }
    }

    #[must_use]
    pub fn base_path(&self) -> &str {
        self.base.base_path()
    }

    fn client(&self) -> &HttpClient {
        self.base.client()
    }

    fn path(&self, parts: &[&str]) -> String {
        self.base.path(parts)
    }

    /// `GET /lookup/phone_number/{e164_number}` (generated operation method; query params).
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure, a non-2xx status.
    pub fn phone_number(
        &self,
        e164: &str,
        params: &HashMap<String, String>,
    ) -> Result<Value, SignalWireRestError> {
        self.client()
            .get(&self.path(&["phone_number", e164]), params)
    }
}

/// `Mfa` resource for the SignalWire `relay-rest` REST API.
///
/// Composes [`BaseResource`] (its base path is baked in) and adds the
/// resource's own methods.
pub struct Mfa<'a> {
    base: BaseResource<'a>,
}

impl<'a> Mfa<'a> {
    /// Construct the resource; its base path (§4) is baked in.
    #[must_use]
    pub fn new(client: &'a HttpClient) -> Self {
        Mfa {
            base: BaseResource::new(client, "/api/relay/rest/mfa"),
        }
    }

    #[must_use]
    pub fn base_path(&self) -> &str {
        self.base.base_path()
    }

    fn client(&self) -> &HttpClient {
        self.base.client()
    }

    fn path(&self, parts: &[&str]) -> String {
        self.base.path(parts)
    }

    /// `POST /mfa/sms` (generated operation method).
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure, a non-2xx
    /// status, or an unparseable response body.
    pub fn sms(&self, request: MfaSmsRequest) -> Result<Value, SignalWireRestError> {
        self.client().post(&self.path(&["sms"]), &request.build())
    }

    /// `POST /mfa/call` (generated operation method).
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure, a non-2xx
    /// status, or an unparseable response body.
    pub fn call(&self, request: MfaCallRequest) -> Result<Value, SignalWireRestError> {
        self.client().post(&self.path(&["call"]), &request.build())
    }

    /// `POST /mfa/{mfa_request_id}/verify` (generated operation method).
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure, a non-2xx
    /// status, or an unparseable response body.
    pub fn verify(
        &self,
        request_id: &str,
        request: MfaVerifyRequest,
    ) -> Result<Value, SignalWireRestError> {
        self.client()
            .post(&self.path(&[request_id, "verify"]), &request.build())
    }
}

/// `NumberGroups` resource for the SignalWire `relay-rest` REST API.
///
/// Composes [`CrudResource`] (its base path is baked in) and adds the
/// resource's own methods.
pub struct NumberGroups<'a> {
    base: CrudResource<'a>,
}

impl<'a> NumberGroups<'a> {
    /// Construct the resource; its base path (§4) is baked in.
    #[must_use]
    pub fn new(client: &'a HttpClient) -> Self {
        NumberGroups {
            base: CrudResource::new(client, "/api/relay/rest/number_groups", "PUT"),
        }
    }

    #[must_use]
    pub fn base_path(&self) -> &str {
        self.base.base_path()
    }

    fn client(&self) -> &HttpClient {
        self.base.client()
    }

    fn path(&self, parts: &[&str]) -> String {
        self.base.path(parts)
    }

    /// `list` (delegated to the base; GET base path).
    ///
    /// # Errors
    /// See the base resource.
    pub fn list(&self, params: &HashMap<String, String>) -> Result<Value, SignalWireRestError> {
        self.base.list(params)
    }

    /// `get` (delegated to the base; GET base/{id}).
    ///
    /// # Errors
    /// See the base resource.
    pub fn get(&self, id: &str) -> Result<Value, SignalWireRestError> {
        self.base.get(id)
    }

    /// `create` (delegated to the base; POST base path).
    ///
    /// # Errors
    /// See the base resource.
    pub fn create(&self, data: &Value) -> Result<Value, SignalWireRestError> {
        self.base.create(data)
    }

    /// `update` (delegated to the base; PUT/PATCH base/{id}).
    ///
    /// # Errors
    /// See the base resource.
    pub fn update(&self, id: &str, data: &Value) -> Result<Value, SignalWireRestError> {
        self.base.update(id, data)
    }

    /// `delete` (delegated to the base; DELETE base/{id}).
    ///
    /// # Errors
    /// See the base resource.
    pub fn delete(&self, id: &str) -> Result<Value, SignalWireRestError> {
        self.base.delete(id)
    }

    /// `GET /number_groups/{NumberGroupId}/number_group_memberships` (generated operation method; query params).
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure, a non-2xx status.
    pub fn list_memberships(
        &self,
        group_id: &str,
        params: &HashMap<String, String>,
    ) -> Result<Value, SignalWireRestError> {
        self.client()
            .get(&self.path(&[group_id, "number_group_memberships"]), params)
    }

    /// `POST /number_groups/{NumberGroupId}/number_group_memberships` (generated operation method).
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure, a non-2xx
    /// status, or an unparseable response body.
    pub fn add_membership(
        &self,
        group_id: &str,
        request: NumberGroupsAddMembershipRequest,
    ) -> Result<Value, SignalWireRestError> {
        self.client().post(
            &self.path(&[group_id, "number_group_memberships"]),
            &request.build(),
        )
    }

    /// `GET /number_group_memberships/{id}` (generated operation method; query params).
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure, a non-2xx status.
    pub fn get_membership(
        &self,
        id: &str,
        params: &HashMap<String, String>,
    ) -> Result<Value, SignalWireRestError> {
        self.client().get(
            &format!("/api/relay/rest/number_group_memberships/{id}"),
            params,
        )
    }

    /// `DELETE /number_group_memberships/{id}` (generated operation method).
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure, a non-2xx status.
    pub fn delete_membership(&self, id: &str) -> Result<Value, SignalWireRestError> {
        self.client()
            .delete(&format!("/api/relay/rest/number_group_memberships/{id}"))
    }
}

/// `PhoneNumbers` resource for the SignalWire `relay-rest` REST API.
///
/// Composes [`CrudResource`] (its base path is baked in) and adds the
/// resource's own methods.
pub struct PhoneNumbers<'a> {
    base: CrudResource<'a>,
}

impl<'a> PhoneNumbers<'a> {
    /// Construct the resource; its base path (§4) is baked in.
    #[must_use]
    pub fn new(client: &'a HttpClient) -> Self {
        PhoneNumbers {
            base: CrudResource::new(client, "/api/relay/rest/phone_numbers", "PUT"),
        }
    }

    #[must_use]
    pub fn base_path(&self) -> &str {
        self.base.base_path()
    }

    fn client(&self) -> &HttpClient {
        self.base.client()
    }

    fn path(&self, parts: &[&str]) -> String {
        self.base.path(parts)
    }

    /// `list` (delegated to the base; GET base path).
    ///
    /// # Errors
    /// See the base resource.
    pub fn list(&self, params: &HashMap<String, String>) -> Result<Value, SignalWireRestError> {
        self.base.list(params)
    }

    /// `get` (delegated to the base; GET base/{id}).
    ///
    /// # Errors
    /// See the base resource.
    pub fn get(&self, id: &str) -> Result<Value, SignalWireRestError> {
        self.base.get(id)
    }

    /// `create` (delegated to the base; POST base path).
    ///
    /// # Errors
    /// See the base resource.
    pub fn create(&self, data: &Value) -> Result<Value, SignalWireRestError> {
        self.base.create(data)
    }

    /// `update` (delegated to the base; PUT/PATCH base/{id}).
    ///
    /// # Errors
    /// See the base resource.
    pub fn update(&self, id: &str, data: &Value) -> Result<Value, SignalWireRestError> {
        self.base.update(id, data)
    }

    /// `delete` (delegated to the base; DELETE base/{id}).
    ///
    /// # Errors
    /// See the base resource.
    pub fn delete(&self, id: &str) -> Result<Value, SignalWireRestError> {
        self.base.delete(id)
    }

    /// `GET /phone_numbers/search` (generated operation method; query params).
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure, a non-2xx status.
    pub fn search(&self, params: &HashMap<String, String>) -> Result<Value, SignalWireRestError> {
        self.client().get(&self.path(&["search"]), params)
    }

    /// `set_set_swml_webhook` — update wrapper binding a fixed `call_handler` (§7).
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure or a non-2xx status.
    pub fn set_swml_webhook(
        &self,
        resource_id: &str,
        request: PhoneNumbersSetSwmlWebhookRequest,
    ) -> Result<Value, SignalWireRestError> {
        self.update(resource_id, &request.build())
    }

    /// `set_set_cxml_webhook` — update wrapper binding a fixed `call_handler` (§7).
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure or a non-2xx status.
    pub fn set_cxml_webhook(
        &self,
        resource_id: &str,
        request: PhoneNumbersSetCxmlWebhookRequest,
    ) -> Result<Value, SignalWireRestError> {
        self.update(resource_id, &request.build())
    }

    /// `set_set_cxml_application` — update wrapper binding a fixed `call_handler` (§7).
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure or a non-2xx status.
    pub fn set_cxml_application(
        &self,
        resource_id: &str,
        request: PhoneNumbersSetCxmlApplicationRequest,
    ) -> Result<Value, SignalWireRestError> {
        self.update(resource_id, &request.build())
    }

    /// `set_set_ai_agent` — update wrapper binding a fixed `call_handler` (§7).
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure or a non-2xx status.
    pub fn set_ai_agent(
        &self,
        resource_id: &str,
        request: PhoneNumbersSetAiAgentRequest,
    ) -> Result<Value, SignalWireRestError> {
        self.update(resource_id, &request.build())
    }

    /// `set_set_call_flow` — update wrapper binding a fixed `call_handler` (§7).
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure or a non-2xx status.
    pub fn set_call_flow(
        &self,
        resource_id: &str,
        request: PhoneNumbersSetCallFlowRequest,
    ) -> Result<Value, SignalWireRestError> {
        self.update(resource_id, &request.build())
    }

    /// `set_set_relay_application` — update wrapper binding a fixed `call_handler` (§7).
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure or a non-2xx status.
    pub fn set_relay_application(
        &self,
        resource_id: &str,
        request: PhoneNumbersSetRelayApplicationRequest,
    ) -> Result<Value, SignalWireRestError> {
        self.update(resource_id, &request.build())
    }

    /// `set_set_relay_topic` — update wrapper binding a fixed `call_handler` (§7).
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure or a non-2xx status.
    pub fn set_relay_topic(
        &self,
        resource_id: &str,
        request: PhoneNumbersSetRelayTopicRequest,
    ) -> Result<Value, SignalWireRestError> {
        self.update(resource_id, &request.build())
    }
}

/// `Queues` resource for the SignalWire `relay-rest` REST API.
///
/// Composes [`CrudResource`] (its base path is baked in) and adds the
/// resource's own methods.
pub struct Queues<'a> {
    base: CrudResource<'a>,
}

impl<'a> Queues<'a> {
    /// Construct the resource; its base path (§4) is baked in.
    #[must_use]
    pub fn new(client: &'a HttpClient) -> Self {
        Queues {
            base: CrudResource::new(client, "/api/relay/rest/queues", "PUT"),
        }
    }

    #[must_use]
    pub fn base_path(&self) -> &str {
        self.base.base_path()
    }

    fn client(&self) -> &HttpClient {
        self.base.client()
    }

    fn path(&self, parts: &[&str]) -> String {
        self.base.path(parts)
    }

    /// `list` (delegated to the base; GET base path).
    ///
    /// # Errors
    /// See the base resource.
    pub fn list(&self, params: &HashMap<String, String>) -> Result<Value, SignalWireRestError> {
        self.base.list(params)
    }

    /// `get` (delegated to the base; GET base/{id}).
    ///
    /// # Errors
    /// See the base resource.
    pub fn get(&self, id: &str) -> Result<Value, SignalWireRestError> {
        self.base.get(id)
    }

    /// `create` (delegated to the base; POST base path).
    ///
    /// # Errors
    /// See the base resource.
    pub fn create(&self, data: &Value) -> Result<Value, SignalWireRestError> {
        self.base.create(data)
    }

    /// `update` (delegated to the base; PUT/PATCH base/{id}).
    ///
    /// # Errors
    /// See the base resource.
    pub fn update(&self, id: &str, data: &Value) -> Result<Value, SignalWireRestError> {
        self.base.update(id, data)
    }

    /// `delete` (delegated to the base; DELETE base/{id}).
    ///
    /// # Errors
    /// See the base resource.
    pub fn delete(&self, id: &str) -> Result<Value, SignalWireRestError> {
        self.base.delete(id)
    }

    /// `GET /queues/{queue_id}/members` (generated operation method; query params).
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure, a non-2xx status.
    pub fn list_members(
        &self,
        queue_id: &str,
        params: &HashMap<String, String>,
    ) -> Result<Value, SignalWireRestError> {
        self.client()
            .get(&self.path(&[queue_id, "members"]), params)
    }

    /// `GET /queues/{queue_id}/members/next` (generated operation method; query params).
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure, a non-2xx status.
    pub fn get_next_member(
        &self,
        queue_id: &str,
        params: &HashMap<String, String>,
    ) -> Result<Value, SignalWireRestError> {
        self.client()
            .get(&self.path(&[queue_id, "members", "next"]), params)
    }

    /// `GET /queues/{queue_id}/members/{id}` (generated operation method; query params).
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure, a non-2xx status.
    pub fn get_member(
        &self,
        queue_id: &str,
        id: &str,
        params: &HashMap<String, String>,
    ) -> Result<Value, SignalWireRestError> {
        self.client()
            .get(&self.path(&[queue_id, "members", id]), params)
    }
}

/// `Recordings` resource for the SignalWire `relay-rest` REST API.
///
/// Composes [`BaseResource`] (its base path is baked in) and adds the
/// resource's own methods.
pub struct Recordings<'a> {
    base: BaseResource<'a>,
}

impl<'a> Recordings<'a> {
    /// Construct the resource; its base path (§4) is baked in.
    #[must_use]
    pub fn new(client: &'a HttpClient) -> Self {
        Recordings {
            base: BaseResource::new(client, "/api/relay/rest/recordings"),
        }
    }

    #[must_use]
    pub fn base_path(&self) -> &str {
        self.base.base_path()
    }

    fn client(&self) -> &HttpClient {
        self.base.client()
    }

    fn path(&self, parts: &[&str]) -> String {
        self.base.path(parts)
    }

    /// `GET /recordings` (generated operation method; query params).
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure, a non-2xx status.
    pub fn list(&self, params: &HashMap<String, String>) -> Result<Value, SignalWireRestError> {
        self.client().get(self.base_path(), params)
    }

    /// `GET /recordings/{id}` (generated operation method; query params).
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure, a non-2xx status.
    pub fn get(
        &self,
        id: &str,
        params: &HashMap<String, String>,
    ) -> Result<Value, SignalWireRestError> {
        self.client().get(&self.path(&[id]), params)
    }

    /// `DELETE /recordings/{id}` (generated operation method).
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure, a non-2xx status.
    pub fn delete(&self, id: &str) -> Result<Value, SignalWireRestError> {
        self.client().delete(&self.path(&[id]))
    }
}

/// `RegistryBrands` resource for the SignalWire `relay-rest` REST API.
///
/// Composes [`BaseResource`] (its base path is baked in) and adds the
/// resource's own methods.
pub struct RegistryBrands<'a> {
    base: BaseResource<'a>,
}

impl<'a> RegistryBrands<'a> {
    /// Construct the resource; its base path (§4) is baked in.
    #[must_use]
    pub fn new(client: &'a HttpClient) -> Self {
        RegistryBrands {
            base: BaseResource::new(client, "/api/relay/rest/registry/beta/brands"),
        }
    }

    #[must_use]
    pub fn base_path(&self) -> &str {
        self.base.base_path()
    }

    fn client(&self) -> &HttpClient {
        self.base.client()
    }

    fn path(&self, parts: &[&str]) -> String {
        self.base.path(parts)
    }

    /// `GET /registry/beta/brands` (generated operation method; query params).
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure, a non-2xx status.
    pub fn list(&self, params: &HashMap<String, String>) -> Result<Value, SignalWireRestError> {
        self.client().get(self.base_path(), params)
    }

    /// `POST /registry/beta/brands` (generated operation method; union body).
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure, a non-2xx
    /// status, or an unparseable response body.
    pub fn create(&self, body: &Value) -> Result<Value, SignalWireRestError> {
        self.client().post(self.base_path(), body)
    }

    /// `GET /registry/beta/brands/{id}` (generated operation method; query params).
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure, a non-2xx status.
    pub fn get(
        &self,
        id: &str,
        params: &HashMap<String, String>,
    ) -> Result<Value, SignalWireRestError> {
        self.client().get(&self.path(&[id]), params)
    }

    /// `GET /registry/beta/brands/{id}/campaigns` (generated operation method; query params).
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure, a non-2xx status.
    pub fn list_campaigns(
        &self,
        id: &str,
        params: &HashMap<String, String>,
    ) -> Result<Value, SignalWireRestError> {
        self.client().get(&self.path(&[id, "campaigns"]), params)
    }

    /// `POST /registry/beta/brands/{id}/campaigns` (generated operation method; union body).
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure, a non-2xx
    /// status, or an unparseable response body.
    pub fn create_campaign(&self, id: &str, body: &Value) -> Result<Value, SignalWireRestError> {
        self.client().post(&self.path(&[id, "campaigns"]), body)
    }
}

/// `RegistryCampaigns` resource for the SignalWire `relay-rest` REST API.
///
/// Composes [`BaseResource`] (its base path is baked in) and adds the
/// resource's own methods.
pub struct RegistryCampaigns<'a> {
    base: BaseResource<'a>,
}

impl<'a> RegistryCampaigns<'a> {
    /// Construct the resource; its base path (§4) is baked in.
    #[must_use]
    pub fn new(client: &'a HttpClient) -> Self {
        RegistryCampaigns {
            base: BaseResource::new(client, "/api/relay/rest/registry/beta/campaigns"),
        }
    }

    #[must_use]
    pub fn base_path(&self) -> &str {
        self.base.base_path()
    }

    fn client(&self) -> &HttpClient {
        self.base.client()
    }

    fn path(&self, parts: &[&str]) -> String {
        self.base.path(parts)
    }

    /// `GET /registry/beta/campaigns/{id}` (generated operation method; query params).
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure, a non-2xx status.
    pub fn get(
        &self,
        id: &str,
        params: &HashMap<String, String>,
    ) -> Result<Value, SignalWireRestError> {
        self.client().get(&self.path(&[id]), params)
    }

    /// `PUT /registry/beta/campaigns/{id}` (generated operation method).
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure, a non-2xx
    /// status, or an unparseable response body.
    pub fn update(
        &self,
        id: &str,
        request: RegistryCampaignsUpdateRequest,
    ) -> Result<Value, SignalWireRestError> {
        self.client().put(&self.path(&[id]), &request.build())
    }

    /// `GET /registry/beta/campaigns/{id}/numbers` (generated operation method; query params).
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure, a non-2xx status.
    pub fn list_numbers(
        &self,
        id: &str,
        params: &HashMap<String, String>,
    ) -> Result<Value, SignalWireRestError> {
        self.client().get(&self.path(&[id, "numbers"]), params)
    }

    /// `GET /registry/beta/campaigns/{id}/orders` (generated operation method; query params).
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure, a non-2xx status.
    pub fn list_orders(
        &self,
        id: &str,
        params: &HashMap<String, String>,
    ) -> Result<Value, SignalWireRestError> {
        self.client().get(&self.path(&[id, "orders"]), params)
    }

    /// `POST /registry/beta/campaigns/{id}/orders` (generated operation method).
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure, a non-2xx
    /// status, or an unparseable response body.
    pub fn create_order(
        &self,
        id: &str,
        request: RegistryCampaignsCreateOrderRequest,
    ) -> Result<Value, SignalWireRestError> {
        self.client()
            .post(&self.path(&[id, "orders"]), &request.build())
    }
}

/// `RegistryNumbers` resource for the SignalWire `relay-rest` REST API.
///
/// Composes [`BaseResource`] (its base path is baked in) and adds the
/// resource's own methods.
pub struct RegistryNumbers<'a> {
    base: BaseResource<'a>,
}

impl<'a> RegistryNumbers<'a> {
    /// Construct the resource; its base path (§4) is baked in.
    #[must_use]
    pub fn new(client: &'a HttpClient) -> Self {
        RegistryNumbers {
            base: BaseResource::new(client, "/api/relay/rest/registry/beta/numbers"),
        }
    }

    #[must_use]
    pub fn base_path(&self) -> &str {
        self.base.base_path()
    }

    fn client(&self) -> &HttpClient {
        self.base.client()
    }

    fn path(&self, parts: &[&str]) -> String {
        self.base.path(parts)
    }

    /// `DELETE /registry/beta/numbers/{id}` (generated operation method).
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure, a non-2xx status.
    pub fn delete(&self, id: &str) -> Result<Value, SignalWireRestError> {
        self.client().delete(&self.path(&[id]))
    }
}

/// `RegistryOrders` resource for the SignalWire `relay-rest` REST API.
///
/// Composes [`BaseResource`] (its base path is baked in) and adds the
/// resource's own methods.
pub struct RegistryOrders<'a> {
    base: BaseResource<'a>,
}

impl<'a> RegistryOrders<'a> {
    /// Construct the resource; its base path (§4) is baked in.
    #[must_use]
    pub fn new(client: &'a HttpClient) -> Self {
        RegistryOrders {
            base: BaseResource::new(client, "/api/relay/rest/registry/beta/orders"),
        }
    }

    #[must_use]
    pub fn base_path(&self) -> &str {
        self.base.base_path()
    }

    fn client(&self) -> &HttpClient {
        self.base.client()
    }

    fn path(&self, parts: &[&str]) -> String {
        self.base.path(parts)
    }

    /// `GET /registry/beta/orders/{id}` (generated operation method; query params).
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure, a non-2xx status.
    pub fn get(
        &self,
        id: &str,
        params: &HashMap<String, String>,
    ) -> Result<Value, SignalWireRestError> {
        self.client().get(&self.path(&[id]), params)
    }
}

/// `ShortCodes` resource for the SignalWire `relay-rest` REST API.
///
/// Composes [`BaseResource`] (its base path is baked in) and adds the
/// resource's own methods.
pub struct ShortCodes<'a> {
    base: BaseResource<'a>,
}

impl<'a> ShortCodes<'a> {
    /// Construct the resource; its base path (§4) is baked in.
    #[must_use]
    pub fn new(client: &'a HttpClient) -> Self {
        ShortCodes {
            base: BaseResource::new(client, "/api/relay/rest/short_codes"),
        }
    }

    #[must_use]
    pub fn base_path(&self) -> &str {
        self.base.base_path()
    }

    fn client(&self) -> &HttpClient {
        self.base.client()
    }

    fn path(&self, parts: &[&str]) -> String {
        self.base.path(parts)
    }

    /// `GET /short_codes` (generated operation method; query params).
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure, a non-2xx status.
    pub fn list(&self, params: &HashMap<String, String>) -> Result<Value, SignalWireRestError> {
        self.client().get(self.base_path(), params)
    }

    /// `GET /short_codes/{id}` (generated operation method; query params).
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure, a non-2xx status.
    pub fn get(
        &self,
        id: &str,
        params: &HashMap<String, String>,
    ) -> Result<Value, SignalWireRestError> {
        self.client().get(&self.path(&[id]), params)
    }

    /// `PUT /short_codes/{id}` (generated operation method).
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure, a non-2xx
    /// status, or an unparseable response body.
    pub fn update(
        &self,
        id: &str,
        request: ShortCodesUpdateRequest,
    ) -> Result<Value, SignalWireRestError> {
        self.client().put(&self.path(&[id]), &request.build())
    }
}

/// `SipProfile` resource for the SignalWire `relay-rest` REST API.
///
/// Composes [`BaseResource`] (its base path is baked in) and adds the
/// resource's own methods.
pub struct SipProfile<'a> {
    base: BaseResource<'a>,
}

impl<'a> SipProfile<'a> {
    /// Construct the resource; its base path (§4) is baked in.
    #[must_use]
    pub fn new(client: &'a HttpClient) -> Self {
        SipProfile {
            base: BaseResource::new(client, "/api/relay/rest/sip_profile"),
        }
    }

    #[must_use]
    pub fn base_path(&self) -> &str {
        self.base.base_path()
    }

    fn client(&self) -> &HttpClient {
        self.base.client()
    }

    /// `GET /sip_profile` (generated operation method; query params).
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure, a non-2xx status.
    pub fn get(&self, params: &HashMap<String, String>) -> Result<Value, SignalWireRestError> {
        self.client().get(self.base_path(), params)
    }

    /// `PUT /sip_profile` (generated operation method).
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure, a non-2xx
    /// status, or an unparseable response body.
    pub fn update(&self, request: SipProfileUpdateRequest) -> Result<Value, SignalWireRestError> {
        self.client().put(self.base_path(), &request.build())
    }
}

/// `VerifiedCallers` resource for the SignalWire `relay-rest` REST API.
///
/// Composes [`CrudResource`] (its base path is baked in) and adds the
/// resource's own methods.
pub struct VerifiedCallers<'a> {
    base: CrudResource<'a>,
}

impl<'a> VerifiedCallers<'a> {
    /// Construct the resource; its base path (§4) is baked in.
    #[must_use]
    pub fn new(client: &'a HttpClient) -> Self {
        VerifiedCallers {
            base: CrudResource::new(client, "/api/relay/rest/verified_caller_ids", "PUT"),
        }
    }

    #[must_use]
    pub fn base_path(&self) -> &str {
        self.base.base_path()
    }

    fn client(&self) -> &HttpClient {
        self.base.client()
    }

    fn path(&self, parts: &[&str]) -> String {
        self.base.path(parts)
    }

    /// `list` (delegated to the base; GET base path).
    ///
    /// # Errors
    /// See the base resource.
    pub fn list(&self, params: &HashMap<String, String>) -> Result<Value, SignalWireRestError> {
        self.base.list(params)
    }

    /// `get` (delegated to the base; GET base/{id}).
    ///
    /// # Errors
    /// See the base resource.
    pub fn get(&self, id: &str) -> Result<Value, SignalWireRestError> {
        self.base.get(id)
    }

    /// `create` (delegated to the base; POST base path).
    ///
    /// # Errors
    /// See the base resource.
    pub fn create(&self, data: &Value) -> Result<Value, SignalWireRestError> {
        self.base.create(data)
    }

    /// `update` (delegated to the base; PUT/PATCH base/{id}).
    ///
    /// # Errors
    /// See the base resource.
    pub fn update(&self, id: &str, data: &Value) -> Result<Value, SignalWireRestError> {
        self.base.update(id, data)
    }

    /// `delete` (delegated to the base; DELETE base/{id}).
    ///
    /// # Errors
    /// See the base resource.
    pub fn delete(&self, id: &str) -> Result<Value, SignalWireRestError> {
        self.base.delete(id)
    }

    /// `POST /verified_caller_ids/{id}/verification` (generated operation method; no body).
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure, a non-2xx status.
    pub fn redial_verification(&self, id: &str) -> Result<Value, SignalWireRestError> {
        self.client().post(
            &self.path(&[id, "verification"]),
            &Value::Object(Map::new()),
        )
    }

    /// `PUT /verified_caller_ids/{id}/verification` (generated operation method).
    ///
    /// # Errors
    /// Returns [`SignalWireRestError`] on transport failure, a non-2xx
    /// status, or an unparseable response body.
    pub fn submit_verification(
        &self,
        id: &str,
        request: VerifiedCallersSubmitVerificationRequest,
    ) -> Result<Value, SignalWireRestError> {
        self.client()
            .put(&self.path(&[id, "verification"]), &request.build())
    }
}
