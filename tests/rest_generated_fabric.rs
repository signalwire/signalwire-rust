// Code generated by scripts/generate_rest_tests.py; DO NOT EDIT.
//
// AUTO-GENERATED full-mock REST wire tests for the 'fabric' namespace — regenerate:
//   python3 scripts/generate_rest_tests.py
//
// Each route the SDK implements (captured from the real client by the
// rest-test-plan binary, joined to the spec operationId) gets a SUCCESS test
// (call it, assert method + matched_route on the mock journal) and an ERROR test
// (arm a 500, assert SignalWireRestError with .status_code() == 500). The
// assertion oracle is the spec operationId — independent of the resource
// generator — so these catch SDK-vs-contract drift, not a generator self-snapshot.
// Full-mock harness fixtures (common::mocktest).

#![allow(unused_imports)]

#[path = "common/mod.rs"]
mod common;

use serde_json::json;
use signalwire::rest::namespaces::generated::calling_resources_generated as cg;
use signalwire::rest::namespaces::generated::chat_resources_generated as chat_gen;
use signalwire::rest::namespaces::generated::datasphere_resources_generated as datasphere_gen;
use signalwire::rest::namespaces::generated::fabric_resources_generated as fabric_gen;
use signalwire::rest::namespaces::generated::project_resources_generated as project_gen;
use signalwire::rest::namespaces::generated::pubsub_resources_generated as pubsub_gen;
use signalwire::rest::namespaces::generated::relay_rest_resources_generated as relay_gen;
use signalwire::rest::namespaces::generated::video_resources_generated as video_gen;

#[test]
fn test_fabric_addresses_get_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c.fabric().addresses().get("x");
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "GET");
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.get_fabric_address")
    );
}

#[test]
fn test_fabric_addresses_get_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set("fabric.get_fabric_address", 500, json!({"error": "x"}));
    let err = c
        .fabric()
        .addresses()
        .get("x")
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.get_fabric_address")
    );
}

#[test]
fn test_fabric_addresses_list_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c
        .fabric()
        .addresses()
        .list(&std::collections::HashMap::new());
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "GET");
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.list_fabric_addresses")
    );
}

#[test]
fn test_fabric_addresses_list_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set("fabric.list_fabric_addresses", 500, json!({"error": "x"}));
    let err = c
        .fabric()
        .addresses()
        .list(&std::collections::HashMap::new())
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.list_fabric_addresses")
    );
}

#[test]
fn test_fabric_ai_agents_create_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c.fabric().ai_agents().create(&serde_json::json!({}));
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "POST");
    assert_eq!(e.matched_route.as_deref(), Some("fabric.create_ai_agent"));
}

#[test]
fn test_fabric_ai_agents_create_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set("fabric.create_ai_agent", 500, json!({"error": "x"}));
    let err = c
        .fabric()
        .ai_agents()
        .create(&serde_json::json!({}))
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(e.matched_route.as_deref(), Some("fabric.create_ai_agent"));
}

#[test]
fn test_fabric_ai_agents_delete_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c.fabric().ai_agents().delete("x");
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "DELETE");
    assert_eq!(e.matched_route.as_deref(), Some("fabric.delete_ai_agent"));
}

#[test]
fn test_fabric_ai_agents_delete_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set("fabric.delete_ai_agent", 500, json!({"error": "x"}));
    let err = c
        .fabric()
        .ai_agents()
        .delete("x")
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(e.matched_route.as_deref(), Some("fabric.delete_ai_agent"));
}

#[test]
fn test_fabric_ai_agents_get_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c.fabric().ai_agents().get("x");
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "GET");
    assert_eq!(e.matched_route.as_deref(), Some("fabric.get_ai_agent"));
}

#[test]
fn test_fabric_ai_agents_get_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set("fabric.get_ai_agent", 500, json!({"error": "x"}));
    let err = c
        .fabric()
        .ai_agents()
        .get("x")
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(e.matched_route.as_deref(), Some("fabric.get_ai_agent"));
}

#[test]
fn test_fabric_ai_agents_list_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c
        .fabric()
        .ai_agents()
        .list(&std::collections::HashMap::new());
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "GET");
    assert_eq!(e.matched_route.as_deref(), Some("fabric.list_ai_agents"));
}

#[test]
fn test_fabric_ai_agents_list_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set("fabric.list_ai_agents", 500, json!({"error": "x"}));
    let err = c
        .fabric()
        .ai_agents()
        .list(&std::collections::HashMap::new())
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(e.matched_route.as_deref(), Some("fabric.list_ai_agents"));
}

#[test]
fn test_fabric_ai_agents_list_addresses_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c
        .fabric()
        .ai_agents()
        .list_addresses("x", &std::collections::HashMap::new());
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "GET");
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.list_ai_agent_addresses")
    );
}

#[test]
fn test_fabric_ai_agents_list_addresses_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set("fabric.list_ai_agent_addresses", 500, json!({"error": "x"}));
    let err = c
        .fabric()
        .ai_agents()
        .list_addresses("x", &std::collections::HashMap::new())
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.list_ai_agent_addresses")
    );
}

#[test]
fn test_fabric_ai_agents_update_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c.fabric().ai_agents().update("x", &serde_json::json!({}));
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "PATCH");
    assert_eq!(e.matched_route.as_deref(), Some("fabric.update_ai_agent"));
}

#[test]
fn test_fabric_ai_agents_update_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set("fabric.update_ai_agent", 500, json!({"error": "x"}));
    let err = c
        .fabric()
        .ai_agents()
        .update("x", &serde_json::json!({}))
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(e.matched_route.as_deref(), Some("fabric.update_ai_agent"));
}

#[test]
fn test_fabric_call_flows_create_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c.fabric().call_flows().create(&serde_json::json!({}));
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "POST");
    assert_eq!(e.matched_route.as_deref(), Some("fabric.create_call_flow"));
}

#[test]
fn test_fabric_call_flows_create_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set("fabric.create_call_flow", 500, json!({"error": "x"}));
    let err = c
        .fabric()
        .call_flows()
        .create(&serde_json::json!({}))
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(e.matched_route.as_deref(), Some("fabric.create_call_flow"));
}

#[test]
fn test_fabric_call_flows_delete_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c.fabric().call_flows().delete("x");
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "DELETE");
    assert_eq!(e.matched_route.as_deref(), Some("fabric.delete_call_flow"));
}

#[test]
fn test_fabric_call_flows_delete_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set("fabric.delete_call_flow", 500, json!({"error": "x"}));
    let err = c
        .fabric()
        .call_flows()
        .delete("x")
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(e.matched_route.as_deref(), Some("fabric.delete_call_flow"));
}

#[test]
fn test_fabric_call_flows_deploy_version_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c
        .fabric()
        .call_flows()
        .deploy_version("x", &serde_json::json!({}));
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "POST");
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.deploy_call_flow_version")
    );
}

#[test]
fn test_fabric_call_flows_deploy_version_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set(
        "fabric.deploy_call_flow_version",
        500,
        json!({"error": "x"}),
    );
    let err = c
        .fabric()
        .call_flows()
        .deploy_version("x", &serde_json::json!({}))
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.deploy_call_flow_version")
    );
}

#[test]
fn test_fabric_call_flows_get_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c.fabric().call_flows().get("x");
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "GET");
    assert_eq!(e.matched_route.as_deref(), Some("fabric.get_call_flow"));
}

#[test]
fn test_fabric_call_flows_get_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set("fabric.get_call_flow", 500, json!({"error": "x"}));
    let err = c
        .fabric()
        .call_flows()
        .get("x")
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(e.matched_route.as_deref(), Some("fabric.get_call_flow"));
}

#[test]
fn test_fabric_call_flows_list_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c
        .fabric()
        .call_flows()
        .list(&std::collections::HashMap::new());
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "GET");
    assert_eq!(e.matched_route.as_deref(), Some("fabric.list_call_flows"));
}

#[test]
fn test_fabric_call_flows_list_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set("fabric.list_call_flows", 500, json!({"error": "x"}));
    let err = c
        .fabric()
        .call_flows()
        .list(&std::collections::HashMap::new())
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(e.matched_route.as_deref(), Some("fabric.list_call_flows"));
}

#[test]
fn test_fabric_call_flows_list_addresses_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c
        .fabric()
        .call_flows()
        .list_addresses("x", &std::collections::HashMap::new());
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "GET");
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.list_call_flow_addresses")
    );
}

#[test]
fn test_fabric_call_flows_list_addresses_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set(
        "fabric.list_call_flow_addresses",
        500,
        json!({"error": "x"}),
    );
    let err = c
        .fabric()
        .call_flows()
        .list_addresses("x", &std::collections::HashMap::new())
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.list_call_flow_addresses")
    );
}

#[test]
fn test_fabric_call_flows_list_versions_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c
        .fabric()
        .call_flows()
        .list_versions("x", &std::collections::HashMap::new());
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "GET");
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.list_call_flow_versions")
    );
}

#[test]
fn test_fabric_call_flows_list_versions_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set("fabric.list_call_flow_versions", 500, json!({"error": "x"}));
    let err = c
        .fabric()
        .call_flows()
        .list_versions("x", &std::collections::HashMap::new())
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.list_call_flow_versions")
    );
}

#[test]
fn test_fabric_call_flows_update_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c.fabric().call_flows().update("x", &serde_json::json!({}));
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "PUT");
    assert_eq!(e.matched_route.as_deref(), Some("fabric.update_call_flow"));
}

#[test]
fn test_fabric_call_flows_update_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set("fabric.update_call_flow", 500, json!({"error": "x"}));
    let err = c
        .fabric()
        .call_flows()
        .update("x", &serde_json::json!({}))
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(e.matched_route.as_deref(), Some("fabric.update_call_flow"));
}

#[test]
fn test_fabric_conference_rooms_create_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c.fabric().conference_rooms().create(&serde_json::json!({}));
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "POST");
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.create_conference_room")
    );
}

#[test]
fn test_fabric_conference_rooms_create_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set("fabric.create_conference_room", 500, json!({"error": "x"}));
    let err = c
        .fabric()
        .conference_rooms()
        .create(&serde_json::json!({}))
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.create_conference_room")
    );
}

#[test]
fn test_fabric_conference_rooms_delete_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c.fabric().conference_rooms().delete("x");
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "DELETE");
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.delete_conference_room")
    );
}

#[test]
fn test_fabric_conference_rooms_delete_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set("fabric.delete_conference_room", 500, json!({"error": "x"}));
    let err = c
        .fabric()
        .conference_rooms()
        .delete("x")
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.delete_conference_room")
    );
}

#[test]
fn test_fabric_conference_rooms_get_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c.fabric().conference_rooms().get("x");
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "GET");
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.get_conference_room")
    );
}

#[test]
fn test_fabric_conference_rooms_get_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set("fabric.get_conference_room", 500, json!({"error": "x"}));
    let err = c
        .fabric()
        .conference_rooms()
        .get("x")
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.get_conference_room")
    );
}

#[test]
fn test_fabric_conference_rooms_list_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c
        .fabric()
        .conference_rooms()
        .list(&std::collections::HashMap::new());
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "GET");
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.list_conference_rooms")
    );
}

#[test]
fn test_fabric_conference_rooms_list_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set("fabric.list_conference_rooms", 500, json!({"error": "x"}));
    let err = c
        .fabric()
        .conference_rooms()
        .list(&std::collections::HashMap::new())
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.list_conference_rooms")
    );
}

#[test]
fn test_fabric_conference_rooms_list_addresses_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c
        .fabric()
        .conference_rooms()
        .list_addresses("x", &std::collections::HashMap::new());
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "GET");
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.list_conference_room_addresses")
    );
}

#[test]
fn test_fabric_conference_rooms_list_addresses_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set(
        "fabric.list_conference_room_addresses",
        500,
        json!({"error": "x"}),
    );
    let err = c
        .fabric()
        .conference_rooms()
        .list_addresses("x", &std::collections::HashMap::new())
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.list_conference_room_addresses")
    );
}

#[test]
fn test_fabric_conference_rooms_update_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c
        .fabric()
        .conference_rooms()
        .update("x", &serde_json::json!({}));
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "PUT");
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.update_conference_room")
    );
}

#[test]
fn test_fabric_conference_rooms_update_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set("fabric.update_conference_room", 500, json!({"error": "x"}));
    let err = c
        .fabric()
        .conference_rooms()
        .update("x", &serde_json::json!({}))
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.update_conference_room")
    );
}

#[test]
fn test_fabric_cxml_applications_delete_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c.fabric().cxml_applications().delete("x");
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "DELETE");
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.delete_cxml_application")
    );
}

#[test]
fn test_fabric_cxml_applications_delete_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set("fabric.delete_cxml_application", 500, json!({"error": "x"}));
    let err = c
        .fabric()
        .cxml_applications()
        .delete("x")
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.delete_cxml_application")
    );
}

#[test]
fn test_fabric_cxml_applications_get_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c
        .fabric()
        .cxml_applications()
        .get("x", &std::collections::HashMap::new());
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "GET");
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.get_cxml_application")
    );
}

#[test]
fn test_fabric_cxml_applications_get_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set("fabric.get_cxml_application", 500, json!({"error": "x"}));
    let err = c
        .fabric()
        .cxml_applications()
        .get("x", &std::collections::HashMap::new())
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.get_cxml_application")
    );
}

#[test]
fn test_fabric_cxml_applications_list_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c
        .fabric()
        .cxml_applications()
        .list(&std::collections::HashMap::new());
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "GET");
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.list_cxml_applications")
    );
}

#[test]
fn test_fabric_cxml_applications_list_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set("fabric.list_cxml_applications", 500, json!({"error": "x"}));
    let err = c
        .fabric()
        .cxml_applications()
        .list(&std::collections::HashMap::new())
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.list_cxml_applications")
    );
}

#[test]
fn test_fabric_cxml_applications_list_addresses_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c
        .fabric()
        .cxml_applications()
        .list_addresses("x", &std::collections::HashMap::new());
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "GET");
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.list_cxml_application_addresses")
    );
}

#[test]
fn test_fabric_cxml_applications_list_addresses_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set(
        "fabric.list_cxml_application_addresses",
        500,
        json!({"error": "x"}),
    );
    let err = c
        .fabric()
        .cxml_applications()
        .list_addresses("x", &std::collections::HashMap::new())
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.list_cxml_application_addresses")
    );
}

#[test]
fn test_fabric_cxml_applications_update_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c
        .fabric()
        .cxml_applications()
        .update("x", fabric_gen::CxmlApplicationsUpdateRequest::new());
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "PUT");
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.update_cxml_application")
    );
}

#[test]
fn test_fabric_cxml_applications_update_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set("fabric.update_cxml_application", 500, json!({"error": "x"}));
    let err = c
        .fabric()
        .cxml_applications()
        .update("x", fabric_gen::CxmlApplicationsUpdateRequest::new())
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.update_cxml_application")
    );
}

#[test]
fn test_fabric_cxml_scripts_create_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c.fabric().cxml_scripts().create(&serde_json::json!({}));
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "POST");
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.create_cxml_script")
    );
}

#[test]
fn test_fabric_cxml_scripts_create_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set("fabric.create_cxml_script", 500, json!({"error": "x"}));
    let err = c
        .fabric()
        .cxml_scripts()
        .create(&serde_json::json!({}))
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.create_cxml_script")
    );
}

#[test]
fn test_fabric_cxml_scripts_delete_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c.fabric().cxml_scripts().delete("x");
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "DELETE");
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.delete_cxml_script")
    );
}

#[test]
fn test_fabric_cxml_scripts_delete_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set("fabric.delete_cxml_script", 500, json!({"error": "x"}));
    let err = c
        .fabric()
        .cxml_scripts()
        .delete("x")
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.delete_cxml_script")
    );
}

#[test]
fn test_fabric_cxml_scripts_get_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c.fabric().cxml_scripts().get("x");
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "GET");
    assert_eq!(e.matched_route.as_deref(), Some("fabric.get_cxml_script"));
}

#[test]
fn test_fabric_cxml_scripts_get_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set("fabric.get_cxml_script", 500, json!({"error": "x"}));
    let err = c
        .fabric()
        .cxml_scripts()
        .get("x")
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(e.matched_route.as_deref(), Some("fabric.get_cxml_script"));
}

#[test]
fn test_fabric_cxml_scripts_list_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c
        .fabric()
        .cxml_scripts()
        .list(&std::collections::HashMap::new());
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "GET");
    assert_eq!(e.matched_route.as_deref(), Some("fabric.list_cxml_scripts"));
}

#[test]
fn test_fabric_cxml_scripts_list_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set("fabric.list_cxml_scripts", 500, json!({"error": "x"}));
    let err = c
        .fabric()
        .cxml_scripts()
        .list(&std::collections::HashMap::new())
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(e.matched_route.as_deref(), Some("fabric.list_cxml_scripts"));
}

#[test]
fn test_fabric_cxml_scripts_list_addresses_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c
        .fabric()
        .cxml_scripts()
        .list_addresses("x", &std::collections::HashMap::new());
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "GET");
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.list_cxml_script_addresses")
    );
}

#[test]
fn test_fabric_cxml_scripts_list_addresses_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set(
        "fabric.list_cxml_script_addresses",
        500,
        json!({"error": "x"}),
    );
    let err = c
        .fabric()
        .cxml_scripts()
        .list_addresses("x", &std::collections::HashMap::new())
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.list_cxml_script_addresses")
    );
}

#[test]
fn test_fabric_cxml_scripts_update_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c
        .fabric()
        .cxml_scripts()
        .update("x", &serde_json::json!({}));
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "PUT");
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.update_cxml_script")
    );
}

#[test]
fn test_fabric_cxml_scripts_update_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set("fabric.update_cxml_script", 500, json!({"error": "x"}));
    let err = c
        .fabric()
        .cxml_scripts()
        .update("x", &serde_json::json!({}))
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.update_cxml_script")
    );
}

#[test]
fn test_fabric_cxml_webhooks_create_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c.fabric().cxml_webhooks().create(&serde_json::json!({}));
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "POST");
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.create_cxml_webhook")
    );
}

#[test]
fn test_fabric_cxml_webhooks_create_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set("fabric.create_cxml_webhook", 500, json!({"error": "x"}));
    let err = c
        .fabric()
        .cxml_webhooks()
        .create(&serde_json::json!({}))
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.create_cxml_webhook")
    );
}

#[test]
fn test_fabric_cxml_webhooks_delete_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c.fabric().cxml_webhooks().delete("x");
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "DELETE");
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.delete_cxml_webhook")
    );
}

#[test]
fn test_fabric_cxml_webhooks_delete_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set("fabric.delete_cxml_webhook", 500, json!({"error": "x"}));
    let err = c
        .fabric()
        .cxml_webhooks()
        .delete("x")
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.delete_cxml_webhook")
    );
}

#[test]
fn test_fabric_cxml_webhooks_get_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c.fabric().cxml_webhooks().get("x");
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "GET");
    assert_eq!(e.matched_route.as_deref(), Some("fabric.get_cxml_webhook"));
}

#[test]
fn test_fabric_cxml_webhooks_get_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set("fabric.get_cxml_webhook", 500, json!({"error": "x"}));
    let err = c
        .fabric()
        .cxml_webhooks()
        .get("x")
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(e.matched_route.as_deref(), Some("fabric.get_cxml_webhook"));
}

#[test]
fn test_fabric_cxml_webhooks_list_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c
        .fabric()
        .cxml_webhooks()
        .list(&std::collections::HashMap::new());
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "GET");
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.list_cxml_webhooks")
    );
}

#[test]
fn test_fabric_cxml_webhooks_list_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set("fabric.list_cxml_webhooks", 500, json!({"error": "x"}));
    let err = c
        .fabric()
        .cxml_webhooks()
        .list(&std::collections::HashMap::new())
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.list_cxml_webhooks")
    );
}

#[test]
fn test_fabric_cxml_webhooks_list_addresses_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c
        .fabric()
        .cxml_webhooks()
        .list_addresses("x", &std::collections::HashMap::new());
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "GET");
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.list_cxml_webhook_addresses")
    );
}

#[test]
fn test_fabric_cxml_webhooks_list_addresses_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set(
        "fabric.list_cxml_webhook_addresses",
        500,
        json!({"error": "x"}),
    );
    let err = c
        .fabric()
        .cxml_webhooks()
        .list_addresses("x", &std::collections::HashMap::new())
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.list_cxml_webhook_addresses")
    );
}

#[test]
fn test_fabric_cxml_webhooks_update_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c
        .fabric()
        .cxml_webhooks()
        .update("x", &serde_json::json!({}));
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "PATCH");
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.update_cxml_webhook")
    );
}

#[test]
fn test_fabric_cxml_webhooks_update_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set("fabric.update_cxml_webhook", 500, json!({"error": "x"}));
    let err = c
        .fabric()
        .cxml_webhooks()
        .update("x", &serde_json::json!({}))
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.update_cxml_webhook")
    );
}

#[test]
fn test_fabric_freeswitch_connectors_create_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c
        .fabric()
        .freeswitch_connectors()
        .create(&serde_json::json!({}));
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "POST");
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.create_freeswitch_connector")
    );
}

#[test]
fn test_fabric_freeswitch_connectors_create_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set(
        "fabric.create_freeswitch_connector",
        500,
        json!({"error": "x"}),
    );
    let err = c
        .fabric()
        .freeswitch_connectors()
        .create(&serde_json::json!({}))
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.create_freeswitch_connector")
    );
}

#[test]
fn test_fabric_freeswitch_connectors_delete_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c.fabric().freeswitch_connectors().delete("x");
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "DELETE");
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.delete_freeswitch_connector")
    );
}

#[test]
fn test_fabric_freeswitch_connectors_delete_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set(
        "fabric.delete_freeswitch_connector",
        500,
        json!({"error": "x"}),
    );
    let err = c
        .fabric()
        .freeswitch_connectors()
        .delete("x")
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.delete_freeswitch_connector")
    );
}

#[test]
fn test_fabric_freeswitch_connectors_get_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c.fabric().freeswitch_connectors().get("x");
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "GET");
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.get_freeswitch_connector")
    );
}

#[test]
fn test_fabric_freeswitch_connectors_get_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set(
        "fabric.get_freeswitch_connector",
        500,
        json!({"error": "x"}),
    );
    let err = c
        .fabric()
        .freeswitch_connectors()
        .get("x")
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.get_freeswitch_connector")
    );
}

#[test]
fn test_fabric_freeswitch_connectors_list_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c
        .fabric()
        .freeswitch_connectors()
        .list(&std::collections::HashMap::new());
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "GET");
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.list_freeswitch_connectors")
    );
}

#[test]
fn test_fabric_freeswitch_connectors_list_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set(
        "fabric.list_freeswitch_connectors",
        500,
        json!({"error": "x"}),
    );
    let err = c
        .fabric()
        .freeswitch_connectors()
        .list(&std::collections::HashMap::new())
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.list_freeswitch_connectors")
    );
}

#[test]
fn test_fabric_freeswitch_connectors_list_addresses_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c
        .fabric()
        .freeswitch_connectors()
        .list_addresses("x", &std::collections::HashMap::new());
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "GET");
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.list_freeswitch_connector_addresses")
    );
}

#[test]
fn test_fabric_freeswitch_connectors_list_addresses_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set(
        "fabric.list_freeswitch_connector_addresses",
        500,
        json!({"error": "x"}),
    );
    let err = c
        .fabric()
        .freeswitch_connectors()
        .list_addresses("x", &std::collections::HashMap::new())
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.list_freeswitch_connector_addresses")
    );
}

#[test]
fn test_fabric_freeswitch_connectors_update_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c
        .fabric()
        .freeswitch_connectors()
        .update("x", &serde_json::json!({}));
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "PUT");
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.update_freeswitch_connector")
    );
}

#[test]
fn test_fabric_freeswitch_connectors_update_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set(
        "fabric.update_freeswitch_connector",
        500,
        json!({"error": "x"}),
    );
    let err = c
        .fabric()
        .freeswitch_connectors()
        .update("x", &serde_json::json!({}))
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.update_freeswitch_connector")
    );
}

#[test]
fn test_fabric_relay_applications_create_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c
        .fabric()
        .relay_applications()
        .create(&serde_json::json!({}));
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "POST");
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.create_relay_application")
    );
}

#[test]
fn test_fabric_relay_applications_create_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set(
        "fabric.create_relay_application",
        500,
        json!({"error": "x"}),
    );
    let err = c
        .fabric()
        .relay_applications()
        .create(&serde_json::json!({}))
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.create_relay_application")
    );
}

#[test]
fn test_fabric_relay_applications_delete_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c.fabric().relay_applications().delete("x");
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "DELETE");
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.delete_relay_application")
    );
}

#[test]
fn test_fabric_relay_applications_delete_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set(
        "fabric.delete_relay_application",
        500,
        json!({"error": "x"}),
    );
    let err = c
        .fabric()
        .relay_applications()
        .delete("x")
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.delete_relay_application")
    );
}

#[test]
fn test_fabric_relay_applications_get_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c.fabric().relay_applications().get("x");
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "GET");
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.get_relay_application")
    );
}

#[test]
fn test_fabric_relay_applications_get_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set("fabric.get_relay_application", 500, json!({"error": "x"}));
    let err = c
        .fabric()
        .relay_applications()
        .get("x")
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.get_relay_application")
    );
}

#[test]
fn test_fabric_relay_applications_list_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c
        .fabric()
        .relay_applications()
        .list(&std::collections::HashMap::new());
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "GET");
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.list_relay_applications")
    );
}

#[test]
fn test_fabric_relay_applications_list_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set("fabric.list_relay_applications", 500, json!({"error": "x"}));
    let err = c
        .fabric()
        .relay_applications()
        .list(&std::collections::HashMap::new())
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.list_relay_applications")
    );
}

#[test]
fn test_fabric_relay_applications_list_addresses_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c
        .fabric()
        .relay_applications()
        .list_addresses("x", &std::collections::HashMap::new());
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "GET");
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.list_relay_application_addresses")
    );
}

#[test]
fn test_fabric_relay_applications_list_addresses_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set(
        "fabric.list_relay_application_addresses",
        500,
        json!({"error": "x"}),
    );
    let err = c
        .fabric()
        .relay_applications()
        .list_addresses("x", &std::collections::HashMap::new())
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.list_relay_application_addresses")
    );
}

#[test]
fn test_fabric_relay_applications_update_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c
        .fabric()
        .relay_applications()
        .update("x", &serde_json::json!({}));
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "PUT");
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.update_relay_application")
    );
}

#[test]
fn test_fabric_relay_applications_update_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set(
        "fabric.update_relay_application",
        500,
        json!({"error": "x"}),
    );
    let err = c
        .fabric()
        .relay_applications()
        .update("x", &serde_json::json!({}))
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.update_relay_application")
    );
}

#[test]
fn test_fabric_resources_assign_domain_application_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c.fabric().resources().assign_domain_application(
        "x",
        fabric_gen::GenericResourcesAssignDomainApplicationRequest::new("x"),
    );
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "POST");
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.assign_resource_domain_application")
    );
}

#[test]
fn test_fabric_resources_assign_domain_application_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set(
        "fabric.assign_resource_domain_application",
        500,
        json!({"error": "x"}),
    );
    let err = c
        .fabric()
        .resources()
        .assign_domain_application(
            "x",
            fabric_gen::GenericResourcesAssignDomainApplicationRequest::new("x"),
        )
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.assign_resource_domain_application")
    );
}

#[test]
fn test_fabric_resources_assign_phone_route_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c.fabric().resources().assign_phone_route(
        "x",
        fabric_gen::GenericResourcesAssignPhoneRouteRequest::new("x", "y"),
    );
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "POST");
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.assign_resource_phone_route")
    );
}

#[test]
fn test_fabric_resources_assign_phone_route_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set(
        "fabric.assign_resource_phone_route",
        500,
        json!({"error": "x"}),
    );
    let err = c
        .fabric()
        .resources()
        .assign_phone_route(
            "x",
            fabric_gen::GenericResourcesAssignPhoneRouteRequest::new("x", "y"),
        )
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.assign_resource_phone_route")
    );
}

#[test]
fn test_fabric_resources_delete_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c.fabric().resources().delete("x");
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "DELETE");
    assert_eq!(e.matched_route.as_deref(), Some("fabric.delete_resource"));
}

#[test]
fn test_fabric_resources_delete_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set("fabric.delete_resource", 500, json!({"error": "x"}));
    let err = c
        .fabric()
        .resources()
        .delete("x")
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(e.matched_route.as_deref(), Some("fabric.delete_resource"));
}

#[test]
fn test_fabric_resources_get_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c
        .fabric()
        .resources()
        .get("x", &std::collections::HashMap::new());
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "GET");
    assert_eq!(e.matched_route.as_deref(), Some("fabric.get_resource"));
}

#[test]
fn test_fabric_resources_get_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set("fabric.get_resource", 500, json!({"error": "x"}));
    let err = c
        .fabric()
        .resources()
        .get("x", &std::collections::HashMap::new())
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(e.matched_route.as_deref(), Some("fabric.get_resource"));
}

#[test]
fn test_fabric_resources_list_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c
        .fabric()
        .resources()
        .list(&std::collections::HashMap::new());
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "GET");
    assert_eq!(e.matched_route.as_deref(), Some("fabric.list_resources"));
}

#[test]
fn test_fabric_resources_list_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set("fabric.list_resources", 500, json!({"error": "x"}));
    let err = c
        .fabric()
        .resources()
        .list(&std::collections::HashMap::new())
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(e.matched_route.as_deref(), Some("fabric.list_resources"));
}

#[test]
fn test_fabric_resources_list_addresses_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c
        .fabric()
        .resources()
        .list_addresses("x", &std::collections::HashMap::new());
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "GET");
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.list_resource_addresses")
    );
}

#[test]
fn test_fabric_resources_list_addresses_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set("fabric.list_resource_addresses", 500, json!({"error": "x"}));
    let err = c
        .fabric()
        .resources()
        .list_addresses("x", &std::collections::HashMap::new())
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.list_resource_addresses")
    );
}

#[test]
fn test_fabric_sip_endpoints_create_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c.fabric().sip_endpoints().create(&serde_json::json!({}));
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "POST");
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.create_sip_endpoint")
    );
}

#[test]
fn test_fabric_sip_endpoints_create_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set("fabric.create_sip_endpoint", 500, json!({"error": "x"}));
    let err = c
        .fabric()
        .sip_endpoints()
        .create(&serde_json::json!({}))
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.create_sip_endpoint")
    );
}

#[test]
fn test_fabric_sip_endpoints_delete_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c.fabric().sip_endpoints().delete("x");
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "DELETE");
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.delete_sip_endpoint")
    );
}

#[test]
fn test_fabric_sip_endpoints_delete_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set("fabric.delete_sip_endpoint", 500, json!({"error": "x"}));
    let err = c
        .fabric()
        .sip_endpoints()
        .delete("x")
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.delete_sip_endpoint")
    );
}

#[test]
fn test_fabric_sip_endpoints_get_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c.fabric().sip_endpoints().get("x");
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "GET");
    assert_eq!(e.matched_route.as_deref(), Some("fabric.get_sip_endpoint"));
}

#[test]
fn test_fabric_sip_endpoints_get_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set("fabric.get_sip_endpoint", 500, json!({"error": "x"}));
    let err = c
        .fabric()
        .sip_endpoints()
        .get("x")
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(e.matched_route.as_deref(), Some("fabric.get_sip_endpoint"));
}

#[test]
fn test_fabric_sip_endpoints_list_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c
        .fabric()
        .sip_endpoints()
        .list(&std::collections::HashMap::new());
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "GET");
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.list_sip_endpoints")
    );
}

#[test]
fn test_fabric_sip_endpoints_list_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set("fabric.list_sip_endpoints", 500, json!({"error": "x"}));
    let err = c
        .fabric()
        .sip_endpoints()
        .list(&std::collections::HashMap::new())
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.list_sip_endpoints")
    );
}

#[test]
fn test_fabric_sip_endpoints_list_addresses_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c
        .fabric()
        .sip_endpoints()
        .list_addresses("x", &std::collections::HashMap::new());
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "GET");
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.list_sip_endpoint_addresses")
    );
}

#[test]
fn test_fabric_sip_endpoints_list_addresses_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set(
        "fabric.list_sip_endpoint_addresses",
        500,
        json!({"error": "x"}),
    );
    let err = c
        .fabric()
        .sip_endpoints()
        .list_addresses("x", &std::collections::HashMap::new())
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.list_sip_endpoint_addresses")
    );
}

#[test]
fn test_fabric_sip_endpoints_update_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c
        .fabric()
        .sip_endpoints()
        .update("x", &serde_json::json!({}));
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "PUT");
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.update_sip_endpoint")
    );
}

#[test]
fn test_fabric_sip_endpoints_update_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set("fabric.update_sip_endpoint", 500, json!({"error": "x"}));
    let err = c
        .fabric()
        .sip_endpoints()
        .update("x", &serde_json::json!({}))
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.update_sip_endpoint")
    );
}

#[test]
fn test_fabric_sip_gateways_create_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c.fabric().sip_gateways().create(&serde_json::json!({}));
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "POST");
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.create_sip_gateway")
    );
}

#[test]
fn test_fabric_sip_gateways_create_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set("fabric.create_sip_gateway", 500, json!({"error": "x"}));
    let err = c
        .fabric()
        .sip_gateways()
        .create(&serde_json::json!({}))
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.create_sip_gateway")
    );
}

#[test]
fn test_fabric_sip_gateways_delete_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c.fabric().sip_gateways().delete("x");
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "DELETE");
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.delete_sip_gateway")
    );
}

#[test]
fn test_fabric_sip_gateways_delete_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set("fabric.delete_sip_gateway", 500, json!({"error": "x"}));
    let err = c
        .fabric()
        .sip_gateways()
        .delete("x")
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.delete_sip_gateway")
    );
}

#[test]
fn test_fabric_sip_gateways_get_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c.fabric().sip_gateways().get("x");
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "GET");
    assert_eq!(e.matched_route.as_deref(), Some("fabric.get_sip_gateway"));
}

#[test]
fn test_fabric_sip_gateways_get_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set("fabric.get_sip_gateway", 500, json!({"error": "x"}));
    let err = c
        .fabric()
        .sip_gateways()
        .get("x")
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(e.matched_route.as_deref(), Some("fabric.get_sip_gateway"));
}

#[test]
fn test_fabric_sip_gateways_list_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c
        .fabric()
        .sip_gateways()
        .list(&std::collections::HashMap::new());
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "GET");
    assert_eq!(e.matched_route.as_deref(), Some("fabric.list_sip_gateways"));
}

#[test]
fn test_fabric_sip_gateways_list_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set("fabric.list_sip_gateways", 500, json!({"error": "x"}));
    let err = c
        .fabric()
        .sip_gateways()
        .list(&std::collections::HashMap::new())
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(e.matched_route.as_deref(), Some("fabric.list_sip_gateways"));
}

#[test]
fn test_fabric_sip_gateways_list_addresses_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c
        .fabric()
        .sip_gateways()
        .list_addresses("x", &std::collections::HashMap::new());
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "GET");
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.list_sip_gateway_addresses")
    );
}

#[test]
fn test_fabric_sip_gateways_list_addresses_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set(
        "fabric.list_sip_gateway_addresses",
        500,
        json!({"error": "x"}),
    );
    let err = c
        .fabric()
        .sip_gateways()
        .list_addresses("x", &std::collections::HashMap::new())
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.list_sip_gateway_addresses")
    );
}

#[test]
fn test_fabric_sip_gateways_update_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c
        .fabric()
        .sip_gateways()
        .update("x", &serde_json::json!({}));
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "PATCH");
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.update_sip_gateway")
    );
}

#[test]
fn test_fabric_sip_gateways_update_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set("fabric.update_sip_gateway", 500, json!({"error": "x"}));
    let err = c
        .fabric()
        .sip_gateways()
        .update("x", &serde_json::json!({}))
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.update_sip_gateway")
    );
}

#[test]
fn test_fabric_subscribers_create_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c.fabric().subscribers().create(&serde_json::json!({}));
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "POST");
    assert_eq!(e.matched_route.as_deref(), Some("fabric.create_subscriber"));
}

#[test]
fn test_fabric_subscribers_create_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set("fabric.create_subscriber", 500, json!({"error": "x"}));
    let err = c
        .fabric()
        .subscribers()
        .create(&serde_json::json!({}))
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(e.matched_route.as_deref(), Some("fabric.create_subscriber"));
}

#[test]
fn test_fabric_subscribers_create_sip_endpoint_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c.fabric().subscribers().create_sip_endpoint(
        "x",
        fabric_gen::SubscribersCreateSipEndpointRequest::new("x", "y"),
    );
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "POST");
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.create_subscriber_sip_endpoint")
    );
}

#[test]
fn test_fabric_subscribers_create_sip_endpoint_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set(
        "fabric.create_subscriber_sip_endpoint",
        500,
        json!({"error": "x"}),
    );
    let err = c
        .fabric()
        .subscribers()
        .create_sip_endpoint(
            "x",
            fabric_gen::SubscribersCreateSipEndpointRequest::new("x", "y"),
        )
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.create_subscriber_sip_endpoint")
    );
}

#[test]
fn test_fabric_subscribers_delete_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c.fabric().subscribers().delete("x");
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "DELETE");
    assert_eq!(e.matched_route.as_deref(), Some("fabric.delete_subscriber"));
}

#[test]
fn test_fabric_subscribers_delete_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set("fabric.delete_subscriber", 500, json!({"error": "x"}));
    let err = c
        .fabric()
        .subscribers()
        .delete("x")
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(e.matched_route.as_deref(), Some("fabric.delete_subscriber"));
}

#[test]
fn test_fabric_subscribers_delete_sip_endpoint_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c.fabric().subscribers().delete_sip_endpoint("x", "x");
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "DELETE");
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.delete_subscriber_sip_endpoint")
    );
}

#[test]
fn test_fabric_subscribers_delete_sip_endpoint_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set(
        "fabric.delete_subscriber_sip_endpoint",
        500,
        json!({"error": "x"}),
    );
    let err = c
        .fabric()
        .subscribers()
        .delete_sip_endpoint("x", "x")
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.delete_subscriber_sip_endpoint")
    );
}

#[test]
fn test_fabric_subscribers_get_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c.fabric().subscribers().get("x");
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "GET");
    assert_eq!(e.matched_route.as_deref(), Some("fabric.get_subscriber"));
}

#[test]
fn test_fabric_subscribers_get_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set("fabric.get_subscriber", 500, json!({"error": "x"}));
    let err = c
        .fabric()
        .subscribers()
        .get("x")
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(e.matched_route.as_deref(), Some("fabric.get_subscriber"));
}

#[test]
fn test_fabric_subscribers_get_sip_endpoint_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c
        .fabric()
        .subscribers()
        .get_sip_endpoint("x", "x", &std::collections::HashMap::new());
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "GET");
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.get_subscriber_sip_endpoint")
    );
}

#[test]
fn test_fabric_subscribers_get_sip_endpoint_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set(
        "fabric.get_subscriber_sip_endpoint",
        500,
        json!({"error": "x"}),
    );
    let err = c
        .fabric()
        .subscribers()
        .get_sip_endpoint("x", "x", &std::collections::HashMap::new())
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.get_subscriber_sip_endpoint")
    );
}

#[test]
fn test_fabric_subscribers_list_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c
        .fabric()
        .subscribers()
        .list(&std::collections::HashMap::new());
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "GET");
    assert_eq!(e.matched_route.as_deref(), Some("fabric.list_subscribers"));
}

#[test]
fn test_fabric_subscribers_list_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set("fabric.list_subscribers", 500, json!({"error": "x"}));
    let err = c
        .fabric()
        .subscribers()
        .list(&std::collections::HashMap::new())
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(e.matched_route.as_deref(), Some("fabric.list_subscribers"));
}

#[test]
fn test_fabric_subscribers_list_addresses_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c
        .fabric()
        .subscribers()
        .list_addresses("x", &std::collections::HashMap::new());
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "GET");
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.list_subscriber_addresses")
    );
}

#[test]
fn test_fabric_subscribers_list_addresses_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set(
        "fabric.list_subscriber_addresses",
        500,
        json!({"error": "x"}),
    );
    let err = c
        .fabric()
        .subscribers()
        .list_addresses("x", &std::collections::HashMap::new())
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.list_subscriber_addresses")
    );
}

#[test]
fn test_fabric_subscribers_list_sip_endpoints_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c
        .fabric()
        .subscribers()
        .list_sip_endpoints("x", &std::collections::HashMap::new());
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "GET");
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.list_subscriber_sip_endpoints")
    );
}

#[test]
fn test_fabric_subscribers_list_sip_endpoints_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set(
        "fabric.list_subscriber_sip_endpoints",
        500,
        json!({"error": "x"}),
    );
    let err = c
        .fabric()
        .subscribers()
        .list_sip_endpoints("x", &std::collections::HashMap::new())
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.list_subscriber_sip_endpoints")
    );
}

#[test]
fn test_fabric_subscribers_update_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c.fabric().subscribers().update("x", &serde_json::json!({}));
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "PUT");
    assert_eq!(e.matched_route.as_deref(), Some("fabric.update_subscriber"));
}

#[test]
fn test_fabric_subscribers_update_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set("fabric.update_subscriber", 500, json!({"error": "x"}));
    let err = c
        .fabric()
        .subscribers()
        .update("x", &serde_json::json!({}))
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(e.matched_route.as_deref(), Some("fabric.update_subscriber"));
}

#[test]
fn test_fabric_subscribers_update_sip_endpoint_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c.fabric().subscribers().update_sip_endpoint(
        "x",
        "x",
        fabric_gen::SubscribersUpdateSipEndpointRequest::new(),
    );
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "PATCH");
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.update_subscriber_sip_endpoint")
    );
}

#[test]
fn test_fabric_subscribers_update_sip_endpoint_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set(
        "fabric.update_subscriber_sip_endpoint",
        500,
        json!({"error": "x"}),
    );
    let err = c
        .fabric()
        .subscribers()
        .update_sip_endpoint(
            "x",
            "x",
            fabric_gen::SubscribersUpdateSipEndpointRequest::new(),
        )
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.update_subscriber_sip_endpoint")
    );
}

#[test]
fn test_fabric_swml_scripts_create_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c.fabric().swml_scripts().create(&serde_json::json!({}));
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "POST");
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.create_swml_script")
    );
}

#[test]
fn test_fabric_swml_scripts_create_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set("fabric.create_swml_script", 500, json!({"error": "x"}));
    let err = c
        .fabric()
        .swml_scripts()
        .create(&serde_json::json!({}))
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.create_swml_script")
    );
}

#[test]
fn test_fabric_swml_scripts_delete_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c.fabric().swml_scripts().delete("x");
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "DELETE");
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.delete_swml_script")
    );
}

#[test]
fn test_fabric_swml_scripts_delete_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set("fabric.delete_swml_script", 500, json!({"error": "x"}));
    let err = c
        .fabric()
        .swml_scripts()
        .delete("x")
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.delete_swml_script")
    );
}

#[test]
fn test_fabric_swml_scripts_get_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c.fabric().swml_scripts().get("x");
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "GET");
    assert_eq!(e.matched_route.as_deref(), Some("fabric.get_swml_script"));
}

#[test]
fn test_fabric_swml_scripts_get_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set("fabric.get_swml_script", 500, json!({"error": "x"}));
    let err = c
        .fabric()
        .swml_scripts()
        .get("x")
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(e.matched_route.as_deref(), Some("fabric.get_swml_script"));
}

#[test]
fn test_fabric_swml_scripts_list_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c
        .fabric()
        .swml_scripts()
        .list(&std::collections::HashMap::new());
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "GET");
    assert_eq!(e.matched_route.as_deref(), Some("fabric.list_swml_scripts"));
}

#[test]
fn test_fabric_swml_scripts_list_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set("fabric.list_swml_scripts", 500, json!({"error": "x"}));
    let err = c
        .fabric()
        .swml_scripts()
        .list(&std::collections::HashMap::new())
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(e.matched_route.as_deref(), Some("fabric.list_swml_scripts"));
}

#[test]
fn test_fabric_swml_scripts_list_addresses_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c
        .fabric()
        .swml_scripts()
        .list_addresses("x", &std::collections::HashMap::new());
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "GET");
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.list_swml_script_addresses")
    );
}

#[test]
fn test_fabric_swml_scripts_list_addresses_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set(
        "fabric.list_swml_script_addresses",
        500,
        json!({"error": "x"}),
    );
    let err = c
        .fabric()
        .swml_scripts()
        .list_addresses("x", &std::collections::HashMap::new())
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.list_swml_script_addresses")
    );
}

#[test]
fn test_fabric_swml_scripts_update_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c
        .fabric()
        .swml_scripts()
        .update("x", &serde_json::json!({}));
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "PUT");
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.update_swml_script")
    );
}

#[test]
fn test_fabric_swml_scripts_update_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set("fabric.update_swml_script", 500, json!({"error": "x"}));
    let err = c
        .fabric()
        .swml_scripts()
        .update("x", &serde_json::json!({}))
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.update_swml_script")
    );
}

#[test]
fn test_fabric_swml_webhooks_create_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c.fabric().swml_webhooks().create(&serde_json::json!({}));
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "POST");
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.create_swml_webhook")
    );
}

#[test]
fn test_fabric_swml_webhooks_create_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set("fabric.create_swml_webhook", 500, json!({"error": "x"}));
    let err = c
        .fabric()
        .swml_webhooks()
        .create(&serde_json::json!({}))
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.create_swml_webhook")
    );
}

#[test]
fn test_fabric_swml_webhooks_delete_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c.fabric().swml_webhooks().delete("x");
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "DELETE");
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.delete_swml_webhook")
    );
}

#[test]
fn test_fabric_swml_webhooks_delete_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set("fabric.delete_swml_webhook", 500, json!({"error": "x"}));
    let err = c
        .fabric()
        .swml_webhooks()
        .delete("x")
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.delete_swml_webhook")
    );
}

#[test]
fn test_fabric_swml_webhooks_get_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c.fabric().swml_webhooks().get("x");
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "GET");
    assert_eq!(e.matched_route.as_deref(), Some("fabric.get_swml_webhook"));
}

#[test]
fn test_fabric_swml_webhooks_get_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set("fabric.get_swml_webhook", 500, json!({"error": "x"}));
    let err = c
        .fabric()
        .swml_webhooks()
        .get("x")
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(e.matched_route.as_deref(), Some("fabric.get_swml_webhook"));
}

#[test]
fn test_fabric_swml_webhooks_list_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c
        .fabric()
        .swml_webhooks()
        .list(&std::collections::HashMap::new());
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "GET");
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.list_swml_webhooks")
    );
}

#[test]
fn test_fabric_swml_webhooks_list_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set("fabric.list_swml_webhooks", 500, json!({"error": "x"}));
    let err = c
        .fabric()
        .swml_webhooks()
        .list(&std::collections::HashMap::new())
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.list_swml_webhooks")
    );
}

#[test]
fn test_fabric_swml_webhooks_list_addresses_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c
        .fabric()
        .swml_webhooks()
        .list_addresses("x", &std::collections::HashMap::new());
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "GET");
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.list_swml_webhook_addresses")
    );
}

#[test]
fn test_fabric_swml_webhooks_list_addresses_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set(
        "fabric.list_swml_webhook_addresses",
        500,
        json!({"error": "x"}),
    );
    let err = c
        .fabric()
        .swml_webhooks()
        .list_addresses("x", &std::collections::HashMap::new())
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.list_swml_webhook_addresses")
    );
}

#[test]
fn test_fabric_swml_webhooks_update_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c
        .fabric()
        .swml_webhooks()
        .update("x", &serde_json::json!({}));
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "PATCH");
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.update_swml_webhook")
    );
}

#[test]
fn test_fabric_swml_webhooks_update_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set("fabric.update_swml_webhook", 500, json!({"error": "x"}));
    let err = c
        .fabric()
        .swml_webhooks()
        .update("x", &serde_json::json!({}))
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.update_swml_webhook")
    );
}

#[test]
fn test_fabric_tokens_create_embed_token_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c
        .fabric()
        .tokens()
        .create_embed_token(fabric_gen::FabricTokensCreateEmbedTokenRequest::new("x"));
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "POST");
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.create_embeds_token")
    );
}

#[test]
fn test_fabric_tokens_create_embed_token_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set("fabric.create_embeds_token", 500, json!({"error": "x"}));
    let err = c
        .fabric()
        .tokens()
        .create_embed_token(fabric_gen::FabricTokensCreateEmbedTokenRequest::new("x"))
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.create_embeds_token")
    );
}

#[test]
fn test_fabric_tokens_create_guest_token_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c.fabric().tokens().create_guest_token(
        fabric_gen::FabricTokensCreateGuestTokenRequest::new(serde_json::json!({})),
    );
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "POST");
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.create_subscriber_guest_token")
    );
}

#[test]
fn test_fabric_tokens_create_guest_token_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set(
        "fabric.create_subscriber_guest_token",
        500,
        json!({"error": "x"}),
    );
    let err = c
        .fabric()
        .tokens()
        .create_guest_token(fabric_gen::FabricTokensCreateGuestTokenRequest::new(
            serde_json::json!({}),
        ))
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.create_subscriber_guest_token")
    );
}

#[test]
fn test_fabric_tokens_create_invite_token_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c
        .fabric()
        .tokens()
        .create_invite_token(fabric_gen::FabricTokensCreateInviteTokenRequest::new("x"));
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "POST");
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.create_subscriber_invite_token")
    );
}

#[test]
fn test_fabric_tokens_create_invite_token_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set(
        "fabric.create_subscriber_invite_token",
        500,
        json!({"error": "x"}),
    );
    let err = c
        .fabric()
        .tokens()
        .create_invite_token(fabric_gen::FabricTokensCreateInviteTokenRequest::new("x"))
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.create_subscriber_invite_token")
    );
}

#[test]
fn test_fabric_tokens_create_subscriber_token_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c.fabric().tokens().create_subscriber_token(
        fabric_gen::FabricTokensCreateSubscriberTokenRequest::new("x"),
    );
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "POST");
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.create_subscriber_token")
    );
}

#[test]
fn test_fabric_tokens_create_subscriber_token_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set("fabric.create_subscriber_token", 500, json!({"error": "x"}));
    let err = c
        .fabric()
        .tokens()
        .create_subscriber_token(fabric_gen::FabricTokensCreateSubscriberTokenRequest::new(
            "x",
        ))
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.create_subscriber_token")
    );
}

#[test]
fn test_fabric_tokens_refresh_subscriber_token_success() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    let _ = c.fabric().tokens().refresh_subscriber_token(
        fabric_gen::FabricTokensRefreshSubscriberTokenRequest::new("x"),
    );
    let e = common::mocktest::journal_last();
    assert_eq!(e.method, "POST");
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.refresh_subscriber_token")
    );
}

#[test]
fn test_fabric_tokens_refresh_subscriber_token_error() {
    let _g = common::mocktest::begin();
    let c = common::mocktest::client();
    common::mocktest::scenario_set(
        "fabric.refresh_subscriber_token",
        500,
        json!({"error": "x"}),
    );
    let err = c
        .fabric()
        .tokens()
        .refresh_subscriber_token(fabric_gen::FabricTokensRefreshSubscriberTokenRequest::new(
            "x",
        ))
        .expect_err("expected a 500 error");
    assert_eq!(err.status_code(), 500);
    let e = common::mocktest::journal_last();
    assert_eq!(e.response_status, Some(500));
    assert_eq!(
        e.matched_route.as_deref(),
        Some("fabric.refresh_subscriber_token")
    );
}
